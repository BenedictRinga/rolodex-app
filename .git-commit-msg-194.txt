build 194: THE SIGNAL DETECTOR (founder: "the very windows we presented to users to exit to third-party apps is a veritable signal detector stage - catch every copy, and even assuming they do not continue beyond to actually SMS/WhatsApp/share, that event by itself, and the ends of our user-AI Assistant chat, are reducible to measurable logs over time, by themselves or by cross-matching with volume"). (1) send_exit { channel, surface } fires AT THE TAP on every exit door, completion or not - categorical channel + surface only, never draft text: the Confidante composer's five doors (sms/email/whatsapp/in-app/copy - the ENTIRE AI->draft->send funnel previously fired ZERO analytics), the contact-card message menu (whatsapp/sms/email/copy), the home chat's COPY (copyRolodexAi - one of the ENDS of the user-AI conversation, previously untracked), and the inbox's intro-note + voice-outline copies. (2) message_sent now carries props.channel + surface:'loop' - the fire-and-close deed; LoopsService.markSent received the channel parameter all along and threw it away; walk/shelf/card/agent doors all funnel through it, so the per-channel funnel needs no new taps. (3) Command Center Trends gains the CHANNEL FUNNEL table (server build 74 e6df0af: channelExits = door taps vs sends per channel, 30d, organic) - exits > sends per channel is the drop-off to read, cross-matchable with the dailyEvents volume matrix. No user-facing app flow changed - the doors only gained a voice. Proofs: yarn build typecheck clean (Done in 67.48s); yarn build:prod clean (Done in 75.42s); base href /loopkeeper/; build:194 in main bundle; send_exit + "Channel (30d)" in chunk 4789.
