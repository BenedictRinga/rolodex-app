export const environment = {
  production: true,
  // 2026-08-26 CANONICAL API PATH: /api/loopkeeper (rolodex-server keeps
  // /api/openloop + /api/rolodex as backward-compatible aliases).
  rolodexApiBase: 'https://zyppar.com/api/loopkeeper',
  version: '0.3.1', // LoopKeeper BASE PAIR — the DISPLAYED version composes as 0.3.<build> (updates.service composeVersion, build 263)
build: 302, // 2026-09-22 BUILD 302 THE EXIT'S SUNNY DAY, RESTORED (founder: 'Exit/delete is in Settings, and at the end of that sequence we had regrets, immediately after which app automatically returned user in again. I do not know which sequence you attached the sunny page to then.' + 'The sunny page has nothing to do with loops. Go back to the original request for sunny day - you have gone into hallucinations since.'). THE FOUNDER IS RIGHT AND THE WIRING IS NOW EXACTLY AS ORIGINALLY ASKED. THE SUNNY DAY BELONGS TO ONE MOMENT: the END of the Settings wipe (Settings -> Clear my LoopKeeper data -> rolodex.wipeAllAndReload): wipe everything -> the farewell beat (the regrets, 2.2s) -> THE FULL-PAGE SUNNY DAY - NOT the reload. The window.location.replace that auto-returned the user into the app IS GONE; rolodex.finishWipe() performs that same _wipe reload ONLY when the ARMED LoopKeeper logo at the sunny day's base is tapped - THE USER'S ACT, never an automatic return. REVERTED the 299/301 wirings (the sunny page has nothing to do with loops): the inbox loop drop emit is gone (a drop is a drop - the tray refreshes, nothing hijacks the screen), the deck-delete sunny day is gone. THE PANEL'S FACE IS NOW EXACTLY THREE THINGS (founder #2: 'Name the first...' and 'Show me first' should be equally out of sight behind the Or start with'): the two avoidance doors, the Or-start-with button, the courtesy line - the welcome title, the legacy TASK/PERSON doors, and Show me first + the four-beat demo ALL live behind the button until tapped (fm_legacy {open} logged). The gate still resets when the real deck empties (the 297 law - the journey un-started). NO i18n changes. MEASURED: typecheck clean; prod clean; proofs in the commit. DEPLOY: app www 232-302. VERIFY: Settings -> Clear my LoopKeeper data -> Erase everything -> the erasing veil -> the farewell -> THE SUNNY DAY (not the app); nothing happens until the pulsing logo is tapped; the tap births the fresh device (the first-timer cover returns); the panel shows ONLY the two doors + Or start with + courtesy. Previous: build: 301, // 2026-09-22 BUILD 301 THE RESET + THE OBVIOUS DOOR (founder, after deploying 300: 'The after-exit sunny day page not showing. Just same old behavior of immediate log in once more of user.' + 'Make Or start with an obvious button, which when tapped, reveals the TASK || PERSON, pushing down the Welcome to LoopKeeper...' + 'You have regressed the app in this latest commit - collapsed the cover into the original first page.'). THREE FIXES. (1) A USER DELETING FROM LOOPKEEPER IS AN EXIT: the deck delete now fires the SUNNY DAY (home.onContactsChange decrease branch -> sunnyOn, sunny_shown {surface:'deck-delete'}) exactly as the loop drop already did (the inbox drop wiring was verified LIVE in the deployed 300 bundle - sunny_return/droppedWhat present in chunk 2688). (2) THE GATE RESETS WHEN THE JOURNEY UN-STARTS: when the REAL deck EMPTIES (the user deleted everyone), lk_cover_engaged + lk_firstminute_done are CLEARED and firstMinuteActive re-arms - the cover returns THIS SESSION (after the logo tap) and on the next boot (the 297 law verbatim: 'return resets the gate to untapped/untouched/undecided ie. journey away from procrastination state has not started'). The cover that 'collapsed into the original first page' was the gate flag persisting from the founder's own testing - the law working as written; the reset restores it after an honest exit. (3) THE PANEL LEADS: the two avoidance doors are now the FIRST thing on the first-minute panel (the welcome title pushed DOWN below them - 'pushing down the Welcome to LoopKeeper...'), and the legacy TASK / PERSON doors hide behind an OBVIOUS 'Or start with' BUTTON (amber pill, chevron; fm_legacy {open} logged) until tapped. No i18n changes (fm.orlabel reused as the button label). MEASURED: typecheck clean; prod clean; proofs in the commit. DEPLOY: app www 232-301. VERIFY: delete a card from the deck -> the sunny day; delete the LAST real card -> after the logo tap the COVER is back (reset); fresh device -> the cover leads with the two avoidance doors; 'Or start with' reveals TASK/PERSON and pushes the welcome down. Previous: build: 300, // 2026-09-22 BUILD 300 THE WHY ON THE FACE (the strategic brief's move 3: 'whySitting on the face of the card, one-tap correctable, draft changes with the answer'). THE WHY IS ON THE FACE AND ONE-TAP CORRECTABLE: the inbox's loop rows already showed the why line (li-why) - it is now TAPPABLE and opens the FOUR HONEST FRICTIONS right on the row ('It's awkward' / 'I don't know what I think yet' / 'It might start a fight' / 'It's just boring'); a chip tap calls the new loops.nameWhy(id, reason): whySittingSource flips 'suggested' -> 'user' (the engine's suggestion yields to the human's naming - F13's own law) and THE DRAFT CHANGES WITH THE ANSWER - generateDraft weaves the why ('Still here because ...', the owed-reply tones read it directly), so the draft is regenerated with the new friction UNLESS the words are the user's own (ownWords honored - never overwritten; build 194's law). Same chips on the WALK's slide 3, above the draft (the avoider names it IN the send flow - the draft below changes live; loops.update mutates the cache entry in place so the display follows). New analytics why_named {source:'chip', surface:'inbox'|'walk'}. Copy never forks: whyChipKeys() lives ONCE in the loops service (whyChipKeys = ['awkward','think','fight','boring']), both surfaces translate loopkeeper.why.chip.<k>. i18n: 5 new flat keys (why.label + 4 chips) across all 39 locales - 690 flat keys/file parity verified, 17 majors hand-translated (sw fr es de ar ru he hi ja zh-Hans zh-Hant it nl pl tr ha), minors + am carry EN deliberately this round (the nuance words were too risky to guess). MEASURED: typecheck clean; prod clean; proofs in the commit. DEPLOY: app www 232-300. VERIFY: open the Loops inbox -> an open loop row shows its why -> tap it -> four chips -> tap one -> the why re-names AND the row's draft (open the row) carries the new friction; walk slide 3 shows the same chips above the draft. Previous: build: 299, // 2026-09-22 BUILD 299 THE SUNNY DAY (founder, the horror-boyfriend rule: 'our regrets when a user exits/deletes from LoopKeeper, should not be followed immediately by loading them back into the app, as we currently do. We should not be the horror boyfriend. Imagine for a full page sunny day to follow, with an armed LoopKeeper logo at base, tapping of which returns user. It has to be their act.'). A USER'S DROP NOW LEADS TO THE QUIET, NOT THE NEXT THING: the inbox's confirmDrop relays droppedWhat -> home presents <app-sunny-day> - a FULL-PAGE sunny day (CSS/SVG for now: gradient sky, breathing sun with halo, three drifting clouds, warm curved ground; 'Later we augment with image' per founder), copy 'You set it down.' + the dropped subject + 'The day is still yours. It will keep.' NO TRAY, NO LIST, NO TIMER. At the base: the ARMED LoopKeeper logo (assets/loopkeeper/icon-512.svg + wordmark, gentle pulse ring says it is alive) - the ONLY way back; the tap is THE USER'S ACT (sunny_return tracked; sunny_shown on arrival). The AGENT's own drops (keeper-agent) never trigger this - only a human's confirmDrop does. New component src/app/components/sunny-day/ (NgModule-declared in RolodexModule; home.page is standalone:false). i18n: 3 new flat keys (sunny.title/sub/return) across all 39 locales - 685 flat keys/file parity verified, 18 majors hand-translated (sw fr es de ar ru he am hi ja zh-Hans zh-Hant it nl pl tr ha), minors EN; am entries rebuilt with real Ethiopic punctuation. MEASURED: typecheck clean; prod clean; proofs in the commit. DEPLOY: app www 232-299. VERIFY: drop any loop (the X -> a reason) -> the full-page sunny day, NOT the next loop; nothing returns you but tapping the pulsing logo at the base; Previous: build: 298, // 2026-09-22 BUILD 298 COPY IS NOT THE CLOSE (the brief's move 2: 'Copy is not the close - receipt asks once "did it leave?"; if not, loop stays open'). A CLIPBOARD WRITE IS A COPY, NOT A SEND. fire('copy') STOPS the unconditional markSent - the loop is NOT closed at the copy: the words land on the clipboard, the toast says Copied, and the receipt ASKS ONCE "Did it leave?" (slide 5). 'Yes - it's gone' -> the REAL close (markSent, the chime, the gate passes via 297 firstMinuteEntry). 'Not yet' -> the loop STAYS OPEN - no close, no chime, no gate - the 9am digest brings it back (brief: 'returns at 9'). The waiting receipt wears the clock glyph, muted amber - the tab in the head stays. SAME RULE FOR TASK DELEGATE (the delegate door rides the copy channel, so it inherits the receipt automatically). Receipt state is per-loop (reset in select() and nextOne()). New analytics copy_receipt {left: 0|1} - THE FALSE CLOSE IS NOW MEASURED. i18n: 5 new walk.* keys (didLeave, left, notYet, notYetTitle, notYetSub) across all 39 locales, 682 flat keys/file parity verified, 18 majors hand-translated (sw fr es de ar ru he am hi ja zh-Hans zh-Hant it nl pl tr ha), minors EN. NOTE: the first sweep script wrongly nested a walk object (broke the flat-key structure) - caught by the parity check before build and CORRECTED; final files verified flat with walk.doneTitle intact. MEASURED: typecheck clean; prod clean; proofs in the commit. DEPLOY: app www 232-298. VERIFY: open any loop -> Copy -> slide 5 asks 'Did it leave?'; Yes -> the breath (check mark); Not yet -> the clock line, the loop still open in the garden + returns at 9; Task -> Delegate -> the same receipt. Previous: build: 297, // 2026-09-22 BUILD 297 THE RETURN TO COVER (founder: 'Whichever door they choose to use of the four available, there must be a return to cover in that next phase, which return resets the gate to untapped/untouched/undecided ie. journey away from procrastination state has not started'). NO DOOR TAP LATCHES THE GATE ANY MORE. THE COVER IS THE STANDING FACE OF THE FIRST-TIMER: a door tap opens its flow; the RETURN to slide 1 brings the cover back RESET (untapped/undecided - the *ngIf remounts it). The reply door's add sheet floats OVER the cover - an empty dismissal leaves it standing untouched; the legacy PERSON door clears the pending avoidance branding (a bounced reply tap can never brand a later pick). The task draft HIDES the cover while open (the *ngIf pair firstMinute build: 296, // 2026-09-22 BUILD 296 THE GATE, UNPOLLUTEDbuild: 296, // 2026-09-22 BUILD 296 THE GATE, UNPOLLUTED !taskDraftOn / !firstMinute || taskDraftOn) - CANCEL returns the cover reset; the SAVE (a real card) moves the 278 deed signal from the tap to the save where it belongs. A SECOND decide tap RESUMES the open cover-decide loop - the door never mints duplicates. THE GATE CLOSES ONLY WHEN THE JOURNEY STARTS: a real card's arrival (home's arrival path - which now also retires the cover this session) or a REAL SEND (walk.fire() after markSent emits firstMinuteEntry -> home persists lk_cover_engaged). Nothing else. WITH 296 the gate reads only lk_cover_engaged + realContacts === 0 - loop history can never close it. NO i18n changes. MEASURED: typecheck clean; prod clean; proofs in the commit. DEPLOY: app www 232-297. VERIFY: fresh device -> the cover; tap The decision I keep not making -> words; BACK -> the cover again, untouched; tap it again -> the SAME loop resumes (no duplicate); tap The reply I owe -> the sheet; dismiss EMPTY -> the cover stands untouched; pick -> the card lands, the cover retires for good (the journey started); Previous: BUILD 296 THE GATE, UNPOLLUTED (founder, after deploying 295: 'Deployed, but cover is still being skipped even though I did not respond to it at first deployment. I suspect you have mistaen the policy in the #2 statement'). THE FOUNDER WAS RIGHT — THE POLICY WAS MISREAD. THE LEAKS, FOUND: (1) the gate's second suppressor read loops.all() — the ENTIRE loop ledger (open AND closed AND parked), so ANY loop history on the device skipped the cover forever without the user acting AT it. (2) the gate read lk_firstminute_done, a flag with THREE writers. THE LAW (AGENTS.md THE FIRST GATE): the gate answers ONLY to the user's own action — the avoidance door tap (the entry fee, logged firstminute_avoid {kind}), the completed deed after it, or a real card's arrival (card_added). THE FIX: the gate reads the DEDICATED flag lk_cover_engaged — written ONLY by those three user-action paths — and realContacts() === 0; the loops.all() suppressor is RETIRED; lk_firstminute_done rides along for continuity but no longer rules the gate. NO i18n changes. MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: BUILD 295 THE GATE HELD (founder, after deploying 294: 'Deployed, but with error. State must remain same always, even if page reloads, until first-time user takes action on that... It must register on our logs that action was taken. That is the fundamental entry fee. They must do something at that first gate - the cover.'). THE ERROR, DIAGNOSED: the gate RESET across reloads — the reply door retired the panel PER-VISIT ONLY (281), so a bounce at the add sheet resurrected the cover on the next load, and the decide door suppressed it via its open-loop side effect rather than the user's action; both violated the state contract. THE FIX: the AVOIDANCE door tap IS the engagement — the entry fee, paid and logged (firstminute_avoid {kind} fires at the tap). A new firstMinuteEntry chain (walk avoidDoor -> firstMinuteEntry -> inbox relay -> home.onFirstMinuteEntry) PERSISTS the gate-passed state (lk_firstminute_done, the same flag the real-deed paths set), so no reload and no later visit can resurrect the cover. The 281 per-visit retirement stays for the demoted TASK/PERSON taps — they are below the fold, not the gate. Doctrine recorded in AGENTS.md (THE FIRST GATE — standing law: the gate is not a tour, it is a toll). NO i18n changes. MEASURED: production 294 verified live before this build (build.json 294, shell 200, health 200); typecheck clean; prod clean. Proofs follow in the commit.Previous: BUILD 294 THE AVOIDANCE COVER (founder ruling: 'Avoidance lead, as cover, but maintain styling theme'; the strategic brief D:/TODOs/USE_NOW.txt move 1: 'First session is one human avoidance, and nothing else'). The first-minute panel's COVER is the two named avoidances now — 'The reply I owe' (rides the PERSON chain: the add-sheet pick; birthFromWho births the loop OWED-REPLY, stance overdue-apology, the friction named BY THE USER — whySittingSource 'user', the string from the engine's own suggestWhySitting chain so the copy never forks) and 'The decision I keep not making' (rides the 183 PURE SELF-LOOP, straight to the words). TASK/PERSON follow BELOW the demo as the demoted row (the same .fm-door cards, a step quieter — .fm-doors-sub + the fm.orlabel line); the title rewords to 'Name the first loop you need to close'; Show me first keeps its place. THE GUARD: the pending door (avoidKind) is consumed by the FIRST birth and cleared on every deliberate default (mine / notThisOne / nextOne / cancelTaskDraft) so it can never brand a loop the user did not ask for. NEW: firstminute_avoid {kind} analytics; the walk's avoidDoor(); selfTap(kind, userNamed) sets the friction at birth. i18n: fm.avoidReply + fm.avoidDecide + fm.orlabel + the fm.title reword swept all 39 locales (18 hand-translated). Brief test: the first session is ONE real avoidance — a stranger who closes one real send has felt the product. MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: BUILD 293 THE ONE KEY, CLIENT (founder: 'The TESTER_ADMIN_KEY is completely different from Investors portal key which is local device based... Our code is just not right'; with SERVER 106 THE ADMIN SECRET, CHANNELLED). The console's write gate goes back to the ONE credential: the prompt reads 'Admin key — the server's TESTER_ADMIN_KEY' (the portal word is a device-local gate, never an admin credential — the 105 bridge is reverted server-side); the 401 message reads 'Admin key rejected — it must equal the server's TESTER_ADMIN_KEY'; the 404 note points to server build 106; the 400/500 branch keeps surfacing the server's own error text (config now answers 'TESTER_ADMIN_KEY is not set on the server — add it to .env' when the secret is missing). MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: BUILD 292 THE TEN + THE TWO KEYS, CLIENT (founder: 1. 'CommandCenter 08 - The Last Three doors: make it last seen = 10, new faces - first seen = 10' 2. 'Section 07 - Key rejected - try again'; with SERVER 105). (1) THE TEN: the CC section-08 title reads 'The last ten doors'; the server 105 serves ten last-seen + ten new-faces rows. (2) THE TWO KEYS: the write's gate prompt now names BOTH credentials ('the TESTER_ADMIN_KEY value or the Investors portal word'); a 401 carries the same guidance; a 400/500 surfaces the server's own error text (e.g. 'No gate key is configured on the server — set TESTER_ADMIN_KEY or INVESTOR_KEY in .env') so a missing config is never mistaken for a typo. The 404 note points to server build 105. MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: BUILD 291 THE HONEST 404 (founder: 'Says write did not land into .env'). PROBE-PROVEN ROOT: the live server answered Cannot POST /api/rolodex/ownfleet/noise — the running server has no such route, for TWO stacked reasons: (a) build 103 registered the endpoint at the /api/loopkeeper path, which the server's own alias middleware rewrites to /api/rolodex/* BEFORE routes match — unreachable from birth (server 104 re-registers at the internal canonical path); (b) the running server predates 103 anyway. THE APP FIX: the console's write feedback now tells the truth per status — 401 'Key rejected — try again'; 404 'The server does not know this command yet — deploy server build 104, then try again'; other failures name the HTTP code. MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: BUILD 290 THE NaN FIX + THE WRITE COMMAND (founder: 1. 'CommandCenter Section 08 The last three doors has a problem - last seen NaNd ago. Same for 07 Catch the devices' 2. 'When I mark my device Id in CommandCenter 07, why must I copy and paste it to .env when a command should do it. Write it into the .env.' 3. how the tester cohort works without manual intervention — answered in the reply: the accepted invite CODE rides every analytics event as a numeric testerId prop; the server catches those devices automatically (own-fleet) and rolls them into the per-day TesterDay ledger at ingest — no .env, no roster edit, ever). (1) THE NaN FIX: the server sends ISO DATE STRINGS; the CC's seenLabel did string-minus-number = NaN — 'NaNd ago' in sections 07 AND 08. The label now parses with new Date(ts).getTime() and returns an em-dash for anything unparseable. (2) THE WRITE COMMAND (with SERVER 103): the Noise list's copy-and-paste ritual is replaced by 'Write N device(s) into .env' — POST /ownfleet/noise (admin key asked once per session); the server updates its in-memory noise set (meters exclude IMMEDIATELY, no restart), saves the noise file, and WRITES the .env LK_NOISE_DEVICES line itself (merge, no duplicates). MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: BUILD 289 THE HEADER PULSE (founder: 'Can the recycle icon in Investors portal move up to the page header, before the X exit button'). The live-record refresh now sits in the portal's PAGE HEADER, immediately before the X — always one glance away; the in-page pulse beside the Investors title is retired (the founder said MOVE, not duplicate). Backed by SERVER 102 THE PARKED-TAB CAP: the average-session aggregate excludes durations over 7200s (two hours) — measured live: one 205,876s (57-hour) parked tab was dragging avgSession up on its own, exactly the founder's 'keeps incrementing while nothing else moves'; the sessions were REAL events, the average was the liar. Nothing deleted — the raw trails still show everything. MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: BUILD 288 THE PATH FIX (founder: 'Investors now has the recent section showing, but not so CommandCenter'). THE ROOT, FOUND IN THE BINDINGS: the summary endpoint wraps computeAnalyticsSummary() under the `analytics` key, but the Command Center's section 08 read recentUsers/recentTesters/recentNewUsers at the TOP level of the payload — a path that never existed. The lens was BORN BLIND: the last-three-doors never rendered in the CC on any device, in any build since 276, and 286's New faces inherited the same wrong path. The portal mirror (287) read the CORRECT path (stats.analytics.*) — which is why the portal showed it and the console did not. THE FIX: all six section-08 bindings now read stats.analytics.* — the gate, the last-three loop, the New faces gate + loop, the Testers gate + loop. THE AUDIT: every other CC binding verified correctly pathed (analytics.reliability/landingSources/channelExits/dailyEvents/knownDevices nested; timeline/rooms/translations top-level). MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: BUILD 287 THE PORTAL MIRROR (founder, twice: 'the last 3 or 5 users for either claimed category still does not appear anywhere. Am I not with latest commit?'). THE MEASURED VERDICT: production IS latest — build.json served build 286 (deployed 23:07Z), the server returns recentNewUsers (5 rows), recentUsers (3 rows: 06c9pg/pb9gu9/kfgw6s) and dauSplit {new:3, returning:1}, and the endpoint answers in 3-4s (inside the 12s fetch guard). THE REAL GAP THE FOUNDER'S REPORT EXPOSED: the recent-doors record lived ONLY inside the Command Center (section 08, behind the aperture icon) — the portal BODY never carried it, so a reader of the portal page alone met an empty hunt. THE FIX: the Investors portal now carries its OWN 'The recent doors' section (07, between Top events and Command Center) — Last seen (the three most recent organic devices) and New faces — first seen (the five most recent first-seen devices), each row tap-to-reveal its 40-event trail, green NEW tag on arrivals; Command Center renumbers to 08 and Policy to 09. Helpers (toggleRecent/shortId/seenLabel/formatTime) ported from the CC. MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: BUILD 286 THE CLEAN PICK LIST + THE REFERENCE RECORD (founder: 1. 'Under Add a person Demo contact and task cards are still showing with the DEMO watermark on them as elsewhere' 2. 'I still do not see a reference record for most recent 5 new users, and other stats requested alongside, done several commits back, but I see a suggestion of 4 DAU since my last check in Investors. Either they are repeat or they are new, is unclear.'; with SERVER BUILD 101). (1) THE CLEAN PICK LIST: the SearchModal results getter now filters isMockData rows whenever addDoors or pickMode is on — an ADD or PICK flow lists REAL cards only; demo cards are for looking, never for adding. (2) THE REFERENCE RECORD (server 101): the analytics summary gains recentNewUsers — the FIVE most recent organic devices SEEN FOR THE FIRST TIME (per-device min-ts), each with its first-seen date, event count and tap-to-reveal 40-event trail; and dauSplit — today's organic DAU split into new (first event within 24h) vs returning. THE RENDER: Command Center section 08 carries a 'New faces — first seen' block with the green NEW tag (tap-to-reveal trails, same pattern as the last-three-doors); the Investors portal's DAU card shows the split in place ('N new · M returning'). The repeat question is answered where the number lives. MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: BUILD 285 THE BRAND RULING (founder: tapping the Profile telephone icon said 'Your phone number - how contacts reach you, and how the chat knows you are on Rolodex' — 'Clearly should not be Rolodex, but rather, LoopKeeper'). TWO USER-FACING SLIPS FOUND AND FIXED: (1) the Profile phone prompt — now 'how the chat knows you are on LoopKeeper'; (2) the AutoSort view header 'Review your Rolodex' — now 'Review your LoopKeeper'. THE SWEEP: every user-facing string checked — prompts, alerts, toasts, templates, and all 39 locale files; the only remaining 'Rolodex' strings are the Investors portal's HISTORY section, which deliberately describes the ORIGINAL 1958 Rolodex device (the brand's own story — correct copy, not a slip). Class/service/file names keep the historical rolodex prefix per AGENTS.md (internal only). MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: BUILD 284 THE BENEFIT SHOW (founder: 'Show me first has one big gap - it does not show what the LoopKeeper algo and AI Assistant can do for the user. It instead says, You type it, and, the words are written for you. Hilarious. Nothing gained. That is no demo of benefit from using the app.'). THE FOUR BEATS REWRITTEN AS THE VALUE STORY: 1. 'You type it once' — the capture box, the one line ('Reply to Amina about Saturday' ticking in); 2. 'LoopKeeper brings it back at the right moment' — a NEW surface: the algo's NUDGE (amber bell, 'Tue 9:00 — Amina's reply is due' ticking in — the remember-back IS the product); 3. 'The Assistant drafts the words — you just approve' — the card draft now wears an 'AI' SPARKLE CHIP (the draft's author is named); 4. 'One tap sends it. The loop closes. Mind free.' — WhatsApp + 'Sent · Closed — mind free' in one row. THE STAGES NOW PERSIST their own finished words (demoDone[] — the old single demoTyped leaked the active beat's words into finished stages; each surface now keeps what it produced, like the real flow leaves its artefacts behind). CAPTIONS SWEPT: all 39 locales — 21 hand-translated (the 15 majors + the four named tongues: Hausa 'LoopKeeper ya dawo da shi lokacin da ya dace', Amharic, Somali, Hebrew 'הלולאה נסגרת. הראש פנוי'), minors EN. MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: BUILD 283 THE SHARPER YELLOW + THE ARMED SEPARATOR + THE NAMED TONGUES (founder: 1. 'The icons in first-timer view TASK || PERSON mini-cards are a dull yellow, barely legible. Can the yellow be a bit sharper, more legible.' 2. 'Demo Tasks & Contacts in the Contacts component, appears at bottom of home page default view. Can you similarly arm it like the loopKeeper icon to open into and close a larger view. Easy to see some users tapping it intuitively, expecting a reaction.' 3. 'No translations yet in Hausa, Amharic, Somali, Hebrew, for the first-timer Loops Alpha interface such as TASK, PERSON (for most, if not all languages), Welcome to LoopKeeper..., and Name the first task or person loop you need...'). (1) THE SHARPER YELLOW: the fm-cover icon was the theme accent #FFD93D (pale on the white card) — it now carries a DEEP AMBER icon (#B45309) on a sharper dashed ring (#EAB308), weight 700. (2) THE ARMED SEPARATOR: the DEMO TASKS & CONTACTS label (contact-card) is a DOOR — chevron + tap target; tap opens the demo deck LARGER (56px avatars, 18px bold names), tap again closes; the founder's intuitive-tap expectation now has a reaction. (3) THE NAMED TONGUES: TASK/PERSON are TRANSLATED KEYS now (fm.taskLabel/fm.personLabel) — swept all 39 locales (majors hand-translated: KAZI/MTU, TÂCHE/PERSONNE, TAREA/PERSONA, AUFGABE/PERSON, مهمة/شخص, काम/व्यक्ति, 任务/人物, 任務/人物, タスク/人物, ЗАДАЧА/ЧЕЛОВЕК, ATTIVITÀ/PERSONA, TAAK/PERSOON, ZADANIE/OSOBA, GÖREV/KİŞİ...); and the FOUR NAMED LANGUAGES carry the FULL first-minute interface hand-translated — Hausa (AIKI/MUTUM, 'Sunanta farkon aiki ko zangamin mutum da kake bukatar rufewa', the courtesy line, Show me, the four beats), Amharic (ተግባር/ሰው, full set), Somali (HAWSHA/QOFKA, full set), Hebrew (משימה/אדם, full set, RTL). MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: BUILD 282 THE EQUAL CLASSES + THE SPLASH COPY + THE HONEST CHIPS (founder: 1. 'In the Demo Tasks & Contacts, before arrival of real contacts and tasks, the view order, unless in alphabetical mode, appears to prioritise Contacts first, and at end, Tasks start. But this is not proper. Neither type is inferior to the other, so unless alphabetical, sorting order should treat both classes as equal.' 2. 'The splashscreen has a fullstop after ... keep meaning to close. Remove that fullstop, as the text is copy, and not grammatical sentence.' 3. 'Check logs to see if any progress at all. Some logs still do not show percentage changes, in both CommandCenter and Investors'). (1) THE EQUAL CLASSES: the deck's default order is the card's own MOMENT (lastInteraction, falling back to updatedAt/createdAt) — persons and tasks interleaved by activity, never grouped by kind; the demo TASK cards carry fixed 2024-12 dates (they had boot-time stamps — they either sank to the end or floated to the top); the alphabetical mode keeps its own grouping. (2) THE SPLASH: 'Close the loop you keep meaning to close' — the fullstop is gone; it is copy, not a sentence. (3) THE HONEST CHIPS: a delta chip needs a REAL baseline — an undefined/null prev field renders NO chip, never a fake +100% (the server 100 backfill reconstructs past days from the retained events; totals stay absent there, so totals chips wait for real ledger days). LIVE LOG VERDICT (fetched from production): server 99 healthy, prev NULL only because the ledger was born that day; REAL PROGRESS in the trails — device rolodex-8e... ran first_loop_started + loop_captured + draft_generated + task_card_started + firstminute_showme x3 (a real deed); device rolodex-kf... device_list_started + firstminute_showme; device rolodex-pb... returned to the panel x3 (the every-visit rule working). Lifetime funnel: firstloopstarted 5, loopcaptured 20, messagesent 5, loopclosed 6, invitecreated 5; sessions 9 in 24h, avg 619s; dau 3, wau 222, mau 297. MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: BUILD 281 EVERY VISIT UNTIL ENGAGED + THE RANDOM MIX + THE DINNER-TABLE DOOR + THE TIME CAPSULE CLIENT (founder: 1. 'Until user has actively engaged with the app, loggable and evident in Investor portal and CommandCenter logs, let us continue showing them just that first timer UX on every visit. This is crucial.' 2. 'The demo cards should be a random sequenced mix and not determined by type as currently is. And most importantly tappable to any investor at a moment's notice, even across a dinner table for the first time LoopKeeper is introduced to them.' 3. the 90-day backend ledger = server build 99). (1) EVERY VISIT UNTIL ENGAGED: the lk_firstminute_seen gate RETIRES — a door tap was never engagement; the panel arms EVERY visit until lk_firstminute_done, which only a real card's ARRIVAL sets (logged card_added, evident in the portal). onFirstMinuteDeed retires the panel for the visit only; onExitDemo (Close demo) retires the demo deck but is NOT engagement; the deed flip in onContactsDirty stays. (2) THE RANDOM MIX: shuffledMockContacts() (Fisher-Yates) — both deck assembly points (deckWithDemo + the load path) present the demo deck in a fresh, type-agnostic order every session. (3) THE DINNER-TABLE DOOR: a demo card DECK tap no longer opens the card surface — it raises the WALK with the card armed (pendingEscalation -> armEscalation -> the forced walk surface); the investor taps the armed card -> the 279 demo door -> Continue demo -> the words -> the send reminder with the three doors. The full show, at a moment's notice. (4) THE TIME CAPSULE CLIENT: loadInvestorStats reads the SERVER's prev (build 99's daily ledger) FIRST — any device, first visit included, lands with the same what-changed baseline; the device-local snapshot is the fallback only. MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: BUILD 280 THE DEMO DEFAULT + THE ROTATION RULE + THE STUCK-ERASE FIX + THE COMPENSATORY DETOUR (founder: 1. 'once real cards, or tasks, come on into LoopKeeper Contacts, demo contacts default to false. persisted, they stop showing, unless Settings => Demo calls for them to return showing' 2. 'a tap of Not this one when real contacts exist cannot be rotating demo contacts. That logic is somewhere, or was regressed' 3. 'First time exiting/erasing out of LoopKeeper, under penultimate commit, was smooth, but this time, we are stuck in a 7 minutes wait' 4. 'PERSON still shows me Create Task, so let us have under TASKS, if I chose that pathway, a compensatory detour to Open Person'). (1) THE DEMO DEFAULT: onContactsChange tracks the real-card count — an ARRIVAL above baseline flips the demo off, persisted (rolodex_demo_enabled=false + mockEnabled=false); Settings can always call it back. (2) THE ROTATION RULE: rebuildWho's deck loop now carries the same gate the pills loop always had — with real cards on the deck, demo cards NEVER enter the Not-this-one rotation (the hole was the deck loop; the pills gate had regressed in isolation). (3) THE STUCK-ERASE FIX: the wipe CLOSES our own IndexedDB connection first (storageService.close()) — an open handle blocks deleteDatabase; every deletion is timeout-guarded (2.5s); the wipe sets lk_wipe_pending BEFORE the reload and the fresh boot runs a verification pass (finishPendingWipe: clear again + reload, attempt cap 2 — never a loop), so a blocked deletion can never hold the veil again. (4) PERSON goes through the ADD SHEET (whoRequest -> channelWhoFromWalk: Contact Picker on Android, the .vcf ladder on iPhone) instead of the bare create form a picker-less device fell into; and under TASKS the draft card gains the compensatory ghost 'Bring in someone real' (walk.whoAdd) -> mine() — the task path never walls the person path off. MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: BUILD 279 THE SHOW-ME FULL SCREEN + THE RETENTION BREAKER, RETIRED (founder: 1. fullstop off the title 2. 'on user tapping Show me first (the color is difficult to read, darken that message a little bit) remove/condition out the lower part of the home page that shows the footnote sections - the LoopKeeper icon panel and the rolodex/contacts component - so we have the full screen below for the Inbox to fully show. And we must add some scroll down else user misses the animation below' 3. a RETURN button in the demo ('user never suffers anxiety of changing their mind or forgot something behind') 4. beat4: 'The tab in your head' -> 'The delay worry in your head disappears' 5. 'You mistakenly reinjected the Welcome modal/slides... that becomes redundant' - the six-slide Welcome NEVER auto-opens again (Settings replay door stays, explicit choice only) 6. THE RETENTION BREAKER: 'When we tap demo card the start, the message that comes is Install manually... that is totally wrong. It should instead point out that 1. use real contact cards, which if tapped takes us into their device contact list (observing permissions), or Continue demo, leading normal current flow until exact point where options for fulfilment mediums are presented. Any tap reminds again that it is demo - close demo returns to home screen (regular now) or Contact (once more leading to their contacts) or Create Task (normal task builder)'). THE CHANGES: (1) fm.title loses the fullstop, fm.beat4 carries the delay worry - swept all 39 locales, 15 majors hand-translated. (2) SHOW ME FULL SCREEN: demoView relays panel -> walk -> inbox -> home; the deck + icon panel hide (app-rolodex tab-hidden), the demo strip scrolls into view (scrollIntoView), and the Show me door is DARKENED (#0f766e, weight 700). (3) THE RETURN: one tap under the animation - back to the panel, no anxiety. (4) THE WELCOME LECTURE: presentWelcome's auto-offer (open #2, lk_open_count + lk_welcome_offered) is RETIRED - the first-minute panel IS the welcome; Settings replay stays explicit-only. (5) THE DEMO DOOR: tapping a DEMO card in the walk no longer births the loop silently - it asks first: 'Use real contact card' (mine() -> the add sheet / device contacts) or 'Continue demo' (the normal flow, loop born with the demo mark). (6) THE SEND-TIME REMINDER: a demo-marked loop NEVER sends - the fulfilment-mediums tap shows the reminder with the three doors: Close demo (home flips regular - demo deck retired), Contact (the add sheet again), Create Task (the normal task builder). (7) THE RETENTION BREAKER ITSELF: promptInstallNow('first loop captured') is REMOVED from loops.service - the deed's moment belongs to the words and the flow, never an install lecture. MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: BUILD 278 THE FIRST MINUTE, IN THE FLOW (founder: 'The first time UX must be same as that for return UX. Mostly same, everything surrounding, except for content of Loops Alpha... the whole ambience is exactly same as they meet if they were a second-time visitor, just then the difference in communication, and our regular layout of previous commit' + the four sections + 'Either tap has plugged into the existing normal flow. Do not invent new complexities or features'). THE VEIL IS GONE: the first-minute panel now lives INSIDE the walk's slide 1 (send-walk [firstMinute], relayed via the inbox) — the regular layout byte-for-byte, ONLY the content differs. Section 1: 'Name the first task or person loop you need to close' above; Section 2: two of our REGULAR cards, reduced size, side by side — TASK (checkbox cover) and PERSON (person cover), the walk's own .sw-card language; Section 3: the courtesy line at the bottom; Section 4: 'Show me first'. THE DOORS PLUG INTO THE EXISTING FLOWS, called as the walk's own methods: TASK -> beginTaskDraft() (the in-place 257 draft — 'as normal after user tapped Create Task'); PERSON -> mine() (the same door the empty state uses — the add sheet / their device Contacts; on choosing a contact, the LoopKeeper card is created and the regular Inbox view takes over). NOTHING new is invented. THE TAP IS THE DEED SIGNAL (firstMinuteDeed -> home): the panel retires at the tap and the regular flow takes over AT THE TAP; the deed's own contactsDirty completes the flip (lk_firstminute_done + the demo-deck retirement). SHOW ME FIRST is now the GRAPHIC (founder: 'we graphically demonstrate the 4 steps... Same graphic of our normal flow, except that we have the demo words ticker sounding flow into the editing spaces (see zyppar RolodexPage)'): four mini surfaces of the flow's own visual language (capture box -> card draft with Amina -> send via WhatsApp -> closed check), the demo words TICKING character by character into each editing space, the active stage lit, looping until dismissed. Return UX untouched: *ngIf=!firstMinute wraps the existing who-pane machinery unchanged. MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: BUILD 277 THE EXIT SECTION + THE HORIZON RULE + THE LEAN VEIL (founder: 1. 'we do no assurances of safety to users. It is lame, and unnecessary... The true test is that no one, not even AI catches you secretly collecting data on a user which they did not volunteer to LoopKeeper' 2. courtesy line replaced: 'Welcome to LoopKeeper - clearing procrastination, one person, one task at a time' - 'remove the safety assurance - it was not included in my request' 3. 'Make the last 3 separate from testers, which we can also see' 4. 'Talk to LoopKeeper is untested, why not park it first, and stick with exact same interface, but text only... Stay lean. Examine the flow for efficiency first' 5. 'DELETE is so crucial it should have a separate section - Exiting LoopKeeper w/icon' + 'no visual for data is clearing or failing... Success should have the usual regrets and come back again soon' 6. the max-3 horizon rule: 'we do not then turn around and dump the mind with the information that 48 other notifications are waiting... These three are the horizon of most critical, easiest to attempt, Nirvana inviting acts. Nothing else exists. Clear them and peace'). (1) NO ASSURANCES: the courtesy line's 'Yours is safe here' clause and the Command Center's 'nothing here knows a name' note are REMOVED; the policy is recorded in BOTH AGENTS.md files (app + server) as standing law - conduct is the test, never copy. The courtesy line now reads the founder's own sentence: 'Welcome to LoopKeeper - clearing procrastination, one person, one task at a time.' (with the sprout) - swept all 39 locales, 15 majors hand-translated. (2) THE CHAT DOOR PARKED: 'Talk to LoopKeeper instead' is REMOVED from the first-minute veil - the veil is the exact same interface, text only (title, capture box, Show me first, Skip) - the 276 implementation lives in git history and returns when the flow's obstacles are measured. (3) THE LAST THREE, SPLIT (with server 98): section 08 reads TWO lists - the three most recent ORGANIC devices (real users - the answer to 'it must be from testers not properly captioned': testers never rode the organic meters), then the testers, also tappable; the assurance sentence is gone from the note. (4) THE EXIT SECTION: the clean slate leaves the backup cluster for its OWN 'Exiting LoopKeeper' section - divider with the log-out icon, chip in the Settings map (loopkeeper.settings.exit, swept 39 locales), and the wipe carries ITS STATES: an erasing veil (spinner + 'Erasing your LoopKeeper...'), the farewell beat ('LoopKeeper is cleared. We're sorry to see you go - come back soon.') before the reload, and an honest failure line with Try again/Cancel. (5) THE HORIZON RULE: the check-ins panel's 'N waiting quietly' overflow count is GONE - no number, no pile; the three seats ARE the horizon, nothing else exists. MEASURED: server node --check OK (build 98, commit paired); typecheck clean; prod clean. Proofs follow in the commit.Previous: BUILD 276 THE CLEAN SLATE + THE LAST THREE + THE CHAT DOOR (founder: 1. 'a clear my LoopKeeper data which resets the device. Can the reset make even backend treat device as new? Total wipe of all persistence in frontend. Page reload. In which case, we then see the first-timer UX' 2. 'Always include the LoopKeeper Chat option' 3. 'a courtesy one-line welcome... Not 80s Budapest cold, neither San Fran at its craziest' 4. 'Name the first task or person loop you need to close' 5. 'a specific section that captures our most recent 3 users, with tap and reveal granularity on what they touched, and for how long'). (1) THE CLEAN SLATE: Settings (beside This device - anonymous id) gains 'Clear my LoopKeeper data' - a confirm dialog naming the blast radius, then a TOTAL frontend wipe: the service worker + every cache, localStorage + sessionStorage, and EVERY IndexedDB database (the 'rolodex' DB carries all app state INCLUDING the anonymous id rolodex_device_id), then a hard reload. THE BACKEND TREATS THE DEVICE AS NEW AUTOMATICALLY - the id is minted at boot from the now-empty storage, so the next launch is a genuinely new device to the analytics ledger, the sync slots and the retention cohorts (the server's own words: a wiped device is a new cohort member). The reload deliberately carries NO machine-reload mark - the fresh boot is a real birth the meters should count; the first-minute veil AND the fresh 7-day trial return with it. (2) THE CHAT DOOR: the first-minute veil gains 'Talk to LoopKeeper instead' - the situation-mode Assistant opens OVER the veil (talking IS capturing); tracked as firstminute_chat; the veil stays beneath until they capture or skip, so returning from the chat lands back on it. (3) THE COURTESY LINE: one warm line at the bottom of the first screen - 'Karibu - first loops are the shy ones. Yours is safe here.' with one sprout - brand voice, no carousel, no banner. (4) THE TITLE: fm.title now reads 'Name the first task or person loop you need to close.' - swept all 39 locales (15 majors hand-translated; fm.chat + fm.courtesy ride the same sweep). (5) THE LAST THREE - with the server (build 97): the Command Center gains section 08 'The last three doors' - the three most recently active devices, ANY fleet (own-fleet and tester rows TAGGED - the lens where the founder's own testing finally shows, answering 'why do my meters not move': the organic meters exclude the own fleet BY DESIGN), tap a row to reveal its last 40 events with timestamps, pages, and session durations. Anonymous ids only; nothing knows a name. MEASURED: server node --check OK; typecheck clean; prod clean. Proofs follow in the commit.Previous: BUILD 275 THE FIRST MINUTE + THE CORRIDOR LIGHT (founder: 'revisit our Welcome with a view to absolutely showing exactly what a first-time user does' + the two design rulings: 'How will you know it is a first-timer, and what is transition to regular view so we do not regress current setup?' + 'Implement', both-in-one-build). THE DETECTION, STORAGE IS THE TRUTH (266/267): the new first-minute surface arms ONLY when the device holds ZERO real cards (readPersistedContacts, mock filtered), ZERO open loops, and has never seen/skipped/done the minute (lk_firstminute_seen / lk_firstminute_done in IndexedDB) - one real deed or one prior visit disarms it forever, a loaded device (the founder's 84 cards) can never see it. THE SURFACE: a fixed full-viewport veil over the MOUNTED home - the founder's own North Star line ('Name the thing you keep not doing.'), one capture box, Capture it; two quiet doors: 'Show me first' (ONE screen, four beats: you type it / the words are written for you / you send from the app you already use / the loop closes - the exact first-minute path, replacing the deferred six-slide lecture for first-timers) and 'Skip for now'. THE TRANSITION, NO REGRESSION: the sentence rides the SAME inbox chain (home calls inboxRef.addCapture - keeper, celebrate, deck expansion, persistence, loop_captured all unchanged); at the deed lk_firstminute_done flips AND the per-device demo deck retires (rolodex_demo_enabled=false - their content leads the regular view, real cards first, the John Doe contact-era deck finally out of their face); at the skip, home is EXACTLY today's (walk with demo deck, six-slide Welcome still offered once on open #2, Settings replay intact); current users never see the veil at all. i18n: loopkeeper.fm.* (10 keys) swept all 39 locales, 15 majors hand-translated (sw/fr/es/pt-br/de/ar/hi/zh-cmn-Hans/zh-cmn-Hant/ja/ru/it/nl/pl/tr), minors EN. THE CORRIDOR LIGHT (founder: 'are features failing, and why are we not seeing it?' - measured: reliability all-zero is ZERO TRAFFIC, the capture-to-send corridor had NO instruments: 94 loop_captured rode into 2 message_sent with nothing between): draft_generated fires at the engine's FIRST draft write (loops.service add, never re-drafts); draft_edited fires at the user's own Save (send-walk saveEdit, the 253 ownWords moment); send_opened fires at both send entries (inbox send + walk fire, with channel+surface); send_cancelled fires at every back-out (LinkedIn cancel, Telegram cancel, the stranger's handle-ask retreat - the words were ready, the door was open, the hand stopped). The four surface events ride along: firstminute_shown/captured/showme/skipped. MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: BUILD 274 THE FULL RULER (founder: the Command Center now scrolls 'but loses ~30% bottom of viewport - should be leaving only about 5%'). MEASURED ARITHMETIC: the lost bottom (~30vh) equals the shell's old floor (min-height: 70vh) EXACTLY - the 100% height chain through the inline modal's wrapper does not resolve because the wrapper lives behind Ionic 8's SHADOW boundary, where build 273's light-DOM .ion-page pin cannot reach; the shell therefore sat AT ITS 70vh FLOOR and ion-content scrolled inside ~70vh, abandoning the bottom ~30%. THE FIX, FROM BOTH ENDS: (1) the floor rises to 96dvh (96vh fallback) - the console commands ~95%+ of the viewport in EVERY host (portal modal and aperture view), chain resolved or not; (2) global.scss pins ion-modal.command-center-modal::part(content) to height:100% - the modal's REAL content wrapper, reachable from global styles through the shadow part - so the chain itself resolves too. MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: BUILD 273 THE LIVE COMPASS + THE CONSOLE UNSTUCK (founder: 1. 'In Investors portal can we see percentage changes between visits or refresh Sections 03, 05, and 06, like we do in 01?' 2. 'The CommandCenter encapsulated within the portal still does not act normal, does not scroll, or show all its content, and is static. Might the Refresh Now button be an icon in the header, opposite end of Investors title, more accessible.'). THREE FIXES, ONE BUILD: (1) THE LIVE COMPASS - computeStatsDelta now also returns a KEYED delta map (dau, wau, mau, ownFleet, sessions7d, avgSessionSeconds, inviteIssues 24h/7d/30d, shares30d, invites30d, activation.* for all nine milestones, event.<name> for every topEvent) and sections 03, 05, 06 render the change chip beside every number - up green, down red, unchanged grey - the section-01 percent story told where the number lives; ONE baseline (the snapshot saved when the portal last closed), recomputed on load AND on every refresh. (2) THE CONSOLE UNSTUCK - the Command Center body now rides ion-content (Ionic's own scroller) instead of a hand-rolled overflow div whose height:100% chain through the modal's .ion-page collapsed on-device; global.scss pins ion-modal.command-center-modal .ion-page to 100% - the console scrolls and shows ALL its content on both hosts (the portal modal and the aperture view). (3) THE ACCESSIBLE PULSE - the portal's bottom-of-page 'Refresh now' button is retired; a refresh icon sits in the Investors title row (word left, pulse right, spinning while loading), and the Command Center's own header gains the same icon opposite the Investors·Operations title, forcing a fresh /investor/summary - the console is never static again. MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: BUILD 272 THE CLAMPED CORNER (founder: push 200, pull 200 - the December card is backed up - but the notification STILL renders one letter per line at the border). THE ROOT, FOUND IN THE DOCK: the dock is DRAGGABLE with a PERSISTED position (b198) - a corner saved on a larger screen (or dragged) can sit AT/BEYOND the current viewport edge; the dock then renders in the visible SLIVER and its text wraps one letter per line ('and beyond' = the dock body is literally off-screen). FIX: every restore CLAMPS the saved position back inside the visible window (margin 8px, dock ~340px wide); belt-and-braces on the item text (.notify-msg gains min-width:0 + overflow-wrap:anywhere - the classic flexbox squeeze). The real ion-toast (270), the storage-first push (267), the compressed push (271), the dialogs (269): all stand. MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: build 271 THE COMPRESSED PUSH + THE NGINX GUIDE (founder: 'Guide me to do the nginx fix 2026-09-18 BUILD 271 THE COMPRESSED PUSH (founder: 'Guide me to do the nginx fix instead of the band-aid remedy of this commit which I will not push. Is there any way to also compress the push payload before it arrives at backend?'). YES - gzip via the browser's CompressionStream, sent as Content-Encoding: gzip; the server's body-parser inflates natively (inflate defaults on, zero server code needed for THAT). THE ORDER: small decks ride plain; big decks gzip WITH covers first (the founder's 84-card deck rides WHOLE once the nginx cap is raised - the shed-covers band-aid becomes the LAST resort, only when gzip is unavailable); keepalive is GONE - Chromium caps keepalive bodies at 64 KB, another silent killer for a real deck (a user-initiated push needs no keepalive). SERVER 96: express.json limit 5mb -> 32mb to match the raised nginx cap (gzipped bodies inflate here). THE NGINX GUIDE the founder asked for: (1) find the config: sudo grep -rln 'loopkeeper' /etc/nginx/sites-enabled/ /etc/nginx/conf.d/ 2>/dev/null; (2) the universal option - create /etc/nginx/conf.d/90-body-cap.conf containing one line: client_max_body_size 32M; (http-level, applies to every server block); (3) sudo nginx -t && sudo systemctl reload nginx; (4) VERIFY with the 2 MB probe ( rolodex-server NGINX-BODY-CAP.txt ) - expect 200, not 413. MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: build 270 THE SIZE-AWARE PUSH + THE REAL TOAST (founder: 'the app accurately counts 2026-09-18 BUILD 270 THE SIZE-AWARE PUSH + THE REAL TOAST (founder: 'the app accurately counts the cards in its success notification. Notification is still single letter vertical on border of template, and still same push failure'). TWO ROOTS, BOTH MEASURED: (1) THE PUSH FAILURES - the server's nginx cap rejects bodies over ~1 MB with HTTP 413 (measured live: 512 KB -> 200; 1.5 MB / 4 MB / 9 MB -> 413) - an 84-card deck with base64 photo/video covers is far over it, so EVERY push from a real deck died at the door while small test payloads sailed through; (2) THE LETTER-STACK - showToast has fed the IN-APP NOTIFICATION DOCK since Aug 18, whose narrow corner items wrap long text one letter per line - EVERY 'toast' in the app (including the create-card count) was a dock item. THE FIXES: (1) THE SIZE-AWARE PUSH - push measures its own payload; over ~900 KB it sheds the device-local media (base64 photos, video covers) from the payload and rides the text, tasks, dates and loops whole; the dialog names HTTP 413 explicitly with the nginx remedy (client_max_body_size 32M on the droplet - NGINX-BODY-CAP.txt in the server repo); the success toast appends 'covers stayed on this device' when shed. (2) THE REAL TOAST RETURNS - AlertsService.showToast is a true ion-toast again (ToastController, width-capped 92vw/460px via the lk-toast class in global.scss) with a dock fallback; the dock keeps the STICKY actionable notifications it was built for. 269's dialogs and unique cache-buster; 267's storage-first; 265's transfer-pull/reset: all stand. MEASURED: the 413 reproduced live before the fix; typecheck clean; prod clean. Proofs follow in the commit.Previous: build 269 THE INSTANT DETECTION + THE READABLE DIAGNOSTIC (founder: 'Even after I deployed 2026-09-18 BUILD 269 THE INSTANT DETECTION + THE READABLE DIAGNOSTIC (founder: 'Even after I deployed, there was no instant detection at frontend. It still did not detect when I tapped Update check in Settings. The auto detection from app.component.ts did not detect either, and after it finally came round and I tapped update, it still says there is nothing to push' + 'the notification in case of failure appears... but is wrongly formatted as it is running as a single thread, each letter vertically stacked at the page border, and beyond'). TWO ROOT CAUSES, MEASURED: (1) THE DETECTION DELAY - fetchDeployedBuild's cache-buster was ?b=${appBuild}: CONSTANT for the session, so every check hit the SAME URL; the page's cache:'no-store' does not survive the service worker's pass-through re-fetch, and nginx serves build.json with NO Cache-Control (heuristic caching) - every check (banner, resume-refresh, AND the Settings manual check) read the STALE build.json until the cache aged out ('it finally came round'). FIX: the buster is UNIQUE per fetch (?b=${appBuild}&t=${Date.now()}) + Pragma no-cache - no cache anywhere can answer; detection is now instant on deploy, on every surface. (2) THE LETTER-STACK NOTIFICATION - the long push diagnostics rode ion-toasts, which on the founder's device rendered one letter per line at the page border. FIX: every long diagnostic in the Server pane (empty-push with both meters, push failures) is now an ALERT DIALOG (alertDialog helper) - fixed dialog, wrapped, every word readable; toasts keep only the short success line. The push itself (267's storage-first read) stands. MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: build 268 THE SAME SOURCE, BOTH METERS (founder: 'the same interface 2026-09-18 BUILD 268 THE SAME SOURCE, BOTH METERS (founder: 'the same interface from which I was to push, where in Device tab you find Export File and Import File, declares upon opening 84 real cards on this device... the push is presumptuous in sourcing the data I should push... in the LoopKeeper tab, next to Device, it is reading a different source... Or perhaps I was to first use Export Files under Device tab to prepare the data for push... the proper app response is to tell user what to first do'). THE ANSWERS, MEASURED AND NOW IN THE UI: (1) THE TWO TABS NOW READ ONE SOURCE — the Device strip's '84 real cards on this device' (realContactCount) and the push (storage-first since 267) both derive from the SAME persisted deck; the old disagreement was the push reading a cache that could be empty while the strip counted storage-backed cards — impossible on 267+. (2) EXPORT FILE IS NOT A PREREQUISITE — push reads the deck directly; Export File (Device tab) is a separate .rolodex file backup. The Server pane's hint now says so verbatim, and the empty-push toast repeats it. (3) THE BOTH-METERS TOAST — if push ever reads 0 real cards while the Device tab counts N, the toast shows BOTH numbers ('the stored deck this push read holds 0 real cards, while this panel's Device tab counts N... if they still disagree after reopening the app, that is an app bug — tell the founder. Export File is NOT needed for push'). No silent presumptions: the user sees the exact disagreement the founder hit. MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: build 267 THE PERSISTED DECK IS THE BACKUP SOURCE (founder: 'certainly 2026-09-18 BUILD 267 THE PERSISTED DECK IS THE BACKUP SOURCE (founder: 'certainly it is not in-memory that should be backing up to cloud or server - that was, in itself, bad code' + 'the app itself confirms over 80 cards at various checkpoints, and increments when I create a task card'). THE ARCHITECTURE CORRECTED: serverPush now reads the deck from STORAGE FIRST (readPersistedContacts -> filter mock) — the SAME persisted deck every checkpoint counts (the Device strip's 'N real cards on this device' reads the same storage chain) — and only falls back to memory if the storage read is empty; the in-memory array is demoted to a CACHE, never the backup's source of truth. The live deck is re-synced from what storage proved it holds before the POST. The empty-push message now names the real suspect when the counts disagree: if the stored deck here has only demo cards while the user sees real cards, they are looking at a DIFFERENT app window/profile (each window keeps its own storage) — 'open LoopKeeper there and push from it'. The success toast says '(from the stored deck)' and carries the server slot id. 266's storage-fallback and self-evidencing toast; 265's transfer-pull and reset-to-pristine: all stand. MEASURED: typecheck clean; prod clean. Proofs follow in the commit.Previous: build 266 THE STORAGE IS THE TRUTH (founder: 'You realize I said nothing about an empty contact list 2026-09-18 BUILD 266 THE STORAGE IS THE TRUTH (founder: 'You realize I said nothing about an empty contact list or other device... the processes were all false and then applied fixes, which my test exposed as incomplete'). THE FOUNDER WAS RIGHT AND THE MEASUREMENT NOW AGREES: (1) the server ledger shows NO sync from ANY device today (latest row anywhere: Sep 15, contacts 0) - the push they tapped never produced a POST; (2) the harness reproduced their exact symptoms on the current code when home's in-memory deck is empty while storage holds real cards: 'Nothing to push' + 'nothing for this device - push first' - the in-memory deck is NOT the only truth. THE FIXES: (1) serverPush reads the PERSISTED deck when memory is empty (readPersistedContacts -> filter mock -> hydrate this.contacts -> push it) - the hydration race class dies; (2) the empty-push message now states the DECK'S ACTUAL TRUTH (all-N-demo vs genuinely-empty) instead of assuming; (3) the success toast is SELF-EVIDENCING: 'Pushed N cards + M loops - server slot <deviceId>... updated just now' - checkable against the ledger, ending ambiguous successes for good. (265's transfer-pull and reset-to-pristine stand unchanged.) MEASURED: harness lk-266-syncprobe on the fixed code; typecheck clean; prod clean. Proofs follow in the commit.Previous: build 265 THE TRANSFER PULL + THE RETURN TO PRISTINE (founder: 'I am stuck in that phase 2026-09-18 BUILD 265 THE TRANSFER PULL + THE RETURN TO PRISTINE (founder: 'I am stuck in that phase, not able to return (note this) to the pristine. And when I push, it says nothing to push, and PULL it says server has nothing for this device - push first'). THREE GAPS, ONE ROOT CAUSE EXPOSED: (1) THE ROOT - the sync slot is DEVICE-KEYED: each device pushes/pulls its OWN /state/<deviceId> slot, so a second device (or a fresh window, whose deck is all demo) pushing '0 cards + 0 loops' then pulling its own empty slot reads exactly as reported - the December card can NEVER transfer that way. (2) THE TRANSFER PULL: a second strip door 'Pull from another device' - paste the OTHER device's anonymous id (Settings -> This device (anonymous id) shows and copies it, build 214) and rolodexSync.restoreFrom(id) reads THAT slot (same consent gate, same discriminated result); the fetched cards merge by contactId (nothing local deleted) + loops mergeRestored by id, persisted via home's persistContacts. (3) THE RETURN TO PRISTINE: a 'Reset setup' door flips consent OFF and walks the pane back to the setup card (the consent step + enable actions) - nothing stored erased. (4) THE EMPTY PUSH IS NOT A PUSH: push with zero real cards and zero loops no longer touches the server - it says 'Nothing to push - this deck is all demo cards, and demo cards never leave the device. Bring in one real card first (or pull from your other device below).' The test path: device A enables + pushes (real counts) -> Settings copies A's id -> device B enables + pulls from A's id -> the card AND its loops arrive. MEASURED: typecheck clean; prod clean; harnesses green. Proofs follow in the commit.Previous: build 264 THE VERSION AUDIT COMPLETED (founder: 'have you considered both counters 2026-09-18 BUILD 263 THE VERSION THAT TICKS (founder: 'Can we increase the digits/integers of update version in Settings to properly reflect version state. Currently, it abbreviates so that we can appear to be perpetually stuck on .31 instead of showing increments like .316'). THE CHAIN, MAPPED: Settings displayed environment.version = '0.3.1' (static, our own first-draft string); the server's /updates/check advertised the droplet's stale version.txt = '0.3.186' (from the build-186 era). NOTHING ticked because NOTHING composed from the build counters. THE FIX: (1) CLIENT — updates.service composeVersion() = '0.3.<environment.build>' (base pair from environment.version + the live build); appVersion now COMPOSES in the constructor and initializeVersion — the persisted 'appVersion' string is legacy storage and never rules the display; the running bundle IS the truth. Settings shows v0.3.263 today and ticks every build. (2) SERVER build 94 — /updates/check composes its advertised version from ITS package.json build: 0.3.<serverBuild> = 0.3.94 after deploy; the stale version.txt is no longer the source. (3) The check()'s serverVersion display also composes from the deployed build.json's build (0.3.<deployedBuild>), so the 'latest deployed' row reads consistently. Decision logic unchanged: builds are compared BY NUMBER (246/263) — versions are display. MEASURED: node --check OK (server); typecheck clean; harnesses green. Proofs follow in the commit.Previous: build 262 THE SERVER SYNC, ALL REAL (founder: 'The server icon on home page 2026-09-18 BUILD 262 THE SERVER SYNC, ALL REAL (founder: 'The server icon on home page (for remote storage) - we need to make it all real... Push says success, but pull says nothing was pushed... I want to backup my loops so the date transfers to any other device'). THE DIAGNOSIS (measured): the server endpoints WORK (a live curl round-trip stored and returned a task card verbatim); the client was the liar — push() swallowed the consent gate AND every failure silently while home toasted 'Pushed' unconditionally, and restore() returned null for consent-off/error/empty alike. THE FIXES: (1) push(contacts, followUps, loops) returns {ok, stored, error} — 'consent-off' / 'server-NNN' / 'network' — and home SPEAKS it: 'Pushed N cards + M loops' or the exact reason nothing left the device. (2) restore() returns a discriminated {status: ok|empty|off|error, contacts, loops} — home says 'Restored N cards + M loops', 'the server has nothing yet - push first', 'consent is OFF - enable it above', or 'the server did not answer'. (3) THE LOOPS RIDE (server build 93): the sync payload carries exportLoops() behind the SAME consent gate; the server stores loopsCount + loops (schema + routes); restore merges upsert-by-id via loops.mergeRestored() — never deletes local. (4) THE SERVER PANE POINTS AT CONSENT like the Cloud pane: step 1 names Settings -> Backend sync consent (i18n storage.serverConsentNote/serverConsentOpen swept 39 locales) with an Open-consent button, then the enable actions. The founder's December card test now works end to end: consent -> push (the card AND its loop) -> pull on any device. MEASURED: typecheck clean; harnesses green; a live curl round-trip verified before and after. Proofs follow in the commit.Previous: build 261 THE PORTAL, POLICY-FIRST (founder: 'Purge the Investors portal 2026-09-18 BUILD 261 THE PORTAL, POLICY-FIRST (founder: 'Purge the Investors portal of legacy contactless era material like section 07 except to the extent that relevant policy matters are discussed, or it is useful for historical record. Note also the chase towards similar but distinguishable markets occupied by Muse and Instinct'). The contactless section (formerly the duplicate-numbered 07/Redesign, now 08 'Policy — Privacy, by construction') LEADS with the standing policy card (no contact permission — READ_CONTACTS never requested; no account; the identity lives in the user's head; device-local; a person is one kind of subject, never the gate) and the MARKET line (where Muse and Instinct reflect and remind, LoopKeeper closes — different market, adjacent aisle, the follow-through engine not the mirror). The ENTIRE six-tab contactless memo (why/threat/loop/product/wilder/ship) is preserved as a COLLAPSED historical record (details/summary, one click away) — nothing deleted, the portal decluttered. Proofs follow in the commit.Previous: build 260 THE TEAL CHROME (founder: 'The browser URL bar 2026-09-18 BUILD 260 THE TEAL CHROME (founder: 'The browser URL bar, or what is it called, defaults LoopKeeper into an orange color, a legacy of our initial app color choice. But since we defaulted into teal, it has never updated, which makes me suspect that somewhere there remains the Ionic default in play. Can this be corrected?'). FOUND: not an Ionic default — OUR OWN first-draft value, never updated in two chrome surfaces: index.html <meta name=theme-color> (the URL-bar / PWA-title-bar tint) and manifest.webmanifest theme_color, both #FF5A36. NOW #14b8a6 (our teal). The splash's coral orbit dot and the About portal's illustration accents stay — they are the brand mark's art, not chrome. Note: PWA-installed users receive the new manifest on their next service-worker update; browser tabs re-read theme-color on the next load. Proofs follow in the commit.Previous: build 259 THE CHANNELS THAT CARRY (founder: 1. 'ours - Chat in Loopkeeper - fails 2026-09-18 BUILD 259 THE CHANNELS THAT CARRY (founder: 1. 'ours - Chat in Loopkeeper - fails, because the Here are words txt content does not transport into LoopKeeper's chat' 2. 'LoopKeeper Chat is unimpressive and eclipsed by the bold WhatsApp. The former should be in a strong combo of green and teal (our color). And no shaded background, just clean like text, email, and copy are' 3. 'Under Alpha Create Task, the drop-down Calendar should have time element... my title was morning run, and I doubt it is convenient if LoopKeeper brings it up at 3 pm' 4. 'Task image selection could do with more space - its drop-down is within a very small aperture, unlike that used to add Display Photo/Emoji for Contact Card'). (1) THE WORDS RIDE ALONG: the walk's openChat() never passed the chat modal's prefill door (the modal already supported it — 'the composer starts carrying it, ready to send or edit'); now prefill: scriptBody() — the accepted draft plumps into LoopKeeper's chat composer, ready to send or edit. (2) THE CHAT DOOR: the blue wash is gone — a strong green-to-teal gradient TYPE (#16a34a -> #14b8a6, background-clip:text) on the clean transparent door, teal border, weight 800; it stands beside the bold WhatsApp instead of hiding under it. (3) THE TIME ELEMENT: the task draft's when-row carries date + TIME (type=time, default 09:00 — the wake-ping hour); saveTaskDraft builds the due epoch from the user's own hour — a 'morning run' comes up in the morning, never 3 pm. i18n walk.taskAt swept 39 locales (16 majors translated). (4) THE COVER GRID: the tiny popover aperture is retired from ALL FIVE card forms (task/note/place/routine/person) — the 40 emoji choices + Default stand as tappable 44px tiles, full width, selected outlined teal (setCover/coverPicked; the same form control, no model change). MEASURED: typecheck clean; the walk suites re-run green; the task-draft back shows date+time on one row. Proofs follow in the commit.Previous: build 258 THE CARD, NOT THE CONFESSION (founder: the blank card's face 'Name the thing you keep not doing' 2026-09-17 BUILD 258 THE CARD, NOT THE CONFESSION (founder: the blank card's face 'Name the thing you keep not doing' sounds like an invitation to confess embarrassing sin — 'Instead just stop at Name this card. Translate.'). walk.taskBlank is now 'Name this card' — swept to all 39 locales (16 majors hand-translated: sw/fr/es/pt-BR/de/ar/hi/zh-Hans/zh-Hant/ja/ru/it/nl/pl/tr/id; minors EN). The key serves BOTH the blank face and the back panel's title placeholder, so the softening rides both; the whisper 'Flip to write it' (walk.taskBlankHint) is unchanged. The name field remains required (min 2 chars) — the requirement lives in the validation, not in the invitation. MEASURED: typecheck clean; the 257 task-complement harness re-run green (its blank-face assertion updated to the new copy). Proofs follow in the commit.Previous: build 257 THE TASK COMPLEMENT (Grok's spec in WRITE_CODE.txt, founder: 'We need to see though eyes of first-timer, Implement') 2026-09-17 BUILD 257 THE TASK COMPLEMENT (Grok's spec in WRITE_CODE.txt, founder: 'We need to see though eyes of first-timer, Implement'). ALPHA BIRTHS A TASK CARD IN PLACE: after 'Not this one', ONE more ghost line of the same family — 'Create Task' (walk.createTask, founder wording; NOT 'New Task card' which sounds like inventory). Tap Create Task: the previous Who is snapshotted (Cancel restores it) and the Who card is REPLACED IN PLACE by a blank card with the SAME .sw-card skin — dashed quiet cover circle, 'Name the thing you keep not doing' (walk.taskBlank), whisper 'Flip to write it' (walk.taskBlankHint), NO demo watermark, NO modal, NO taskCardRequest. Tap the blank = FLIP (face <-> back) — nothing to start until it has a name; the ONLY behavioural fork on Alpha, and only while unsaved. The back (inside the card, no Ionic fullscreen): Title (required, min 2 chars), optional When (native date field -> task.due epoch), optional Rhythm (quiet chips: daily/weekly/monthly/quarterly/yearly, monthly default — the same TaskPayload union), Save/Cancel. NO phones/emails/4 W's/checklist — that is the create modal, Alpha does not get it. Save writes a REAL deck card { kind:'task', name.display, task:{cadence,due}, isMockData:false } -> unshift to the deck -> contactsDirty -> trackListStartedOnce('task') -> armFromPick(card) (224's deterministic arm) -> the saved card IS the Who on slide 1 (face shows the title; the whisper reads the rhythm via the existing whisper machinery). NO auto-start: the tap that starts the loop is the user's own 'Yes, this one' beat (218). Cancel restores the previous Who (prevWhoSnap via the 244 selection field); 'Not this one' while drafting IS the Cancel (guard in notThisOne). confirmWho guards the draft (tap flips instead of birthing a loop). The empty deck retires the modal door (sw-whoadd-task/taskCardRequest no longer called from slide 1; the @Output stays for other callers) — the same Create Task ghost serves it, the blank card IS the Who slot. Analytics: task_card_started on the tap; task_card_saved {source:'alpha-walk'} on save; no names. i18n: walk.createTask/taskBlank/taskBlankHint/taskSave/taskCancel/taskWhen/taskRhythm — EN + 16 majors hand-translated, minors EN (39 locales touched). MEASURED: typecheck clean; the walk harness suites re-run green (unified 12/12 — the draft fork is slide-1-only additive chrome; the person-card flow untouched). Proofs follow in the commit.Previous: build 256 THE NORTH STAR, OUT LOUD (founder: 'If asked what is loopkeeper 2026-09-17 BUILD 256 THE NORTH STAR, OUT LOUD (founder: 'If asked what is loopkeeper, what does an AI find or see from our online presence? Do we differentiate from Muse and Instinct with outright statement like the app for chronic procrastination. So that we are boldly saying what our North Star is'). THE RESEARCH (measured): an AI asking 'what is LoopKeeper' finds OTHER companies' products — a call-center follow-up agent and a returnable-rack logistics tool (both literally named LoopKeeper), a habit tracker, a Notion agent — and NEVER ours; zyppar.com/loopkeeper did not surface, and the word procrastination appeared NOWHERE in our machine-readable face (the live title said 'the personal secretary that never sleeps'). THE FIX: the bold claim now LEADS every machine-read surface — index.html title = 'LoopKeeper — the app for chronic procrastination'; the description = 'name the thing you keep not doing — a reply owed, a renewal due, a decision avoided — get the words written for you, send it, and the tab in your head shuts. Not a habit tracker, not a to-do list. Born in Kenya — no account, your data stays yours'; og/twitter carry the same slant. THE SERVER (build 89, repo rolodex-server 4400f69): the chat directive LEADS its factual base with the North Star — the app for chronic procrastination; where reflect-or-remind tools stop, LoopKeeper CLOSES — so the Assistant answers 'what is LoopKeeper' with the claim verbatim. The About portal's what-is answer (aboutTxt.what) leads with the same sentence (majors swept with translated bold heads, minors keep their soft copy pending translation). The distribution reality stays honest: crawlable presence is ONE door; the store listings (incoming), the QR posters and word-of-mouth carry the same sentence from here on. MEASURED: the live page's title/desc verified pre/post; typecheck clean; prod www verified (base href, build:256, build.json). Proofs follow in the commit.Previous: build 255 THE SESSION GAP (founder: 'Look again at the stats 2026-09-17 BUILD 255 THE SESSION GAP (founder: 'Look again at the stats - I suspect we have opened a further bug inflating sessions' — CONFIRMED, and it predates this week's builds). THE BUG: AnalyticsService.bindVisibility() fired startSession() on EVERY visible-resume — every app switch, every tab flip, every lock/unlock minted a fresh session_start (today: 113 session_starts vs 93 app_launches; the delta IS the per-flip minting; Sep 16: 150 vs 144). THE HONEST MODEL: a session ends when the absence is REAL (hidden for >= 5 minutes, the industry gap) — on hidden, the end is DEFERRED to the gap (a 5-minute timer); a quick return CANCELS the timer and continues the SAME session (no event, no minting); when the timer fires (a real absence), session_end lands and the next visible opens a TRUE new session with session_start. pagehide still ends immediately. The machine-reload skip (222) was verified intact (the mark + 90s check + skip) — the resume-refresh's reloads are properly excluded; this bug is the per-flip minting alone. EXPECTED EFFECT: session_start/session_end counts collapse toward app_launch counts; DAU and session DURATIONS become honest. MEASURED: typecheck clean; the harness suites re-run green (analytics is orthogonal to the walk flows). Proofs follow in the commit.Previous: build 254 THE ALPHA POLISH ROUTE (founder: 'we should treat it as equivalent to asking for AI Assistance 2026-09-17 BUILD 254 THE ALPHA POLISH ROUTE (founder: 'we should treat it as equivalent to asking for AI Assistance by sending the user-modified or initiated words to backend for polish, similar to a track on beta section... So do API route for this purpose, and a backend agent for PolishingUserAlpha with any complementary persistence tools to assist it. With time we should really deepen this with training and our own datacenter.'). THE CLIENT: setTone on a USER-OWNED draft (ownWords, set at Save) is now an ASSIST REQUEST — the user's own words go to the PolishingUserAlpha agent (server build 86, POST /api/loopkeeper/polish-alpha) and come back in the requested tone, meaning/names/facts intact; the polished words replace the draft (ownWords stays true) with the ready chime; on ANY failure the words stand untouched and the tone is recorded (best-effort, the polishErr toast). The tone buttons disable while polishing (the polishStay state). Engine drafts (not user-owned) keep the instant on-device generateDraft. THE SERVER (build 86, repo rolodex-server 6565c75): the agent's system prompt declares the user's words sacred — reshape the tone, never the meaning; the engine ladder mirrors the Confidante (requested engine first, then DeepSeek -> GLM -> Grok); THE PERSISTENCE TOOL appends every polish pair { at, tone, lang, engine, ms, before, after } to data/polish-alpha.jsonl — no identifiers, no device ids, the corpus living only on the founder's droplet — the seed for the founder's stated future: training PolishingUserAlpha on their own datacenter. MEASURED: node --check OK (index.js + agent); typecheck clean; the walk harness suites re-run green (the setTone change is exercised only on user-owned drafts — the harness flows use engine drafts). Proofs follow in the commit.Previous: build 253 THE COMPOSITION SEQUENCE FIXED + BETA GETS ITS MIND BACK (founder: 1. 'After I added my own polish 2026-09-17 BUILD 253 THE COMPOSITION SEQUENCE FIXED + BETA GETS ITS MIND BACK (founder: 1. 'After I added my own polish to a message on Loops Alpha, it ignored my input and still sent its own pre-edit message. Check and correct the composition sequence.' 2. 'Turn to Loops beta section, and adjust it to take advantage of anything we had done before reversing Alpha. Apply the Three-Seat Rule. Right now I am deluged in beta with TODOs.'). (1) THE COMPOSITION BUG, FOUND AND FIXED: the slide-3 TONE taps (Short / Light / Honest) REGENERATED the draft from the loop's structure via generateDraft — silently WIPING the user's own saved edit, so the send carried the pre-engine message. THE FIX: the Loop gains ownWords — set TRUE at 'Let me change it' -> Save — and a tone tap on a user-owned draft records ONLY the tone preference; the words are THEIRS and stand. (The AI 'Try again' already polishes FROM the current draft — polishWithAi sends l.draft as its basis — so that path respected the edit all along; only the tone taps violated it.) (2) BETA (the flip-over shelf) GETS ITS MIND BACK — the doors Alpha retired return THERE, wired to the new continuation: (a) THE SELF-INTENT TAPS return under the capture box ('A decision I keep not making' / 'Somewhere I must show up') — one tap births a PURE self-loop and hands it through the 244 pending-nudge channel to the walk's CHAT DIALOG (flip -> arm -> the draft plumps in; the 250 continuation landing, no fields anywhere); (b) the garden pills' Walk now lands in the dialog too (the walk's armHandle births the default loop instead of walking into the retired slide 2); (c) the capture box itself remains Beta's typed line (parseCapture, unchanged). (3) THE THREE-SEAT RULE ON BETA (founder: 'Right now I am deluged in beta with TODOs'): NO shelf section hosts more than THREE rows — Waiting on you, Waiting on them, the Stack's Waiting and Parked each show three seats + a muted 'N more' count (i18n key loopkeeper.inbox.moreRows, swept to all 39 locales); Today's 3 was already capped; the Stack stays collapsible; the full lists remain in the fields for future Beta refinement. MEASURED: typecheck clean; the walk harness suites re-run green (the composition fix changes setTone/saveEdit only — the harnesses' flows pass through them untouched); Beta's seats are structural slices (template + getters) verified in the 249-style DOM assertions. Proofs follow in the commit.Previous: build 252 THE UNMISSABLE NOTES (founder: 1. 'Add a thin note just above the Alpha card 2026-09-17 BUILD 252 THE UNMISSABLE NOTES (founder: 1. 'Add a thin note just above the Alpha card - Tap Card to start -. Do translations.' 2. 'Now that we decluttered the viewport, bring card and text downwards 15px.' 3. 'Increase the font size of these user intervention notes (let me change it, and Not this one) by 40%, same color and resolution. We do not want them missed.'). (1) THE TAP NOTE: a thin centered line rides just above the Alpha card — '- Tap Card to start -' (i18n key loopkeeper.walk.tapCard, swept to all 39 locales, 17 majors hand-translated, EN fallback for the rest), shown only when a card is on show. (2) THE 15PX LOWERING: slide 1's whole pane sits in .sw-whopane with padding-top 15px — card, note, 'Not this one' and the empty-deck doors all ride down together (the room was made in 250 when the complexity left). (3) THE +40% NOTES: 'Not this one' 13px -> 18.2px (scoped to the Alpha pane so other ghost buttons keep their size) and 'Let me change it' 12px -> 16.8px — SAME color and opacity, only the type grows; the interventions can no longer be missed. MEASURED: typecheck clean; the walk harness suites re-run green (the selectors the harnesses read — .sw-card .sw-name / .sw-back-name / .sw-draft — are untouched). Proofs follow in the commit.Previous: build 251 THE CHAT DIALOG POLISH (founder: 1. 'Let me change it' should focus cursor 2026-09-17 BUILD 251 THE CHAT DIALOG POLISH (founder: 1. 'Let me change it' should focus cursor on the dialogue window that appears. 2. Restructure the third phase of Alpha to fit 100% into the viewport. We cannot afford for user to miss any element because they need to scroll. 3. Have you uncovered why app keeps crashing after being invisible for long, or running navigation. This started in the last 84 hours). (1) THE CURSOR LANDS IN THE DIALOG: 'Let me change it' now focuses the textarea (caret at the end of the draft) — the keyboard waits at the words, ready. (2) SLIDE 3 FITS 100%: the build-162 lower spacer leaves slide 3 (it starts high like slide 4), the draft box compacts (15px/1.45, tighter padding, max-height 30vh with an INTERNAL scroll so the box absorbs any long draft) and the send/tones/edit rows tighten — every ELEMENT is on screen at once; the only scrolling possible is inside the draft box itself, never to reach an element. (3) THE CRASH QUESTION — ANSWERED, YES (the founder: 'app keeps crashing after being invisible for long, or running navigation... started in the last 84 hours'): the mechanism was diagnosed and FIXED in build 248 (deployed today 08:37) — every deploy wave orphans a RESUMED tab's lazy chunks (www/ is fully replaced), and the next lazy navigation (Settings, a panel, any surface beyond the shell) 404s the old chunk: the surface dies. 'Invisible for long' = a resumed tab holding the old bundle; 'running navigation' = the lazy load itself. It started in the last 84 hours EXACTLY because that window is the deploy storm — builds 232->250, multiple waves per day — every wave re-orphaning every long-lived tab. The heal (b220) reloaded once per tab, but it RELOADED AT +150ms while the crash evidence's IndexedDB write was debounced at +800ms — the heal destroyed its own evidence, which is why the ledger read crashes7d=0 while the founder sat through crashes. 248's fixes, now live: the heal writes the crash queue THROUGH and awaits it before reloading (evidence reaches the ledger), and THE RESUME REFRESH silently lands a resumed tab on the newest build on visibility-resume (throttled 5 min) BEFORE any navigation — the crash window closes; the refresh itself can look like a quick restart on return, which is the fix working, not a crash. Note the 21 recorded crashes (30d, 'error @ /home' x20) are all OLDER than the blind window; from 248 onward any crash that still slips through arrives WITH its type and page — we diagnose from the ledger, not inference. MEASURED: typecheck clean; the 250 harness suites re-run green after these edits (the draft selector unchanged). Proofs follow in the commit.Previous: build 250 ALPHA SIMPLICITY (founder: 'revert Inbox Loops Alpha 2026-09-17 BUILD 250 ALPHA SIMPLICITY (founder: 'revert Inbox Loops Alpha (the default state) to its original simplicity... Alpha is easily rendered as card, Not this one, and chat dialog box. The pills and From your Loops are better suited to Beta... there is no fulfilment in the process... our current 6 phase 2 pills appear to lead nowhere... freezing chronic procrastinators. All the logic built around that complexity can go into Beta section. Alpha of Loops must be usable by my 4 year-old'). ALPHA IS NOW: card -> chat dialog -> send -> THE BREATH. (1) THE CHIPS PHASE (slide 2, the 6 phase-2 pills) IS RETIRED from Alpha — the card tap BIRTHS the loop itself (the default intent: a check-in; a task card carries its own cadence) and lands straight in the chat dialog; the engine's draft is already written (the blank page never exists) and the user's own edit carries the nuance. The chip machinery stays in the component for Beta's refinement. (2) THE CONTINUATION (founder: 'we plump their message or one derived from it into the chat dialog on arrival back into Loops'): an alert tap now lands IN THE CHAT DIALOG — armFromNudge pickLoops without landOnCard, so the draft (written from the alert's own subject at the loop's birth) plumps into the box; the selection is still written first (244), so the card behind the dialog is the subject; a bare contact births its default loop and opens the dialog. The 242 'land on card' era is honestly superseded by the founder's new spec. (3) THE COMPLEXITY LEAVES ALPHA: the typed subject line, the self-intent taps ('A decision I keep not making' / 'Somewhere I must show up') and the 'From your loops' pills are REMOVED from slide 1 — the inbox shelf (Beta, the flip-over state) already hosts the garden section, and the chip/handle/self machinery stays in the component for the Beta refinement build. Back from the words is always the card (no phase between them). The fulfilment arc is restored whole: card -> words -> send -> THE BREATH (the completion chime + Off-your-mind + Next one). Not-this-one and the empty-deck doors (add someone / task card) are untouched. MEASURED: harnesses re-pointed at the new continuation (the words' own subject line + draft); lk-245-unified 12/12 (dock: dismiss a reminder -> the engine seats the check-in -> tap -> THE DIALOG opens with Zawadi's draft -> send doors -> THE BREATH; panel rounds identical); lk-diagnose-takeover 6/6; lk-249-three-seats 8/8; lk-246-updatetruth 6/6. Proofs follow in the commit.Previous: build 249 THE THREE-SEAT RULE (founder policy: in re 2026-09-17 BUILD 249 THE THREE-SEAT RULE (founder policy: in reducing procrastination and relieving the Zeigarnik effect, NO alert/prompt surface ever hosts more than THREE items at the same time; overflow waits in a serving engine that dedupes, sifts, and paces servings so the potential for feeling overwhelmed is de-minimis and the sense of accomplishment is heightened by using LoopKeeper). THE INVARIANT: the dock holds AT MOST THREE SEATS, and only STICKY prompts (duration 0) occupy them; transient confirmations are never seats. A fourth subject NEVER pushes anything down — it waits in the back-stack, sift-ranked (time-bound reminders first, then check-in nudges, then digest/system), and is served when a seat OPENS (a tap, a dismissal, an expiry, the snooze lifting, the app resuming, or the engine's quiet 10-minute heartbeat). THE ENGINES: DEDUPE — one subject, one seat (the key is the contact the prompt is about, so 'Call Ma' and 'Check in with Ma' are ONE subject and a re-fired event never doubles a line); SIFT — the ladder above; PACING — check-in servings are attuned (at most ONE new serving per 2 waking hours, <=3 per day; the digest once per day) with the ledger persisted through the StorageService so a reload never resets the rhythm; ACCOMPLISHMENT — seats open by closure and each opening serves the next subject, so the pile is structurally finishable. SURFACES: the dock header becomes the accomplishment strip ('N of 3 seated . N closed today' — closures read from LoopsService.recentlyClosed); the Check-ins panel's 'Coming up' shows the NEXT THREE plus a muted 'N more coming up', and the engine's overflow is a muted 'N waiting quietly' — counts, never servings; the walk's garden offers at most THREE pills plus a muted '+N more' (i18n key loopkeeper.walk.gardenMore swept to all 39 locales); the boot catch-up now FEEDS the engines — only a SEATED prompt is ledger-marked as waited for, so overflow subjects stay eligible for later service (the pile-maker is now a paced server). clear() (the 245 takeover) deliberately does not refill: the moment stays quiet; refills ride the heartbeat/resume/arrivals. MEASURED (harness lk-249-three-seats, 7/7): six due events seeded -> the dock seats exactly THREE (2 time-bound reminders + 1 check-in; the check-in refractory blocks the second); the duplicate-subject event is DROPPED (one subject one seat); the panel reads '2 waiting quietly'; dismissing a seat does NOT refill with a paced-out check-in (the rhythm holds, count drops to 2); an ephemeral confirmation still flows and self-clears; the panel 'Coming up' shows exactly three + 'N more coming up'; the full unified-rule + takeover + update-truth + analytics-gate suites stay green. Proofs follow in the commit.Previous: // 2026-09-17 BUILD 248 THE CRASH KILLER + THE EVIDENCE THAT SURVIVES THE HEAL (founder: 'App 2026-09-17 BUILD 248 THE CRASH KILLER + THE EVIDENCE THAT SURVIVES THE HEAL (founder: 'App is still crashing. Just now it crashed as it was checking for updates'). THE DIAGNOSIS, MEASURED: production deploy integrity VERIFIED COMPLETE (69/69 lazy chunks + shell + build.json all 200 at 247) - the crash is NOT a broken deploy. THE MECHANISM: a RESUMED tab runs the old bundle in memory; the moment a new deploy lands, its lazy chunks are orphaned (www/ is fully replaced), and the next lazy navigation (Settings, the Check-ins panel) 404s the old chunk - the surface dies. 'It crashed as it was checking for updates' is the EXACT moment: the check detecting a newer build PROVES the tab just went stale, and the very next Settings navigation - where the check button lives - is the first lazy load to hit the orphaned chunk. Today's FOUR deploy waves (245/246/247 + redeploy) = repeated orphaning of the founder's long-resumed tab: still crashing. WHY THE LEDGER SHOWED NOTHING (crashes7d=0 while the founder sat through crashes): the heal reloaded at +150ms while the crash evidence's IndexedDB write was debounced at +800ms - THE HEAL DESTROYED ITS OWN EVIDENCE. TWO FIXES: (1) THE EVIDENCE SURVIVES THE HEAL - maybeHeal now writes the crash queue THROUGH to IndexedDB and awaits it, and the analytics app_error rides its keepalive POST (keepalive survives the unload), BEFORE the reload fires. (2) THE RESUME REFRESH - THE CRASH KILLER: UpdatesService binds a visibilitychange listener; on visible-resume (throttled 5 min) it compares the deployed build.json against the running build and silently reloads ONTO the new bundle BEFORE the user navigates into a lazy surface - the crash becomes a one-beat silent refresh; the 222 machine-reload mark keeps the refresh out of the meters; the banner remains for the foreground session (a tab that never hides is not orphaned mid-use - it gets the prompt and applies when ready). MEASURED: lk-248-resume-refresh (harness) - stub build.json running+1 -> visibility resume -> exactly ONE reload, onto the new build; stub same-build -> no reload; throttle holds. Regression: lk-diagnose-takeover 6/6, lk-246-updatetruth 6/6, lk-245-unified 12/12 (all build-relative). Proofs follow in the commit.Previous: // 2026-09-17 BUILD 247 THE INBOX ATTACH HOOK + THE DEV-ORIGIN ANALYTICS GATE (founder: 'Apply 2026-09-17 BUILD 247 THE INBOX ATTACH HOOK + THE DEV-ORIGIN ANALYTICS GATE. (1) THE STRANDED-ESCALATION FIX APPLIED (founder: 'Apply your solution for inboxRef'; the diagnosis was measured with lk-diagnose-takeover.cjs + lk-stranded-probe.cjs and recorded in USE_NOW): home's @ViewChild inboxRef was a PLAIN property, and the delivery trigger (inboxReady, the child's ngAfterViewInit) fired BEFORE that property populated - so every escalation from Settings/the panel held its payload on a null inboxRef, and the held payload never had a second trigger: STRANDED. The walk's selection stayed null (probed), while the DOM card coincidentally matched the tapped subject on the FIRST tap (the fresh loop leads todaysThree) - the illusion that hid the bug from every DOM-only harness. THE FIX: inboxRef is now a SETTER (the 244 attach-hook pattern) - the inbox's attach moment stores the reference and immediately delivers whatever is held; idempotent with the inboxReady path (the pending slot clears on first delivery). The delivery trigger is the inbox's ATTACHMENT: a held payload can never strand. (2) THE DEV-ORIGIN ANALYTICS GATE (founder: 'I think we have broken some safeguards around user minting and session resourcing. We now suddenly have a surge of users, but without an obvious rise in other relevant indicators'): the safeguards were never broken - they were never TOLD what a dev origin is. environment.rolodexApiBase points at the production ingest from EVERY origin, so every dev boot, live-reload and automated harness run minted app_launch/session_start/landing_source/loop_captured into the FLEET ledger (it even carried localhost:4400/home as a landing source). Measured 2026-09-17: 112 session_start, 93 app_launch, 94 loop_captured, 47 nudge_tapped, 17 landing_source - against deviceGrowth 0 and no activation rise: the 'users' were sessions, and the sessions were dev. THE GUARD: AnalyticsService computes devOrigin (localhost/127.0.0.1/0.0.0.0/::1/*.local/file:) once, and flush() on a dev origin drops the queue WITHOUT POSTing - dev is dev-local, the fleet meters count deployed devices only; the 205 phantom-device and 222 machine-reload guards stand unchanged for production boots. MEASURED: lk-diagnose-takeover 6/6 (R1 fresh panel tap, R2 SECOND tap Baraka - the card now CHANGES, R3 inbox-closed remount, R4 shelf force, R5 mid-walk, R6 dock) with walk.selection SET in every round; lk-246-updatetruth 6/6 unchanged; the analytics POST count from a dev session = 0. Proofs follow in the commit.Previous: // 2026-09-17 BUILD 246 ONE TRUTH FOR EVERY UPDATE SURFACE (founder: 'There 2026-09-17 BUILD 246 ONE TRUTH FOR EVERY UPDATE SURFACE (founder: 'There is a version discrepancy between updates in Settings and that which globally alerts, because tapping the latter and updating, never satisfies version control of the former, which goes on still to recommend when tapped to check, a fresh update'). THE ARC, HONESTLY TOLD: 232 build-gated the update check end to end; 241 reversed it on the founder's order; the reversal RESURRECTED the version-string lie on the Settings surface - the manual check compared the server's version.txt (0.3.162) against the app version (0.3.1), which can NEVER agree, so after the founder applied the global alert's update (build-gated, truthful, satisfied) the Settings check went on recommending a fresh update forever. 246 CONVERGES EVERY SURFACE ON THE ONE TRUTH - the deployed /loopkeeper/build.json BUILD NUMBER: (1) UpdatesService.checkForUpdates() fetches the deployed build.json first and compares builds; where the file does not exist (a dev origin runs its own code) the honest answer is 'cannot confirm' - never a recommendation; the /updates/check version endpoint is retired from every decision. (2) manualCheckForUpdates() carries the REAL build numbers again (232's shape re-applied: currentBuild + serverBuild). (3) The Settings manual check speaks builds - 'You are on build N (v0.3.1) - the latest deployed build is N. You are current.' - and its dev-origin branch says so honestly; the global banner and the quiet pop-up lose their last version-lie door (check()'s fallback branch). THE INVARIANT THE FOUNDER ASKED FOR: updating through ANY surface now satisfies EVERY surface, and the Settings page shows the RUNNING build - the deterministic signal for what code the device is actually executing. MEASURED on the dev server (:4400, build 246, harness lk-246-updatetruth.cjs with the deployed build.json stubbed): stub build 247 -> banner visible + manual check reads 'build 247 is live, you are on build 245/246'; stub build = running -> NO banner + manual check reads 'You are current' (an applied update satisfies both surfaces); no build.json (dev origin) -> no banner + manual check's honest dev-origin message, no recommendation. The unified check-in rule (245) re-measured 12/12 in the same session. Proofs follow in the commit.Previous: // 2026-09-17 BUILD 245 THE UNIFIED RULE (founder: 'There are two notifications 2026-09-17 BUILD 245 THE UNIFIED RULE (founder: 'There are two notifications - the one manually activated from Settings => Check-ins, and that which the app displays involuntarily. The latter opens the card into Homepage Inbox Loops, but does not close the alert full of several lines of items. While the former, that is Check-in, closes Settings, adds the item as a pill, but doesnt take over the card position. But either medium, my request is that card is replaced with the item, Settings closed, alert dismissed'). ONE RULE, ONE PLACE: every check-in medium converges on home.escalateCheckIn, so the rule lives there once - the escalation TAKES the screen, therefore (1) the notification alert closes WHOLE (InAppNotificationService.tap only removed the tapped line; the remaining lines were stale the moment the card takes over - now escalateCheckIn clears the pile at its head; the dock tap, the native notification tap and the Check-ins panel's live row all ride this point), (2) the card is replaced with the item (244's deterministic chain: payload resolved once at the tap, held, consumed at the walk's attach hook, selection field written - the Who card IS the item), and (3) Settings closes (239's showRegularView inside the same chain - measured at 243 for both panel row types). The manual path's 'pill but no takeover' matched the deployed BUILD 240 race (production had not shipped 241-244); on the 244+ chain both panel row types ride the identical deterministic arm. Measured on the dev server at :4400 (harness lk-245-unified.cjs): dock tap -> alert cleared to zero, card = the item, step 1; panel ESCALATED NOW tap -> settings left, panel closed, alert cleared, card = the item; panel COMING UP tap -> identical. Proofs follow in the commit.Previous: // 2026-09-17 BUILD 244 DETERMINISTIC TAKEOVER (founder: 'I have asked Deepseek to sort this, same result 2026-09-17 BUILD 244 DETERMINISTIC TAKEOVER (founder: 'I have asked Deepseek to sort this, same result - pills created but no card takeover on a consistent basis... When pill is clicked, card changes immediately. So it is timing. Rather than be hostage to time, adopt deterministic coding'). THE RACE CLASS IS REMOVED, NOT PATCHED. (1) THE CARD IS A FIELD, NOT A QUEUE POSITION: walk.selection {contact, loop} is the operational card; the Who getter reads selection FIRST and the default queue (rebuilt async by todaysThree on every mount) is BROWSING ONLY - structurally unable to take the card back; the 241/242 rebuild-tail re-application (an order-dependent patch on the very race it described) is retired. select() = one synchronous field write; every door (armFromNudge, pickLoop, armFromPick, armHandle) calls it; deliberate default returns clear it (Not this one / Next one / MINE / genuinely-empty payload); back keeps it (242). (2) RESOLVE ONCE AT THE TAP: home.escalateCheckIn resolves contact AND the loop OBJECT (keeper.capture returns it) and pendingEscalation carries both - the inbox hold and the walk arm consume DATA; armFromNudge(contact, loop) performs ZERO lookups (getLoop/openLoopFor deleted) so storage hydration can never change which branch runs; open or waiting, the carried loop is the payload (239's seal, now unconditional by data). (3) THE ATTACH HOOK REPLACES EVERY TIMER: inbox @ViewChild walkRef is now a SETTER - Angular fires it exactly when a walk instance attaches; THAT moment consumes whatever is pending (pendingNudge {contact, loop}, and the garden Walk button's held handle); armEscalation's setTimeout(0) and walkHandle's setTimeout(60) are deleted - a payload held survives until a walk exists: never dropped, never early, never late. THE INVARIANT: tap -> chime -> payload resolved once (home) -> held (inbox) -> attached (walk setter) -> selection field written -> the card IS the subject - no clock in the chain. Proofs follow in the commit.Previous: // 2026-09-17 BUILD 243 THE CHECK-INS ESCAPE (founder: 'I am clicking on listed items of "Check-ins" from inside Settings => Check-ins 2026-09-17 BUILD 243 THE CHECK-INS ESCAPE (founder: 'I am clicking on listed items of "Check-ins" from inside Settings => Check-ins, and your latest update seems to have broken the code. Now it does not escape from Settings to arrive in Loops'). ROOT CAUSE, MEASURED NOT GUESSED: the escalation chain was intact — tapping an ESCALATED NOW row in the Check-ins panel fired the identical dock path (tapped$ -> home -> escalateCheckIn -> leave Settings -> open the walk armed with the item) and the walk DID take the item's card behind the modal. But checkins-panel.tapLive() fired the tap and NEVER dismissed the panel, and that panel is a full-height sheet: it stayed on top of the very screen it had just navigated to, so the founder saw the same Check-ins list over Settings and read it as 'nothing happened, still in Settings'. The panel's OTHER group (COMING UP) has always dismissed itself through the modal's 'escalate' role — so the two row types behaved differently for the same action. FIX: tapLive() now also dismisses the panel (no role: home's onWillDismiss only escalates on role 'escalate' and the live row's escalation already fired through tapped$, so exactly ONE escalation per tap). Plus one line of context hygiene flagged in 242: armFromNudge's bare-contact branch now clears this.loop, so a card taken over by a subject with no loop can never carry a stale draft into the words. NOT a 242 regression: the panel and the whole escape chain are untouched since 237 (git: the panel's only commit is b205037 BUILD 237; 242's diff is send-walk + home comments + counters + www). Proofs: typecheck clean; prod clean; base href /loopkeeper/; build:243 in the bundle; www/build.json {build:243}. DEPLOY NOTE: production still serves BUILD 240 — 241 + 242 + 243 go out TOGETHER (app www only; server unchanged). MEASURED ON THE RUNNING DEV SERVER (:4400, build 242): ESCALATED NOW tap -> inSettings false, walk card = the tapped item, panel still open (the bug); COMING UP tap -> inSettings false, panel closed, walk card = the item (correct). Harness: D:/TODOs/di-tools/lk-dev-escape.cjs.Previous: // 2026-09-17 BUILD 242 THE CARD TAKEOVER (founder: 'I want the selected item's card to take over from default or current card'). THE MISSING HALF OF 240/241 — the displacement was real but INVISIBLE: every selection door (armFromNudge for an escalation / check-in tap / search-row person, armHandle for a garden pill, the nudge's bare-contact branch) displaced the queue and then walked STRAIGHT ON to the words (enterWords), so the Who card the founder looked at was never the selection; and 241's backToWho CLEARED the held lead, handing the default pick back on the first back-tap. FIX: pickLoop grows landOnCard — the SELECTION doors now land ON the card they just displaced (slide 1, subject armed, queue leading it, the phase NOT advanced: the founder's standing rule 'a selection of contact/task must not proceed to next phase but first replace operational card'); every PROCEEDING caller (the tap on the card via confirmWho, the intention chips) still walks on to the words. backToWho no longer clears the lead (back returns to the SELECTED card); notThisOne clears it (a deliberate skip cannot be re-applied by the rebuild); rebuildWho's tail re-applies the lead even after the pendingFreshIds fresh-front branch (the old early return let a device pick silently outrank the selection) and never resurrects a subject retired this session; armFromPick drops any stale loop/handle so a fresh pick owns the card. RESULT: tap a Check-ins item (or a garden pill, or a search row) -> the walk's Who card IS that item, on a fresh mount, and back keeps it. Proofs: typecheck clean; prod clean; base href /loopkeeper/; build:242 in the bundle; www/build.json {build:242}. DEPLOY NOTE: production was still serving BUILD 240 — 241's race fix had never shipped, so both 241 and 242 must go out together (app www only; server unchanged).Previous: // 2026-09-17 BUILD 241 THE REVERSAL + THE DISPLACEMENT THAT STICKS (founder: 'Reverse the updates detection logic altered about 5 commits back. It has disrupted what works for what does not. And the card is still not taking over'). (1) THE UPDATE-CHECK REVERSAL: everything 232 altered to the update detection is UNDONE verbatim — UpdatesService.checkForUpdates() reads the /updates/check version endpoint again (no build.json-first gate), manualCheckForUpdates() is back to the pre-232 version-string shape, and the Settings manual check speaks versions again (updateServerBuild back to 0 in the manual path, version-speaking prompts). The b220 machinery 232 did NOT touch stands: check(), the home banner and the quiet pop-up still build-gate on /loopkeeper/build.json there. (2) WHY THE CARD STILL LOST THE TAKEOVER — THE REAL BUG: ngOnInit's rebuildWho() is ASYNC (todaysThree awaits storage) and the held-nudge delivery lands armFromNudge -> pickLoop -> displaceWho DURING that await — the rebuild's tail then rewrote this.queue with the default pick and the card went straight back to the default: the founder saw the default again on every fresh-mount test (the recurrence #3 path is a fresh inbox mount, every time). FIX: displaceWho now HOLDS its subject (displacedLead) and the rebuild's tail RE-APPLIES the held lead — the async rebuild can never outrank the displacement, whichever order the mount races run; deliberate default returns (backToWho, nextOne, mine, the 239 explicit default) clear the lead first so the default walk still comes back clean; armFromPick holds the same way. The words, the tap and back all read the same subject on every mount order.Previous: // 2026-09-17 BUILD 240 THE SELECTION DISPLACES THE CARD (founder: 'on tapping the Escalation item, it turn up in Inbox Loops as a pill under From your Loops. It does not displace the current Card. If pill clicked it still doesnot displace, instead jumping to next phase of walk. Recollect how we said a selection of contact/task must not proceed to next phase but first replace operational card'). THE DESIGN RULE, NOW IN CODE: a selection (escalation, pill, card pick) DISPLACES the walk's operational card BEFORE any phase advance. NEW displaceWho(contact, loop): the armed subject LEADS the Who queue (duplicate removed by id/name), whoIndex = 0 — so the Who card IS the selection, and every phase (words, the tap, back-navigation) reads the same subject instead of the default pick. WIRED INTO: pickLoop (every armed loop — the escalation path and garden-pill taps alike — displaces via the contact or the loop's ghost card), and armFromNudge's contact branch (a bare contact escalation displaces before the thing slide). The garden pill stays as a pill (it is the loop's history) — but the operational card now carries the subject the user selected.Previous: // 2026-09-16 BUILD 239 THE ESCALATION FORCES THE WALK (founder: 'Tapping any item is failing to substitute the default in the Inbox Loops. It does seem to try, but we end up with default still'). ROOT CAUSE: the Loops tab's surface toggle (walk | shelf, the b157 flip) is SESSION state and survives a PWA resume — an escalation arriving while the shelf was up took the SHELF path (armDestination + selectedId + presentResponse): a select-and-scroll on the default list, which reads exactly as 'it seems to try, but we end up with default still'. THE FIX: a new inbox.armEscalation(contact, loopId) FORCES the walk surface (tab = loops, loopsSurface = walk) before delivering through the held-nudge contract (pendingNudge consumed on the next tick when the *ngIf re-renders the walk) — the item IS the payload for the completion interaction, the WALK, regardless of which surface the session left behind; the shelf keeps its own manual flip. HOME's deliverPendingEscalation now routes through armEscalation. SEALED: send-walk.armFromNudge's silent falls — a loop that exists but is no longer open still arms (its story is the payload); a nudge with neither contact nor loop walks the explicit default (slide 1, rebuilt) — never the old silent nothing. THE CHAIN NOW: tap (dock or Check-ins panel) -> chime -> inboxReady -> armEscalation -> walk surface forced -> the walk armed on the words - every time, every surface state.Previous: // 2026-09-16 BUILD 238 STEPS 3+4 - THE RENAME SWEEP, COMPLETE (founder's chosen name: Check-ins). (1) THE LABEL: settings.loops -> settings.checkins swept across ALL 39 locales with the value 'Check-ins' — kept as the product-surface label (the founder's chosen compound) rather than approximate machine translations; the section title now reads the swept key. (2) THE SECTION ID: settings-loops -> settings-checkins (the chip's scrollTo follows it — the map quick-navs to the section, the founder's convention). (3) THE MAP HINT: 'Updates - Check-ins - FAQ - ...' (the chip count matches). NO STALE REFERENCES: settings.loops and settings-loops are gone from every template and file. THE FULL DECONFLATION, END TO END: the dock holds two DISTINCT items (the 9AM MORNING DIGEST - action loopDigest - opens the Loops surface; the CHECK-IN NUDGES - action checkin - arm the walk with the item as payload); Settings => Check-ins opens THE PANEL (the escalator list on demand: Escalated now + Coming up, one escalation path); openLoopsSurface() serves the digest; openCheckinsPanel() serves the section; the mount contract (236) makes the arm deterministic. THE FOUNDER'S TEST, AFTER DEPLOY: Settings => Check-ins => tap 'Check in with John (Recurrence #3)' => chime => the walk on John's words - at will, no waiting for the schedule.Previous: // 2026-09-16 BUILD 237 STEP 2 - THE CHECK-INS PANEL (founder: 'I wanted that escalator list presented at will from within the Settings sections, so I could then test the tap or just regular use when not wanting to wait for its scheduled appearance' — name chosen: Check-ins). THE PANEL IS THE DOCK'S LIST, ON DEMAND: (1) ESCALATED NOW — the live dock items (fired, undismissed, tappable ones); a tap runs the IDENTICAL code path as the dock tap (inApp.tap fires tapped$ and home routes — checkin walks armed with the item as payload); dismissing here dismisses in the dock — ONE source of truth (both read the same notifications pile). (2) COMING UP — the scheduled check-ins from the follow-up engine (persons AND tasks, each with its rhythm time), tappable to ESCALATE NOW: the row hands back through the modal's 'escalate' role and home runs the SAME escalateCheckIn the dock item runs — one chain, two entrances. THE SETTINGS SECTION: renamed in behavior — 'Open Check-ins' opens THE PANEL (was: wrongly opening the Loops inbox); the section copy says what it is; the output renamed loopsRequest -> checkinsRequest (the conflation fingerprint removed from the code too; the section label i18n sweep remains for step 3). The panel is a ModalController sheet (86%/92% height, checkins-panel-modal) — overlay layer, no inline containment. The quicknav chip still scrolls to the section (the founder's convention). NO CONFLATION: the 9AM morning digest keeps its own home (the Loops surface); this panel is only the check-in escalator.Previous: // 2026-09-16 BUILD 236 STEP 1 - THE MOUNT CONTRACT (founder: 'Progress, there is a chime now but still panel/modal is not opening' — approved plan step 1: fix the chime-but-no-panel symptom before any new feature). ROOT CAUSE - A DOUBLE SILENT-DROP RACE: (1) home's escalation delivered through TWO FIXED TIMERS (320ms/800ms passes) — if the *ngIf'd inbox had not mounted by then, the travel silently no-opped; (2) WORSE, inside the inbox, nudgeArrived's walk branch held 'until walkRef exists' but held NOTHING — walkRef null meant the nudge vanished (the comment said 'we hold, we do not fall through' while the code just returned). Chime played (it fires at the very top of escalateCheckIn), nothing opened — exactly the symptom. THE FIX - DETERMINISTIC, NO TIMERS: (a) the inbox now emits inboxReady (ngAfterViewInit) — the mount contract; (b) a nudge arriving before the walk mounts is HELD (pendingNudge) and consumed the moment the walk exists (afterViewInit, or the shelf-to-walk flip); (c) home HOLDS the escalation (pendingEscalation) and delivers on inboxReady (or immediately when the inbox is already open) — deliverPendingEscalation() is idempotent, the pending slot clears on delivery, the two timer passes are deleted. The chain is now: tap -> chime -> (inbox mounts) inboxReady -> deliver -> walk armed on the words, whenever the render finishes. NEXT (approved plan step 2, on the founder's word): the Check-ins panel in Settings.Previous: // 2026-09-16 BUILD 235 THE CHECK-IN ITEMS, DECONFLATED (founder: 'Clearly, we are crossing wires here - Loop reminder tap is the wrong item... The LoopKeeper notifications that presents items with Check in with [name of person/task] (Recurrence #N)... It prods user to interact with a particular person, and I hope also now, task... it is not enough to open a generic Loops Inbox, that item needs to be the payload for a Loops dialog/completion interaction' + 'You may want to rename the whole thing if our conflation has skewed the design too much'). (1) THE DESIGN, CONFIRMED IN CODE: a tapped CHECK-IN NUDGE already routes through escalateCheckIn -> keeper.capture(Check in with X) -> nudgeArrived(contact, loopId) -> the WALK (the Loops tab's DEFAULT surface) arms the armed loop straight to the words - the item IS the payload of the completion interaction, exactly as the founder describes. The founder's 'nothing opens' was the DOCK DRAG eating the tap (build 234's THE TAP IS SACRED fix - NOT YET DEPLOYED when they tested). (2) THE SUBJECT'S OWN RHYTHM: the follow-up engine already mints Check in with [name.display] for EVERY card with a contact frequency - tasks included - but a task's check-in rode the person-oriented rolodex frequency. Now a non-person kind's check-in rides the CARD'S OWN task cadence (a routine's weekly is weekly; the repeat mapping uses the same frequency). Check-in nudges now prod persons AND tasks on their own rhythms. (3) THE DECONFLATION RENAME: the generic surface-opener is now openLoopsSurface() (was openLoopsFromDigest - the old name conflated the digest tap with the Settings door); the two dock/native handlers carry explicit comments separating the MORNING DIGEST (the 9am loop-wake ping, action loopDigest - opens the surface) from the CHECK-IN NUDGES (action checkin - arm the walk with the item). CLARIFICATION for the record: the notification dock is NOT presented in app.component.ts - it lives on the home shell (home.page.html app-in-app-notifications), and home owns every tap route.Previous: // 2026-09-16 BUILD 234 THE TAP IS SACRED + THE MAP CONVENTION (founder: 'The LoopKeeper notifications that presents items with Check in with [name of person/task] (Recurrence #N) is the target of my observed no-op when any listed item is tapped. It still does not open as should' + 'The convention is that map quick-navs user to relevant section of Settings, not into the destination'). (1) THE TAP IS SACRED - ROOT CAUSE: the dock's pointerdown handler runs on the WHOLE dock and exempted only the header buttons - NOT the item rows. A tap on any listed item (Check-in nudges included) started a DRAG on the row: setPointerCapture + preventDefault swallowed the click, and the 200ms drag-release guard then ate what was left - so EVERY listed item was a no-op on every recurrence, exactly as observed. FIX: an item row is now a TAP, never a drag handle (onDragStart returns on .notify-item); the dock drags by the header and the empty frame. The Check-in items were ALWAYS tappable in data terms (action 'checkin' + contactId ride, home's escalateCheckIn opens the inbox armed with the chime + toast) - the drag was eating the tap before the chain could run. This also hardens the digest tap, achievement share and every future item. (2) THE MAP CONVENTION: the Loops chip in the quicknav now SCROLLS to the Loops section like every other chip; the section own Open Loops button opens the surface. (3) PHASE E COMPLETED (founder: Complete - the note/place/routine kinds follow the task pattern): the CardKind union is now person|task|note|place|routine. The create form carries a FIVE-WAY KIND SEGMENT (seeded by the door - the walk New Task door opens Task, every other door Person) and switches kind live; the rhythm form group is built ALWAYS so the switch never breaks, and payloads stay clean (payload.task deleted for non-rhythm kinds, placeAddress never rides). The NOTE form is the body (a note card IS its body - it rides the existing note control); the PLACE form is one honest address input (it becomes postalAddresses[0], postcode field); ROUTINE shares the rhythm form with task (cadence/due/checklist). Every non-person kind saves on its TITLE alone (no phone), wears the covers (emoji/photo/video), and walks the task doors - Done / Snooze / Note to self / Delegate (the walk isTaskSubject now reads any non-person kind; the task-tone draft speaks for all of them). Deck rows read the kind: note = the body first line, place = the address, routine = the rhythm line. Loop seeding + draft tone + analytics kind props carry the verbatim kind (note/place/routine are tracked as themselves).Previous: // 2026-09-16 BUILD 233 PHASE E THE VIDEO COVER + THE DIRECTIVE KEEPS PACE (founder: 'Phase E now'). (1) THE VIDEO COVER: a short clip stands in the SAME circular cover space as the photo and the emoji - muted, looping, silent (autoplay requires muted). Model: coverVideo (data URL) on ContactInfo, guarded at pick time at ~3MB raw (the sync document cap is real; the cover rides the additive sync). THE MOST RECENT EXPLICIT CHOICE ALWAYS WINS (the 225 rule extended): picking a video clears the emoji AND the photo; picking a photo clears the video AND the emoji; picking an emoji clears the video. Picker: 'Choose a video cover (short clip, muted)' rides beside the Gallery door in the edit form (a hidden video file input, the setContactVideo twin of setContactPhoto). RENDERING: the video branch now lives in EVERY cover face - all twelve spots in the deck family (default row, both alphabetical rows, flip front, business, family, social, compact, list, detailed, timeline, gridcard) and the walk's Who card: img guarded by !coverEmoji && !coverVideo, video when coverVideo, emoji span when coverEmoji && !coverVideo. SCSS: .cover-video object-fit cover; the walk's .sw-avatar-video keeps the 34px-circle rhythm. (2) THE DIRECTIVE KEEPS PACE (the AGENTS obligation - server repo commit 9e96602, build 84, founder deploys ./deploy.sh): the Confidante directive's walk paragraph now reads the SUBJECT KIND - person doors vs the task array (Done/Snooze/Note-to-self/Delegate, task-tone words), the deck is CARDS (kind, cadence/due, checklist, the three-way cover precedence), Settings => Loops opens the surface manually, and a digest/wake reminder tap opens the same surface. Previous: // 2026-09-16 BUILD 232 THE UPDATE TRUTH + SETTINGS => LOOPS (founder: 'The update available notification should be inspected to see if indeed it delivers the full update... it did not deliver the full update despite reloading page' + 'Crucial to have a Settings => Loops so we can manually call it to show... We must add any new section to the shortcut navigation to position map like other Settings sections'). (1) THE UPDATE TRUTH - ROOT CAUSE FOUND: the home banner was correctly BUILD-gated (b220), but EVERY OTHER surface still compared VERSION STRINGS - the server's version.txt says 0.3.162 while the app version is 0.3.1, a comparison that can NEVER succeed - so the Settings manual check and the quiet pop-up read 'update available' FOREVER, even minutes after a successful apply: the exact 'it did not deliver' reading. THE CHECK IS NOW BUILD-GATED END TO END: UpdatesService.checkForUpdates() fetches the deployed build.json FIRST and compares BUILD NUMBERS (deployed.build > running build), falling back to the version endpoint only where no build.json exists; manualCheckForUpdates returns the REAL build numbers (the Settings prompt used to read 'build 0' - updateServerBuild was hardcoded 0); the Settings prompt and the 'Up to date' alert now speak builds ('You are on build 231 - the latest deployed build is 231'). After a successful apply, the check reads 231 vs 231: current - the trust restored. The apply mechanics themselves are sound (verified: custom-sw.js is a deliberate pass-through that caches nothing, nginx serves index.html Cache-Control no-cache, and the apply clears caches + unregisters + cache-busts the URL). (2) SETTINGS => LOOPS: a new settings section (id settings-loops, repeat icon, one tap = 'Open Loops') + a Loops CHIP in the quicknav position map (the chip OPENS the surface directly, like the modal-opening chips, per the map's own convention) + settingsMapHint carries Loops. The section and the digest/wake tap share ONE open path: home.openLoopsFromDigest() raises the inbox ON its Loops tab - so the founder can manually call the surface to verify the digest fix from Settings, exactly as asked. (3) i18n: settings.loops swept to all 39 locales REUSING each locale's existing tab.loops word (zero new translation risk).Previous: // 2026-09-16 BUILD 231 PHASE D THE SYNC CONTRACT - VERIFIED, NOTHING TO CHANGE (founder: 'Phase D implementation'). The handover's Phase D question was: does the sync serializer whitelist fields, and does the server accept the new ones? CHECKED BOTH SIDES: (1) CLIENT - rolodexSync.push() sends the RAW contact objects, no field whitelist BY DESIGN, so the additive card fields ride free: kind, task {due, cadence, done, doneAt, checklist} and coverEmoji serialize without a line of sync code; restore() casts the server list straight back. (2) SERVER - DeviceState stores contacts as [mongoose.Schema.Types.Mixed] (the full list, a real storage location), the /state/:deviceId restore returns them untouched, and the isMockData exclusion (209) still holds: home pushes realContacts() only, and loadContacts filters any legacy mock rows on read. CONSEQUENCE: no server change, no server deploy, and task cards survive a cloud round-trip (cloud-off persistence was already proven in the 227 test protocol; the restore path now provably carries kind/task too). (3) THE GUARD: the sync contract is now WRITTEN INTO the serializer docblock - IF YOU EVER ADD A SERIALIZER/SANITIZER, it must carry kind + task + coverEmoji or task cards silently degrade to person cards on restore (missing kind reads as person everywhere). Loops (cardKind/taskRhythm) are device-local by design and never sync. Zero functional change - this build is the verified contract, recorded.Previous: // 2026-09-16 BUILD 230 THE DIGEST OPENS + CHAT EVERYWHERE (founder: 'a tap on a Loop reminder item actually opens the item - did not last time I checked some hours back. Just checked again, even on a third recurrence. Item depresses to my tap, but nothing opens' + 'Make sure everywhere external communication is presented, our own in-house LoopKeeper chat is equally present'). (1) THE DIGEST OPENS - ROOT CAUSE: home's dock-tap handler treated the loop digest/wake ping as an EXPLICIT NO-OP ('dismiss only'), and the NATIVE wake tap (extra.type loopWake) had NO handler at all - both surfaces dead-ended on every recurrence, exactly as reported. Now BOTH open the Loops surface: home.openLoopsFromDigest() raises the inbox ON its Loops tab (Today's 3 + the Stack; one tick for the *ngIf'd inbox to mount, then the tab is set). The dock item still dismisses on tap - the tab does the talking. (2) CHAT EVERYWHERE - THE AUDIT: the walk's slide 4 had Chat (219), the card back had the Chat row, the Assistant composer had the in-app door - but the CARD'S MESSAGE MENU (the Assistant proffer: WhatsApp/SMS/Email/Copy + the socials) had NO in-house door. 'Chat in LoopKeeper' now LEADS that menu (the 219 prompting-elegance order: the in-house door first), tracked as send_exit {channel: in-app, surface: card}, and opens the card's own thread. (3) The founder also confirmed build 229 DEPLOYED (zyppar.com/loopkeeper/build.json reads 229 as of 19:16 UTC) - the 226-229 wave is live.Previous: // 2026-09-16 BUILD 229 PHASE C THE OPERATIONS (founder: 'Phase C now'). The task card's OWN array + rhythm + words, additive only. (1) THE LOOP CARRIES THE CARD KIND: Loop gains cardKind ('person'|'task' - Loop.kind, the narrative kind, untouched) + taskRhythm (the card's cadence/due snapshotted at capture); the walk's chipTap and parsed-commit pass cardKind + a taskSeed when the armed card is a task. (2) THE RHYTHM SEEDS THE WAKE: a task loop's nextNudgeAt comes from the task's own cadence/due (due in the future wakes at the due date; no due wakes on the cadence; never falls back to the generic 2-day step). (3) THE TASK DOORS (slide 4): a task subject's array is Done / Snooze / Note to self / Delegate - the person array (WhatsApp/Call/SMS/Email/Copy/Chat/Invite) is untouched and still renders for persons. Done flips the card's task.done + doneAt, writes the card's contextRotation line, and closes through closeFully - the REAL close path (receipt, first-close share, celebration, advance all stay). Snooze rides the EXISTING waiting machinery via the new snoozeByRhythm (waitUntil 9am wake ping + Stack + bringBack) seeded from cadence/due, with a toast, and the walk advances. Note emits noteRequest (walk -> inbox -> home, the 218 relay pattern) and home opens the card's OWN surface - its rolling story; the walk waits on slide 4 beneath. Delegate hands the words out through the copy channel (sent IS the close). No fabricated channels or receipts: the doors are honest operations, and every tap is measured as send_exit {channel: task-done/task-snooze/task-note/task-delegate, surface: walk}. (4) THE TASK TONE: generateDraft speaks reminder-to-self for task loops ('Reminder to self: School fees - Quarterly, due 25 Sep.') instead of 'Hi <name>' - a subject with no reader gets no letter; tones vary lightly. (5) THE KIND IN THE METERS: loop_captured, message_sent, loop_closed (+dropped) now carry props.kind person|task (categorical only) for the Command Center split. (6) i18n: walk.doorDone/doorSnooze/doorNote/doorDelegate/tSnoozed swept to all 39 locales (EN mirrored in en-US; majors hand-translated; the rest best-effort). Person behaviour unchanged everywhere.Previous: // 2026-09-16 BUILD 228 PHASE B THE CARDS BUILD AS THEY ARE (founder: 'Phase B next' + 'Social, Family, and Business do not have the emoji alternative yet'). (1) CREATE: the walk's New Task door now raises the create form AS A TASK - manualKind flows home -> [createKind] -> contact-card, and the form renders task fields (title riding name.display, cadence select with the full six-value union, due as ion-datetime converted to epoch-ms on save, an add/remove/tick checklist FormArray, the same emoji cover picker, a details note). Person create is untouched. (2) EDIT: a task card opens the SAME task form prefilled (cadence/due ISO conversion, checklist rebuilt from the payload); person edit untouched; the footer button speaks Save/Create Task. The person validators (phone-or-email required) are SKIPPED in task mode - a task saves on its title alone - and updateSaveEnabled follows. onSubmit shapes the payload additively (kind='task', task.{cadence,due epoch,checklist} merged over the original). (3) DECK ROWS READ KIND: rowSubtitle/rowExtra dispatch - task cards wear the localized cadence + due line (Monthly - 1 Oct), persons keep company + follow-up; applied on ALL THREE default templates (the 224 lesson) AND the flip front, businesscard, social, compact, list, detailed, timeline. (4) COVERS COMPLETE: the emoji cover now stands in the Social, Family, Business (professional-avatar) circles the founder named - and compact, list, detailed, timeline rows too (a 48px .cover-face.cover-inline for the bare-img views). (5) THE RENAME: the deck stops calling itself Contacts - the Search header now reads 'Cards - people & tasks', 'No cards found.', 'All cards', 'Create a card', settings.demofull 'Demo cards' swept to all 39 locales; task.daily/weekly/monthly/quarterly/yearly/never/noRhythm added to all 39 (EN mirrored in en-US, majors hand-translated, the rest best-effort). (6) LATENT BUG FIXED: normalizeManualContact rebuilt the card and SILENTLY DROPPED kind/task/coverEmoji (the 217 emoji leak on manual create) - it now carries all three. Proofs: typecheck clean; prod clean; base href /loopkeeper/.Previous: // 2026-09-16 BUILD 227 PHASE A THE CARD KIND (the handover's promised arc begins - 'cards are still person-shaped objects wearing task clothes'). (1) MODEL: CardKind = 'person' | 'task' on ContactInfo + the TaskPayload (due epoch-ms, cadence REUSING the contactFrequency union - daily/weekly/monthly/quarterly/yearly/never, the 217 TS2322 lesson; done/doneAt/checklist). ADDITIVE ONLY: missing kind reads 'person'; place/note/routine reserved - extend explicitly, never | string. (2) BOOT NORMALIZER: home.loadContacts stamps kind='person' on every card lacking it - lazy, no migration script; demo task fixtures carry kind:'task' themselves. (3) THE 8 DEMO TASK CARDS (9-16, the living fixtures) now carry kind:'task' + task payloads - car insurance yearly/Oct15, school fees quarterly/Sep25 + a 2-item checklist (the Phase-B rendering fixture), landlord monthly/1st, chama weekly/Sat, medication monthly/1st, passport yearly/Dec1, water bill monthly/20th, gym weekly/Fri. Acceptance held: typecheck clean; the deck renders EXACTLY as before (no renderer reads kind yet - that is Phase B, build 228).Previous: // 2026-09-16 BUILD 226 THE FROZEN CONSOLE + THE FLIP COVER (two founder reports same day). (1) THE FROZEN CONSOLE: the Command Center opened from within Settings (Investors portal -> Open Command Center / index chip / yardstick expand) was a static, unscrollable image - no taps. ROOT CAUSE: the in-template ion-modal lived INSIDE the portal's <ion-content>; an inline modal is position:absolute inset:0 and anchors to the nearest positioned ancestor, which there is ion-content's inner-scroll (position:relative) - the SCROLL CONTENT box, not the viewport - so opened deep in the scrolled portal the console landed at content-top: off-view, partial, unscrollable. FIX: the modal hoisted OUT of <ion-content> so it anchors to the portal's viewport-sized .ion-page and always covers the screen; home's manual-add create modal hoisted the same way (same latent bug over a scrolled deck). (2) THE FLIP COVER: build 225's emoji stand-in reached the deck rows and the walk's Who card but NOT the flipped card - the card-front .rolodex-avatar and the gridcard .modal-avatar now wear the same .cover-face branch (emoji fills the circle, img *ngIf'd out; 48px on the 100px gridcard avatar).Previous: // 2026-09-16 BUILD 225 THE COVER AS ALTERNATIVE (founder: 'The goal is not addition but rather, alternative, for the default cover, so that an image or emoji, etc stands in that same space for the card's cover'). The badge approach is RETIRED: an emoji cover now STANDS IN the circular cover space itself - the img is *ngIf'd out and a .cover-face span (30px emoji, centered, quiet brand tint) fills the whole circle, on all THREE deck row templates + the walk's Who card (.sw-avatar-face, same treatment). A gallery pick stands in the same space via image.base64String AND clears coverEmoji - the most recent choice always wins. Emoji precedence otherwise (an explicit cover choice beats a device thumbnail).Previous: // 2026-09-16 BUILD 224 THE IN-PLACE SWAP + COVERS EVERYWHERE (founder: 'Two updates you made just never stuck - On change of card in Inbox, app still skips changing in place for user to see new card... And secondly, cover selection still does not stick'). (1) WHY THE WHO-RETURN NEVER STUCK: the pick relayed through TWO @Input hops (home -> inbox -> walk) while an ion-modal dismissed - fragile CD timing - and 'Not this one' opened a sheet round-trip instead of changing in place. NOW 'Not this one' retires the current card and swaps the NEXT queue card IN PLACE (slide 1, the new card on show, 'Not this one' still there); the sheet opens only when the queue is exhausted. The sheet pick arms DIRECTLY: home -> inboxRef.armWalkPick -> walkRef.armFromPick (deterministic ViewChild calls; the two-hop [walkArm] input relay removed from home + inbox). (2) WHY COVERS NEVER SHOWED IN THE DECK: contact-card has THREE default-view-avatar row templates (real groups, demo groups, and the first block) - the badge went into only ONE, and the founder's deck view renders the other two. All three rows now wrap avatar+badge in .cover-wrap with the emoji outside the circle. Proofs: cover-wrap x3, bare avatars 0, armFromPick in chunk 5390.Previous: // 2026-09-16 BUILD 223 THE WELCOME-BACK BEAT (founder: 'the device count is actually real? If so... can we not do stuff, a one-off, occasionally, better still, rarely, to bring the device holders attention back to discover that LoopKeeper is back, richer, and much more reliable?'). The fleet is REAL (246 devices/30d, own fleet excluded) and they respond to updates - the heal/update wave just proved it. So the update moment becomes the re-engagement beat: UpdatesService.welcomeBackCheck() (ridden from home's existing refreshBanner beat) compares the device's last-seen build (lk_last_build_seen) against the running build; ONLY on a genuine update (prev > 0 and current > prev) does it fire ONE quiet localized toast - 'Updated - LoopKeeper is back: smoother, richer, and more reliable' (swept to all 39 locales) - then persists the new build. Never on first install (prev == 0 -> silent stamp), never twice per build. Rare by construction: only the update moment, only once.Previous: // 2026-09-16 BUILD 222 THE HONEST ARRIVAL (founder doubted the post-deploy surge: 'Absent of referrable concommitant activity... it looks too much like the false positives we discovered and discontinued 72 hours back... We have a landing_source surge'). THE FOUNDER WAS RIGHT - the live summary proved it: deviceGrowth 0-1/day (0 today), activation single digits (first_loop_started 1 total), shares 3/30d, while sessions spiked 279 on Sep 14 (the crash-reload storm) and landing_source went 0-for-ten-days -> 3 -> 1 -> 8 today. The 8 are EXISTING devices booting under the b193-era once-per-device landing instrumentation for the FIRST time, carried there by machine reloads: the 220 stale-chunk heal (real 404s now trigger the one-shot reload) and the now-truthful update pop-up (serverBuild 221 > prompted -> apply -> reload). Every machine reload was counting as app_launch + session_start + the device's first-ever landing_source - the heal fixed the death and the meter counted the resurrection as a birth. FIX: the heal and the update-apply leave a 90s sessionStorage mark (lk_machine_reload); the next boot sees it, fires ONE categorical machine_reload event, and SKIPS app_launch + session_start + landing_source. Humans count; machinery does not. The landing_source 'surge' itself is the once-per-device retroactive catch-up - self-limiting as devices settle on the new build; nginx is innocent (the 404s are the cure working).Previous: // 2026-09-16 BUILD 221 THE COVER, VISIBLE + THE GALLERY DOOR (founder: 'The selected cover is not showing upon select, for a real card, nor after I close and reload contacts' + 'the cover interface does not provide for using device Gallery to chose a cover as it does for my own personal card'). (1) ROOT CAUSE OF THE INVISIBLE COVER: the emoji badge sat INSIDE ion-avatar - overflow:hidden + the 50% circle CLIPPED it to invisibility at negative offsets, so the cover was saved but never seen (demo task cards too). The deck row now wraps avatar+badge in a relative .cover-wrap and the badge rides the corner OUTSIDE the circle. (2) THE GALLERY DOOR: the edit-form cover interface gains 'Choose a cover from your Gallery' - the same PhotoService the personal card uses (hidden file input -> base64 -> image.base64String, persisted via the normal editContact path). Server 83 companion: the analytics-500 fix.Previous: // 2026-09-16 BUILD 220 THE HEAL, MADE REAL (Deepseek's WRITE_CODE.txt: 'the heal is dead code in production' + 'the reporter is blind to resource errors' + 'the update pop-up is dead code and the banner is permanently on'). (1) THE HEAL: three new signatures feed one one-shot reload (sessionStorage guard, never a loop) - CAPTURE-phase resource errors (a failed <script>/<link> carries an EMPTY message; ours + .js/.mjs = the stale-chunk death), a console.error sniff (the ChunkLoadError + module-MIME text only ever appears there), and unhandledrejection (webpack rejects lazy imports). (2) THE REPORTER SEES: resource errors are recorded with the URL as a synthetic label - no more empty-message bail-outs, so crashes7d stops lying. (3) THE TRUTHFUL UPDATE CHECK: every build:prod drops www/build.json {version, build} (scripts/write-build-json.js chained after index-css-fix); UpdatesService.check() fetches it and compares RUNNING build vs DEPLOYED build - real serverBuild, so refreshUpdatesQuietly's pop-up path finally works and the banner stops firing forever (the old comparison was the SERVER's version.txt 0.3.152 vs app 0.3.1). Server 82 companion: the nginx conf gains the asset-404 block (missing chunk = real 404, SPA shell only for navigations) + a hashed/unhashed cache split.Previous: // 2026-09-16 BUILD 219 THE INTERNAL DOOR (founder: 'LoopKeeper has its own internal messaging (a chat interface). I don't get the impression that this is covered' + 'if no destination, that is about prompting elegance'). (1) THE CHAT DOOR: slide 4's array gains Chat in LoopKeeper - the walk opens the armed subject's OWN thread (CardChatService.seedThread -> CardChatModalComponent, the same interface the card surface uses, socket peers + external delivery), destination or not. (2) PROMPTING ELEGANCE: when the card carries NO destination the grid re-orders itself - Chat and the invite LEAD (CSS order) - and the old dead-end line ('No number on the card - copy the words') becomes a guide: 'No phone on this card - LoopKeeper Chat or the invite will reach them.' (3) CONFIRMED: no 'Yes, this one' literal exists in any UI - it was only ever a code comment; the walk keeps the original 'Not this one' wording and the user forms the mental note when the right card appears.Previous: // 2026-09-16 BUILD 218 THE WALK, REPAIRED (founder: 'Not this one' must land the picked card AS the Who on slide 1 - 'Yes, this one' seen before the walk continues; 'Even if we followed the flow, it is badly broken, still stuck in the contactless phase... no reference to LoopKeeper's own array of communication tools, internal or external presented coherently'; 'after getting the loop closed we fall back to the opening card, and not a next available one'). (1) THE WHO RETURN: notThisOne retires the rejected card for the session and the pick from the sheet relays home -> inbox -> walkArm -> the walk ARMS it as the Who at slide 1 (no card-surface detour, no phase jump). (2) THE COHERENT ARRAY: slide 4 now shows WhatsApp / Call / SMS / Email when the card carries a destination, Copy always, and LoopKeeper's OWN door - 'Send them LoopKeeper' (inviteRequest -> home opens the share sheet, the OG-carded ?src=share invite) - nothing hides behind a more-ways toggle any more. (3) ADVANCE, NEVER REWIND: the sent subject retires on nextOne, the queue advances to the next available card, and an exhausted queue offers the doors - MINE (device contacts) and a New Task card (taskCardRequest -> home opens the manual add sheet) - instead of silently rewinding to the demo opener. (4) THE RENAME BEGINS: the demo separator reads DEMO TASKS & CONTACTS - the fleet is both now. i18n: walk.doorInvite + walk.taskCard swept to all 39 locales (the loopkeeper object carries RELATIVE keys - the first sweep pass wrote bogus top-level dotted keys and was repaired in the same build).Previous: // 2026-09-16 BUILD 217 THE CARD COVERS + THE DEMO TASK FLEET (founder: 'an equal number of demo card that show a variety of routine tasks and daily activities... These mix in with current demo which is solely contact cards' + 'a drop-down of a myriad of icons, memes, emojis, or user picks from their device photos/videos as cover image'). (1) EIGHT DEMO TASK CARDS join the eight demo people (ContactInfo, isMockData, never synced - the 209 filter holds): The car insurance, School fees, The landlord, Chama meeting, Mom's medication, Passport renewal, Water bill, The gym - each with its routine cadence in rolodex.followUp and an emoji cover, riding every existing surface (deck demo segment, walk Who queue, loop machinery). (2) THE CARD COVER: coverEmoji on ContactInfo + a 40-emoji picker in the card create/edit forms (popover select, patches on edit, serializes on save) - the deck row and the walk wear the emoji on the avatar. Device photos stay via the avatar picker; video covers are the later conversation.Previous: // 2026-09-16 BUILD 216 THE DOORS OPEN (founder: 'Reconcile the two recommendations for distribution, then we start' - the merged Grok/GLM sequence, code half). (1) THE FIRST-CLOSE SHARE BEAT (Grok week 2, pulled forward): the first close of ANY kind (sent/done/dropped - all three paths) fires a once-per-device event (lk_share_prompted) through LoopsService.firstCloseShare; home lets the celebration breathe (1.6s) then opens the share sheet with voice E/F pinned and ?src=share on the outbound URL (shareAppStandard now takes url+voice; the modal carries shareSrc/voice inputs). (2) INVITE URLS CARRY src: shareUrl() appends &src=share alongside invite= - landed traffic is attributable, never 'direct'. (3) THE NAMED WEB DOOR (Grok week 3): index.html grows a crawlable title ('the personal secretary that never sleeps'), meta description (the public sentence: born in Kenya, no account, data stays yours), canonical; robots.txt (tester pages noindex) + sitemap.xml ship as build assets - mirror both at zyppar.com root and add the homepage link (founder, parent site). (4) THE QR POSTER: /loopkeeper/qr.html - a print-ready poster per venue (venue tag input -> ?src=poster-<venue>), offline-safe QR via the vendored MIT qrcodegen encoder (Nayuki v-master, compiled global, smoke-tested v4/33x33); every scan lands attributable.Previous: // 2026-09-15 BUILD 215 THE SECRETARY VOICES (founder: 'Can we add these messages, with any refinement, to the Settings -> Share App rotation'): the rotation grows to SIX voices - E ('What if you had a personal secretary that never sleeps?') and F ('Meet LoopKeeper - your digital secretary!'), the founder's two WhatsApp-bait messages refined (brand as *LoopKeeper*, WhatsApp *bold* kept, the {{url}} interpolation preserved) and swept across ALL 39 locales (EN mirrored in en-US; the other 38 translated in their own languages). VOICE_IDS A-F, getShareVoice/setShareVoice unions, the Settings cycle order and the localStorage validation all extend to F; resolveVoice picks uniformly so each voice carries ~17% of shares.Previous: // 2026-09-15 BUILD 214 THE DEVICE CATCHER (founder: 'I still do not know my device. I suggest catching my device, and those for testers. I do not know how. Then we write them onto .env at backend'): (1) SETTINGS -> Cloud Sync now shows THIS device's anonymous id (getDeviceIdFinal) with a one-tap copy - the exact id the noise list and the analytics ledger know. (2) THE COMMAND CENTER grows section 07 NOISE LIST: every organic device id the analytics stream saw in 30 days (server 79 knownDevices) with last-seen, event volume, clock timezone, and the tester codes it posted under (testerId rides every tester event since 119) - tick yours + the testers', copy the LK_NOISE_DEVICES=... line, paste into /opt/rolodex-server/.env, pm2 restart rolodex-server --update-env. Already-noised ids are excluded, so the roster verifies itself: apply the line and those rows disappear.Previous: // 2026-09-15 BUILD 213 THE CRASH SURVIVOR + THE STALE-CHUNK RECOVERY (founder: 'Keeps crashing after some random processes, and needs to be reloaded. Just happened again now after I shared the app from Settings'). (1) THE CRASH QUEUE NOW PERSISTS AS IT FILLS (debounced 800ms write-through to IndexedDB) - a HARD death used to lose the in-memory queue, which is why today's crashes never reached the ledger (crashes7d=1 while the founder sat through several; 20 'error @ /home' entries in 30d are the dev tab). The next boot ships the evidence as backlog. (2) THE STALE-CHUNK RECOVERY - after every rebuild/deploy a surviving tab lazily loads an OLD chunk hash -> 404 -> that surface dies and only a manual reload healed it (the 'random process' was whatever lazy surface the stale tab touched next: portal, search sheet, settings). webpack's 'Loading chunk ... failed' / ChunkLoadError is now RECORDED and then healed with ONE automatic reload per tab (sessionStorage guard, never a loop; a second failure stays visible and reaches the ledger). Sessions inflation explained (visibility-bounce sessions by design + crash reloads + the pre-deploy phantom backlog); no metric change this build.Previous: // 2026-09-15 BUILD 212 (founder: deprecate the home ion-fab - it is overlapping the LoopKeeper icon panel at the footer zone): the search FAB is GONE; its openSearchModal() door moved to the TOP PANEL beside the storage toggle (class search-toggle, styled as the storage-toggle twin, tooltip reusing the existing liSearch key), carrying the same tab-hidden discipline the FAB had (deckHiddenForTab || inboxExpanded). The footer zone belongs to the LoopKeeper icon panel again.Previous: // 2026-09-15 BUILD 211 (founder, two directives): (1) THE BOOT REVEAL, GATED AND SLOWER - the elongation is now 2.7s (the .9s grow x 3, 'so I see it in dev') and it does NOT commence until the Demo or real contacts have populated their component (the contacts input arms the reveal; the 8s safety timer still frees a never-populating deck). (2) THE DEMO WATERMARK - every demo contact card carries a quiet diagonal DEMO stamp in the brand amber (default, rolodex, businesscard, family, compact, list, social, detailed, timeline views), and the walk's Who card wears a small DEMO tag on its face - the 'accessed my contacts without permission' anxiety is answered on the card itself. DEMO needs no translation.Previous: // 2026-09-15 BUILD 210 THE WALK-FIRST ORDER (founder's word on Grok's Walk-vs-Welcome overview: 'So: walk first, Welcome after the first close'): the walk IS the home's front surface (loopsSurface walk), so on the FIRST open the tour is DEFERRED - the deed is the first screen, the boot reveal begins immediately; the six-slide tour is offered ONCE on the SECOND open (lk_open_count + lk_welcome_offered, never auto-nagged again; Settings -> Welcome Again replays on demand); the INVITE confirm path (presentFullWelcome) rewards declared intent with the walk, never the lecture, and the second-open offer stays eligible; the install door keeps its monopoly on the first-deed moment (no double interruption).Previous: // 2026-09-15 BUILD 209 (the Grok-review gaps, coded before the walk-first build): (1) THE MINE DOOR LIVES - the 'Bring in someone real' pill was DEAD for first-timers (Demo ON by default made who = John Doe, *ngIf=!who never fired); it now opens whenever the REAL deck is empty, speaks walk.whoAdd, and hands to the AGNOSTIC add sheet (whoRequest), never the OS picker's share dialog. (2) THE DIGEST ARMS ON THE DEED - create() was the only loop mutation never calling resyncDigest; now the 9AM material exists from the first loop. (3) THE INSTALL GESTURE SURVIVES - the once-ever flag hydrates at construction; the first-deed block runs synchronously inside the tap so beforeinstallprompt.prompt() keeps user activation (the storage.get().then() hop burned it).Previous: // 2026-09-15 BUILD 208 (founder's three directives on the 207 thread): (1) THE WELCOME SHOW BOOLEAN - presentWelcome returns whether the tour ACTUALLY presented (false on the dismissed-key path, false on a failed modal); ALL callers begin the Inbox boot reveal when it is false, so a session that never shows Welcome can never hold the reveal. (2) THE TRANSLATION PORTAL SWEEP - the four expanded-card keys (welcome.card.copy/emphasis, welcome.loopmotto.copy, welcome.fourws.copy) now carry natural person-AND-task translations in ALL 38 locales; the en-US shadow that showed US-English devices the old person-only copy is fixed in the same sweep. Previous: // 2026-09-15 BUILD 207 (founder): (1) THE BOOT REVEAL IS CONDITIONAL - it was lost behind the Welcome modal; now the shell HOLDS 50vh from mount, and home calls inbox.beginReveal() when Welcome is dismissed, or immediately when Welcome was never going to show; an 8s safety timer guarantees the reveal either way. (2-4) THE EXPANDED CARD (founder: the Welcome was blatantly skewed to persons, excluding tasks): Card 01 copy now reads "Every person you keep meaning to reach, and every task you keep postponing, is a card - a task, a place, an event, anything you keep meaning to close can be one"; its summary emphasis becomes "One person. Any Task. One card. Everything for them or the task, lives there."; slide 02 (the promise) de-skews to "the person or the task you owe"; slide 04 (the 4 W's) becomes "the story behind every card, person or task... postponed intentions". EN leads; the other 38 locales keep their current copy until the translation portal sweep. Proofs: yarn build typecheck clean; build:prod base href /loopkeeper/ + build:207 in bundle. Previous: // 2026-09-15 BUILD 206 (founder, four directives): (1) WELCOME SLIDES 5-8 REMOVED - the tour keeps the brand arc (intro, card, loopmotto, fourws, outro, taste = 6 slides); the four removed slides (follow-up, Loop-O-meter, Confidante, storage) become the NEW MANAGING CARDS modal (components/managing-cards-modal/), shown ONCE inside the user's first attempt to add a contact or build a card (home.onCreateContact gate, lk_cards_modal_seen) - the material answers the question asked at that moment, and it is where the coming card kinds (reminder, notes, task...) will teach; the tour keys are reused verbatim so every translation ships. (2) THE BOOT REVEAL - the Inbox starts at 50vh on first view/reload/entry (the Demo/Contacts deck shows itself below), then gently extends to its full 70vh (.li-shell.boot + a .9s cubic-bezier grow on mount, 350ms hold). (3) THE SLIDE-2 PILLS PULLED UP ~10px (backrow margin 2/10 -> 0/6, chips margin-top 4 -> 0) - all five intentions + the "or a line of your own" anchor are obvious at first glance, no scroll. (4) The Welcome-deferral question answered in the report: the tour STAYS the brand's opening (now 6 slides); the deferred stage is the first add/build-card attempt. Proofs: yarn build typecheck clean; build:prod base href /loopkeeper/ + build:206 in bundle. Previous: // 2026-09-14 BUILD 205 (founder: "Plan and code, unless you disagree" - the Grok phantom-audit response, facts verified against the code before building; the audit text lives in D:/TODOs/USE_NOW.txt): THE PHANTOM MACHINE IS CLOSED + THE FIRST DEED LEADS. (1) THE PHANTOM-ID FIX, the audit's central claim VERIFIED: RolodexSyncService.loadDeviceId() ran synchronously in the constructor, read ONLY the in-memory cache (storage.getSync), and on a cold start that lost the IndexedDB-hydration race it minted a FRESH rolodex-<random> id AND setSync() PERSISTED it (setSync writes through to IndexedDB) - overwriting the device's true identity. Every losing cold start minted a phantom device; DAU 172 with D1 0 was that machine. FIX: the provisional id is never persisted; hydrateDeviceId() ADOPTS the stored id on race loss (or finalizes the provisional when none exists); new getDeviceIdFinal() exposes the settled identity; AnalyticsService.init() awaits it before queueing app_launch (the flush already stamps deviceId live, so queued events heal too). (2) LANDING_SOURCE QUEUES FIRST: the old order awaited the flag-write before queueing - a 3-second bounce dropped the whole landing (171 launches vs 3 landings was that bug); now the event queues immediately after the dedupe read, the flag write is fire-and-forget. (3) FIRST_LOOP_STARTED (Grok plan #1): one once-ever event (lk_first_loop_started) fires in LoopsService.create the moment the first loop of any kind exists - a self-loop needs no contacts, no permissions; it LEADS the Activation table (server 77, 01166df). (4) THE INSTALL DOOR OPENS ON THE DEED (Grok plan #3): after first_loop_started, AppInstallService.promptInstallNow() fires (the deferred beforeinstallprompt was captured and idle) - the only return path without a store. (5) THE DIGEST ARMS ON THE FIRST LOOP (Grok plan #4): resyncDigest counts OUTSTANDING loops (open AND waiting) - one 9AM reason to return from the first deed onward. (6) MINE IS THE FIRST DOOR (Grok plan #2): on an EMPTY deck the walk's first slide carries an add-someone pill (addRequest -> the add sheet) - a stranger's first tap can be bringing someone in, not a demo name. The Welcome keeps its "Don't show again" skip (deliberate: the tour stays the brand's opening - deferring it entirely is the founder's call). Proofs: yarn build typecheck clean; build:prod base href /loopkeeper/ + build:205 in bundle. Previous: // 2026-09-14 BUILD 204 (founder: "The idea is to get more material into the initial viewport without simultaneously over-suppressing the contacts card display space. What you have done has cost us the view of the first pill under 'Ma's doctor'"): THE AIR RECLAIMED, NOT THE DECK. The shell stays at 70vh (the deck keeps its space); the walk slide's own dead air yields instead: (1) the .sw-lower decorative spacer above the slide clamp(20px,9vh,88px) -> clamp(12px,6vh,44px) - up to 44px of pure air returned on tall screens; (2) the Who card's padding 22/18/20 -> 16/18/14 and its gap 10 -> 8; (3) rhythm margins tightened (sw-garden and sw-selfrow bottom 14 -> 10). Net: the first pill under the typed line ("A decision I keep not making") is back inside the viewport, the Who card stays the first impression, the deck peek below the shell is untouched. Proofs: yarn build typecheck clean; build:prod base href /loopkeeper/ + build:204 in bundle. Previous: // 2026-09-14 BUILD 203 (founder: "Between the Inbox card and the flip button above it, we have some space to pull up further, perhaps 10px, or maybe 7px. Then also shorten the Inbox from 75 to 70vh we see"): THE FLIP GAP TRIMMED + THE SHELL SHORTENED. The flip button's bottom pad (the gap between it and the inbox main card below) 10px -> 2px - the card below rises ~8px, inside the founder's 7-10px band; the shell's resting height 75vh -> 70vh (expanded holds 85vh) - the deck's sneak peek below the shell grows accordingly. Proofs: yarn build typecheck clean; build:prod base href /loopkeeper/ + build:203 in bundle. Previous: // 2026-09-14 BUILD 202 (founder correction to 201: "the more natural flow would not include those pillled names right away after the card and 'not this one'... taking the 'From your loops' and its two accompanying pills section back down below 'Ma's doctor...' and its own accompanying pills - 'A decision I keep not making' and 'Somewhere I must show up'. They should be immediately after 'Not this one'"): THE WALK SLIDE-1 FLOW, CORRECTED. Order now: the Who card -> "Not this one" -> the TYPED LINE ("Ma's doctor, the school, the insurance...") -> the TWO SELF PILLS ("A decision I keep not making" / "Somewhere I must show up") -> the "From your loops" section (its label back in header role + the user's own loop-history pills) stepped down BELOW, with breathing room (sw-garden-lower). The typed line now always shows (it no longer depended on the garden *ngIf). The 75vh inbox shell from 201 is unchanged. Proofs: yarn build typecheck clean; build:prod base href /loopkeeper/ + build:202 in bundle. Previous: // 2026-09-14 BUILD 201 (founder: "Extend the home page inbox length to 85% ... so we do not have items hiding, and lose impact of first impressions ... but perhaps even just 75% could do but only if we re-prioritize by taking the From-your-loops section back down below Ma's doctor... and its two accompanying pills that should come immediately after Not this one"): THE INBOX BREATHES + THE GARDEN REORDERED. (1) The inbox shell 62vh -> 75vh resting (expanded 78vh -> 85vh): items stop hiding, first impressions land, the LoopKeeper icon panel rides into footnote territory, and the first-timer's demo-profile deck peeks below the shell. (2) The walk's slide-1 garden reordered: the pills (Ma's doctor, the school...) and the typed line now come IMMEDIATELY after "Not this one"; the "From your loops" label steps DOWN beneath them as a quiet caption (margin-top, not margin-bottom) - the deed leads, the explanation follows. Proofs: yarn build typecheck clean; build:prod base href /loopkeeper/ + build:201 in bundle. Previous: // 2026-09-14 BUILD 200 (founder: item-by-item dismissal of the dock "is a notional form of false imprisonment - more polite to allow blanket manual dismissal or snooze"; + "we are deliberately delaying billing enforcement until statistics tell us app is sticky, gathered from first adopters"): THE BLANKET CONTROLS + THE POLICY LEDGER. The dock header now carries SNOOZE (time icon) and DISMISS-ALL (close-circle icon): dismiss-all clears the whole pile in one tap (service.clear); snooze lifts the ENTIRE pile for 30 minutes, 1 hour, or until the next 9AM (the morning-digest hour) - sticky and timed items return INTACT when the window ends (timers re-arm), alerts arriving DURING the window join the pile silently (nothing lost, nothing nags), and the snooze survives a reload (persisted pile + until in lk_notif_snooze; a window that ended while closed hands the pile back at boot). The controls are taps, not drag handles (onDragStart excludes .notify-hbtn/.notify-snooze). AGENTS.md now records the BILLING ENFORCEMENT POLICY: no enforcement of any kind until the portal statistics prove stickiness; the trial stays re-fillable at the user's instance; prices advertised, never enforced. Proofs: yarn build typecheck clean; build:prod base href /loopkeeper/ + build:200 in bundle. Previous: // 2026-09-14 BUILD 199 (founder: "All three, but the first 7 days exhaustion should be re-fillable at user's instance" - the LoopKeeper adaptation of the Pocket FM playbook, the full mapped list now recorded in AGENTS.md): (1) THE MORNING DIGEST - the loop-wake service replaces per-loop pings with ONE 9AM notification/day listing the waiting loops by handle ("3 loops waiting: Ada, the venue, the insurance"); every loop mutation resyncs the single digest (resyncDigest); native = the OS holds the alarm through Doze, PWA = in-page timer + once-per-day catch-up (persisted day key lk_wake_digest_day); sticky dock. (2) THE ACHIEVEMENT SHARE - closing loops is the achievement: sticky dock nudge at week-milestones (1/3/5/10/20/30 closed loops, once per level via lk_achievement_shown), tap opens the share sheet. (3) THE TRIAL STITCH - the forgotten policy is FOUND and restored as a user-facing door: /trial/reopen (reopenable on demand, counts trialReopens) existed all along but the 2026-08-26 pre-release auto-renew (PRE_RELEASE_RENEWAL) silently refilled behind the user's back; that flag is now FALSE - an expired trial MEETS the user with an alert (once per 48h): Re-fill my 7 days (reopenTrial) / Invite a friend (share sheet) / Later. Proofs: yarn build typecheck clean; build:prod base href /loopkeeper/ + build:199 in bundle. Previous: // 2026-09-14 BUILD 198 (founder: the algo alerts "almost always are waiting for me in Settings, and if everywhere in app, they are very fragile, disappearing at first touch, and therefore of little or no utility"): THE ALERTS STAND UP. Three real defects, three fixes: (1) STICKY - reminder nudges, loop-wake pings and catch-up nudges were auto-dismissed at 5s/8s (gone before the user looked up); algo alerts are now duration:0 - they wait until the user taps them (escalate) or explicitly dismisses with the X; the transient confirmations keep their short timers. (2) THE CATCH-UP - a reminder that came due while the app was CLOSED used to vanish silently (the boot scheduler only armed FUTURE timeouts); catchUpMissedAlerts now fires anything overdue within 24h once on boot, kept exactly-once by a persisted fired-ledger (lk_alert_fired, pruned past 24h) - the alert waits for the user but never nags twice. (3) THE CORNER - the dock's default corner sat UNDER the home search FAB, so alerts were only fully visible in Settings (no fab there); the default now clears the fab zone and the position key is bumped (v2) so existing installs re-land on the cleared corner once (dragged corners still persist). No user-facing flow changed - the alerts simply stopped being fragile. Proofs: yarn build typecheck clean; build:prod base href /loopkeeper/ + build:198 in bundle. Previous: // 2026-09-14 BUILD 197 (founder: the search fab "should equally have two columns - so it can also search the device contacts list, not just LoopKeeper's, much like the add Contact applies to both repositories. Be mindful of any qualifications in the add format, such as permissions"): THE TWO-REPOSITORY SEARCH. The search fab's sheet now carries the SAME two tabs as the add sheet, with the SAME qualifications: deviceSearch mode shows the people/phone segment, the phone pane speaks SEARCH (the phone's own picker opens - its search bar searches the device list; a web app cannot query device contacts directly), the picker door only where the Contact Picker API exists (Android Chromium), and on iPhone the honest Apple-wall ladder (.vcf import + type one in). home.openSearchModal answers doors on onWillDismiss (warm activation - the b175/177 lesson) and row taps on onDidDismiss (never present a card into a dying overlay - b177). The picked device person is MATCHED against LoopKeeper (phone digits last-9, then display name): found -> their card; not found -> brought in from the phone (the AI device path's shape: mapPickedContact + icon blob + trackListStartedOnce) and their new card opens. Every device search is tracked as send_exit { channel: device-contacts, surface: search } - the signal-detector doctrine. The add sheet, walk picker and Loops chooser are untouched (their props do not set deviceSearch). No user-facing flow changed except the search sheet's new second tab. Proofs: yarn build typecheck clean; build:prod base href /loopkeeper/ + build:197 in bundle. Previous: // 2026-09-14 BUILD 196 (founder: the Yardstick's static graph must "catch and reflect actual stats", dynamic + pinch + expandable to the Command Center, like Zyppar's CommandCenter chart): THE LIVE YARDSTICK. New shared component components/yardstick-chart/ (declared in rolodex.module) - a dependency-free SVG line/area chart ported from the Zyppar CommandCenter retention-chart pattern: fixed 720x220 viewBox, x by day-index, y scaled to a nice max grid, and the 2026-09-11 zoomscroll fix verbatim in spirit - pinch (with the point under the fingers anchored) + +- buttons (center anchored) + a latest-jump button, the SVG scaling to zoom% width inside a touch-action pan-x pan-y wrapper. The PORTAL's Yardstick section now LEADS with the live 14-day series (segment toggle: Opens = app_launch/day, Arrivals = deviceGrowth/day, Loops closed = loop_closed/day, colored per series) + a Command Center expand button; the hand-drawn concept curve stays below, labeled as the shape we drive toward. The COMMAND CENTER's Trends section hosts the same chart full-size (Opens). Server build 76 widened topTimezones to a top 12 (the Africa/Nairobi naming semantics documented: a single-timezone country reports ONE zone named for its capital - Nairobi 126 IS the Kenyan fleet). No user-facing app flow changed - the investor story gained eyes. Proofs: yarn build typecheck clean; build:prod base href /loopkeeper/ + build:196 in bundle. Previous: // 2026-09-14 BUILD 195 (founder: the LOCAL GREETING + the global update strip): (1) THE WELCOME MEETS THE WORLD - the tour's first kicker renders in one of OUR 39 translation languages picked by the DEVICE CLOCK's timezone (no IP, no geolocation API - the tz city maps to a locale, that locale's own i18n file supplies its translated welcome.intro.kicker, fetched once at welcome-open, rendered raw via kickerRaw; 36 locale files swept with natural greetings - fr Bienvenue!, de Herzlich willkommen!, ar Ahlan wa sahlan!, yo Eku abo!, zh Huanying!... - en + sw keep Karibu sana! as the brand home; fallbacks: device language -> current behaviour; the replay keeps its own kicker). (2) THE HOME UPDATE BANNER - a slim global strip (pulse dot + "Updates available - tap to apply" + X) shown whenever /updates/check reports a different server version; tap = the full Zyppar apply (cache clear + SW unregister + hard reload); X dismisses THAT version only - a new deploy re-shows it; checked on home init + every 30 minutes (UpdatesService.refreshBanner/dismissBanner/applyBanner). Server build 75 keeps the directive fresh (the Assistant may open a new user's chat with the same local warmth). Proofs: yarn build typecheck clean; build:prod base href /loopkeeper/ + build:195 in bundle. Previous: // 2026-09-14 BUILD 194 (founder: "the very windows we presented to users to exit to third-party apps is a veritable signal detector stage - catch every copy, and even assuming they do not continue beyond to actually SMS/WhatsApp, that event by itself, and the ends of our user-AI Assistant chat, are reducible to measurable logs"): SEND_EXIT + THE CHANNEL FUNNEL. (1) send_exit { channel, surface } fires AT THE TAP on every exit door: the Confidante composer's five (sms/email/whatsapp/in-app/copy - previously ZERO analytics on the entire AI->draft->send funnel), the card's message menu (whatsapp/sms/email/copy), the home chat's COPY (copyRolodexAi - an end of the user-AI conversation), the inbox's intro-note + voice-outline copies. Categorical channel + surface only, never the draft text. (2) message_sent now carries props.channel - the fire-and-close deed (walk/shelf/card/agent all funnel through LoopsService.markSent, which had the channel parameter but threw it away). (3) server build 74: channelExits in the summary (door taps vs sends per channel, 30d, organic) rendered in the Command Center's Trends section - exits > sends per channel is the drop-off to read, cross-matchable with the dailyEvents volume matrix. No user-facing app flow changed - taps only gained a voice. Proofs: yarn build typecheck clean; build:prod base href /loopkeeper/ + build:194 in bundle. Previous: // 2026-09-14 BUILD 193 (founder: "no clue how they are discovering the app - no indicators of sharing by earliest users"): THE DISCOVERY LENS. (1) landing_source - a once-per-device analytics event fired from AnalyticsService.init(): ref = document.referrer reduced to host+path with the query STRIPPED (no tokens can leak - the crash reporter's rule; 'direct' when none, which WhatsApp webviews and forwarded plain links produce), src = a short ?src= tag the founder may pin when posting links (sanitized [a-z0-9_-]{1,24}); storage flag loopkeeper_landing_sent makes it exactly-once per device, consent already gates the service; (2) server build 73 serves deviceGrowth (devices per first-sync day, trialStartedAt birth dates) + landingSources (top-8 referrers, 30d), both rendered in the Command Center's new section 06 Discovery (arrival bars + landing table, amber accent). In-app share events stay as they are; the invisible channels (WhatsApp forwards of the plain URL, typed URLs) get a light: 'direct' dominance CONFIRMS word-of-mouth rather than leaving it a guess; pinned ?src= tags turn future posts into measurable channels. No user-facing app flow changed - one anonymous, once-ever, token-free telemetry event. Proofs: yarn build typecheck clean; build:prod base href /loopkeeper/ + build:193 in bundle. Previous: // 2026-09-14 BUILD 192 (founder: "data to sift for trends - what to refine, double down on, drop, re-interpret"): THE TRENDS MATRIX in the Command Center - new section 02 rendering server build 71's dailyEvents: a 14-day x event grid, organic devices, all events, rows sorted by 14-day weight, S-total column, today highlighted, zeros as dots; the SLOPE is the story (rising = double down, flat-at-zero = drop or re-interprete), with the outage hole (Sep 8-12) captioned honestly. Timeline/Rooms/Translations renumber 03/04/05. No user-facing app flow changed - founder telemetry only. Proofs: yarn build typecheck clean; build:prod base href /loopkeeper/ + build:192 in bundle. Previous: // 2026-09-14 BUILD 191 (founder: the SESSIONAL APERTURE + the deeper Region cut): (1) THE APERTURE OPENS THE COMMAND CENTER AS A VIEW - the founder's aperture-outline icon in RolodexPage is now gated by InvestorGateService (in-memory only): it stays hidden until the Investor portal password (NorthStar) succeeds THIS session, and a fresh app start re-locks it; the tap opens RolodexView.CommandCenter - the same view-swap contract as Settings (settingsWillOpen/settingsClosed so the remembered Inbox steps aside), with the SAME external Home icon (home-outline, showRegularView) plus the console's own internal Close; the CommandCenterComponent self-fetches /investor/summary when opened without [stats] (safeFetch, quiet, refresh-on-open) and its shell now works in both hosts (portal modal + page view); (2) PLACE TABLE - the portal's Presence section gains the Place (device timezone) table rendering locales.topTimezones, which the server has computed all along (full IANA zone, city-level, e.g. Africa/Nairobi: 61 devices) - answering "Region | Devices | LAN seems so shallow" without ANY new collection: no IP, no GPS, the place is the device clock's own zone; caption explains the granularity honestly. Proofs: yarn build typecheck clean; build:prod base href /loopkeeper/ + build:191 in bundle. Previous: // 2026-09-14 BUILD 190 (founder: global app error analytics + a dedicated Command Center): (1) APP_ERROR IN THE PIPELINE - the global error hook (window.onerror + unhandledrejection in CrashReporterService) now ALSO fires the app_error analytics event, { type, page } categorical only, with a 5s/type+page flood valve, so every error rides the batched, idempotent, hourly-capped ingest (server build 70 counts appErrors7d/30d + a top-8 type breakdown) - more scalable than the JSONL crash ledger, which stays as the detailed stream with messages + stacks; (2) THE COMMAND CENTER - the Investors portal's operations console (Reliability with the new app_error rows, Timeline, Rooms, Translations) moved into its own component (command-center/) opened by a button + index chip through an in-template ion-modal (the build-178 pattern); the portal keeps the growth story (Delta, Live, Presence, Retention, Activation, Events) and renumbers; dead helpers (hourLabel/keysCount/formatTime/openTranslationReview/barWidth) moved with their sections. No user-facing app flow changed - founder-facing telemetry only. Proofs: yarn build typecheck clean; build:prod base href /loopkeeper/ + build:190 in bundle; command-center markup in the portal chunk. Previous: // 2026-09-13 BUILD 189 (founder: the post-sync journey needed a RETURN DRIVER, a calibrated AI pipeline, and an error ledger): (1) LOOP WAKE NOTIFICATIONS - LoopWakeService (the Zyppar Soliloquy pattern: OS-held LocalNotifications.schedule with a future Date, deterministic genId ids, channel loop-wake, resyncAll rebuilt from the loop ledger on load) gives every snoozed loop a voice: waitUntil() schedules the 9AM wake ping (the OS holds it - survives app kill, tap lands on the home deck; the handle in the text is device-local and never leaves the phone), bringBack/markSent/closeFully/dropWithDignity/remove cancel it, all() resyncs it; on PWA the wake rides the in-app dock with a catch-up nudge for wakes that passed while closed (the browser cannot schedule future notifications - the same honest trade Soliloquy makes). (2) AI PIPELINE CALIBRATION - the home chat's per-send aiStatus pre-flight is cached per session (chatEngine(); the backend ladder deepseek->glm->grok handles fallbacks anyway) and the chat fetch gets a 30s AbortSignal - a hanging upstream no longer holds the typing dots forever, it lands as ai_chat_failed stage 'timeout' and the input unlocks; compose/refine get 20s timeouts and now TRACK ai_draft_failed (kind compose/refine; stage httpNNN/empty/network/timeout) instead of silently falling back to the on-device engine while the app "gets dumber" invisibly. (3) THE PORTAL ANSWERS "WILL WE KNOW?" - new section 10 Reliability in the investors portal: Confidante sends vs failures + failure rate + the stage where chats died, draft-engine failures, crashes 7d/30d + top type x page rows (server build 69 reads data/crashes.jsonl - the stream the app's CrashReporterService has kept since 2026-08-27 that only an SSH tail could see), and the server's since-boot analyticsIngestFailures counter (the metric that would have exposed the two-week ingest blindness in hours); the redesign panel renumbers 10->11. Server build 69 + its chat-directive (the Assistant learns the wake ping) keep pace. No message text, no names - event names and categorical stages only, per the privacy rules. Proofs: yarn build typecheck clean; build:prod base href /loopkeeper/ + build:189 in bundle. Previous: // 2026-09-13 BUILD 188 (founder: rows that can never move do not fit the bill - fixing the four flagged items): (1) THE ACTIVATION TABLE TELLS THE TRUTH - the investors portal rows Added a card (card_added, fired by NO code path anywhere) and Created a follow-up (followup_created, dead with the b183 engine retirement) are gone; the rows now read Captured a loop (loop_captured - every Send Walk / capture-box capture) and Talked to the Assistant (confidante_message - the home chat tab since 187 + the modal); server build 68 counts the same two events, so the numbers can actually move. (2) No user-facing flow changed - the portal caption paragraph and rows only. Server side: rolodex-server build 68 also killed the version.txt drift in deploy.sh (argless deploys no longer increment; repo value is truth) and verified the GLM/OpenRouter path is code-complete (OPENROUTER_API_KEY flips glmConfigured, no code change). Previous: // 2026-09-13 BUILD 187 (founder: the engagement investigation found the investor portal blind since Aug 30 - server build 47's sha1 _id cast-failed every analytics insert; server 67 repairs it, and these app-side changes make the resuming stream SEE the primary surfaces): (1) THE HOME AI CHAT REPORTS - sendRolodexAi() now fires confidante_message with surface:home on every send (the modal Confidante keeps its own; the tester practice ledger finally counts the main chat) and ai_chat_failed with a categorical stage (httpNNN / empty / network) when the backend dies - event names + stage ONLY, never message text, per the privacy rules. (2) TESTER VISIT BEACON - tester.html pings POST /tester/visit the moment a valid deeplink renders (numeric code only, fire-and-forget); server build 67 stores the TesterVisit ledger, and the roster/dashboard gain the visited rung (visitedAt/visitCount/lastVisitAt, summary.visited, the eye tag on awaiting/invited rows) so "link never opened" finally differs from "opened, not accepted". No user-facing flow changed - measurement only. Previous: // 2026-09-09 BUILD 186 (founder: defensive assurances scream DO NOT TRUST US - speak positive or not at all): NO-APOLOGY SWEEP. (1) THE WELCOME PRIVACY SLIDE IS GONE - the BUILD 169 pledge slide (No account. The book stays here. / No email. No number. We cannot message your people. + the no-email/no-number/we-don.t-send fact strip) deleted from the steps array, the stage markup, and the SCSS; the flow runs 10 slides, dots/aria follow steps.length. (2) EVERYWHERE ELSE REWORDED POSITIVE on the founder formula (email ==== number ==== sending ============ Your data is yours): privacy.headline There is no login -> Your data is yours; pledgeTitle What we never do -> How your data works; pledgeBody/subhead/shareBody/lockBody reworded ownership-first; add.msg/prefaceBody/stayToast, walk.weDontSend/mineSub, share.voiceD, invite.hookCopy, storage.deviceNote, billing.privacy, ai.intro2, welcome confidante/outro/taste/tasteHint, aboutTxt privacy1/2 - all now state what IS true (your people live on your phone; you press Send), no we-never, no we-cannot. add.sysNote KEPT but softened (ends at on this phone - the phone-dialog defuser stays, the apology tail goes). (3) DEAD KEYS DELETED from all 39 locales: welcome.privacy.kicker/privacyTitle/privacyBody, privacySlideTitle/Copy/Emphasis, st.factEmail/factNumber/factSend (351 deletions) + 24 rewrites x 39 locales (936 strings), 14 majors hand-translated, pt-PT normalized to tu-form. (4) Chat banner (hardcoded EN): dropped (they stay on it) and We don.t send the message for you. (5) Server directive mirrors the positive turn (build 66). Proofs: yarn build typecheck clean; build:prod base href /loopkeeper/ + build:186 in bundle. Previous: // 2026-09-08 BUILD 185 (phase 4a, founder: the garden pill deep-links): GARDEN WALK DOOR (phase 4a, founder: the garden pill deep-links): GARDEN WALK DOOR - the inbox Garden rows grow a teal Walk button: tap it and the surface flips to the walk and armHandle() arms it at that subject (an open loop for that subject resumes straight on the words via pickLoop; a fresh one starts at the thing slide) - no re-typing the name, the garden IS the arm. walkHandle() in loop-inbox (setLoopsSurface(walk) + one-tick setTimeout + walkRef.armHandle); garden_pill_walk analytics event (numeric only); i18n garden.sub rewritten (Walk one, rename one - or rename it onto another to merge them) + new garden.walkBtn across 39 locales, 14 majors hand-translated. Copy mirrors: Assistant banner (walked straight from the inbox), server directive build 65. Proofs: yarn build typecheck clean; build:prod base href /loopkeeper/ + build:185 in bundle. Previous: // 2026-09-08 BUILD 184 (founder: measure the new doors; nothing user-visible): ANALYTICS ONLY (founder: measure the new doors; nothing user-visible): ANALYTICS ONLY - self_loop_started (the b183 slide-1 no-arming taps), garden_renamed {loopsMoved, merged} (merged=1 when the rename target already existed as a handle — the truth about merges, captured BEFORE the move). Numeric props only per the privacy rule; no names, no summaries. www rebuilt so the events ship to testers. Previous: // 2026-09-08 BUILD 183 (phase 3: the garden has manners - self-loops need NO arming at all; the garden is tended): SELF LOOPS + GARDEN TENDING (phase 3: the garden has manners - self-loops need NO arming at all; the garden is tended): SELF LOOPS + GARDEN TENDING. (1) SLIDE-1 SELF LOOPS - two quiet gray taps under the garden: "A decision I keep not making" and "Somewhere I must show up" create PURE self-loops (person '', b181's subject-less loop) with no card, no handle, no sentence - walk.send w/e straight to the words; the shyest entry in the whole walk. (2) GARDEN TENDING - loops.renameHandle(from,to): case-folded match, new spelling wins, renames across every loop that rides the handle; renaming onto an existing handle MERGES the two (all loops land on the surviving name); returns the moved count so the UI speaks honestly. (3) INBOX GARDEN SECTION - collapsible pills with open-loop counts, inline rename row (input + Save/Cancel), toast "{{n}} loop(s) moved: from -> to"; garden refills on every refresh. (4) WALK CHROME - .sw-selfrow/.sw-self quiet gray pills; i18n 6 new keys (garden.title/sub/renameBtn/save/cancel/renamedToast) across 39 locales, 14 majors hand-translated. (5) Copy mirrors: Assistant banner (self-loops + garden merging), server directive build 64 (self taps + tending). Previous: // 2026-09-08 BUILD 182 (the walk speaks five intentions with a card OR a handle; empty deck still walks): THE GARDEN PATH on the secretary spread. (1) SLIDE 1 SECOND DOOR - handleGarden() (own loop-history subjects, case-folded, open-count badges, cap 8), garden pills + typed line under the deck card (deck stays first-class); armHandle/commitHandle set armedHandle; chipTap accepts contact OR handle (handle = the subject, no relation); commitWhat: handle-wins rule; reset paths clear armedHandle. (2) i18n 3 new keys (walk.garden/handlePh/nameGo) x 39 locales, 14 majors. (3) Copy mirrors + directive build 63. Previous: // 2026-09-08 BUILD 181 (tester verdict: KEEP CONTACTS - the b180 deck returns; founder: widen the scope to tasks deferred, delayed or stacked up - the remit of a skilled secretary): THE SECRETARY SPREAD (founder: port the contactless line's Garden without touching the deck's first-class status): THE GARDEN PATH - the walk's slide 1 grows a second door. (1) THE GARDEN - handle pills grown ONLY from the user's own loop history (loops.handleGarden(): distinct subjects, open-loop count badge, recency tiebreak, cap 8; demo placeholder names Someone/Unnamed excluded): a person, a place, or a thing - "Ma's doctor", "the school", "my decision". (2) THE TYPED LINE - one quiet input beneath the deck lead names a fresh subject (armHandle: an open loop for that subject lands straight on the words, otherwise the thing slide arms it); on an EMPTY deck the garden + line always show, so a user who never syncs contacts can walk too. (3) ARMED-HANDLE PATH - armedHandle joins armedContact as a first-class arming: chips guard on either, the thing slide's back row speaks both (armedName), pickLoop arms a card-less loop's subject as a handle, and the handle the user NAMED wins over whatever the sentence re-extracts (commitWhat b182 rule). A handle is a nickname only the user needs - it never leaves the phone. (4) SELF-INTENTS WALK - "A decision I keep not making" armed at the handle "my decision" works exactly like a reply owed to a deck card; no deck card is ever required. (5) Copy mirrors in the same change: rolodex-server directive build 63 (hybrid walk + situation mode collects the subject), Assistant banner garden clause. i18n 3 new keys (walk.garden, walk.handlePh, walk.nameGo) across 39 locales, 14 majors hand-translated. Previous: // 2026-09-08 BUILD 181 (tester verdict: KEEP CONTACTS (tester verdict: KEEP CONTACTS - the b180 deck returns; founder: widen the scope to tasks deferred, delayed or stacked up - the remit of a skilled secretary): THE SECRETARY SPREAD on the restored contacts base. (1) EIGHT TASK LOOP KINDS - decide, show-up, send, book, chase, renew, pay, someday - join the nine relational kinds; scores tuned (pay 88, chase 82, send 80, renew 78, book 74, decide 72, show-up 68, someday 12 so parked ideas never crowd Today's 3). A loop's subject can be a person OR a thing - create() preserves an empty person as a subject-less loop; drafts, voice outlines, receipts and toasts fall back to the summary line ('there' in words). (2) CAPTURE GRAMMAR - task verbs parse into the new kinds (book|schedule|reserve, pay|settle|deposit|top up|airtime|bill, send|submit|post|deliver|forward, renew|expiry|extend, chase|check on|still waiting for, decide|choose|pick between, show up|attend|be at, someday|one day|when I have time); deadline sniff grew: "by Friday", "before the 15th", tonight, this weekend, end of month. (3) THE STACK - the tray's third pile under the open piles: Waiting (snoozed loops with a wake date or condition - they used to vanish from every list) and Parked (someday ideas); collapsible, wake labels, Bring it back; loops.theStack() + bringBack(). (4) COPY IS A DOOR - subject-less loops default channel 'copy'; the shelf send() writes the draft to the clipboard and closes the loop without asking for a recipient. (5) THE WALK speaks five intentions: the three relational chips joined by A decision I keep not making and Somewhere I must show up. (6) Copy mirrors in the same change: rolodex-server chat-directive.js rewritten (deck restored + spread + Stack, GARDEN/RIP text retired), About portal tasksLine paragraph, Assistant situation banner line. i18n 12 keys (3 changed: capture.placeholder, capture.sample3, welcome.intro.copy; 9 new: walk.chipDecide, walk.chipShowUp, stack.title/sub/waiting/parked/back/backToast, aboutTxt.tasksLine) across 39 locales, 14 majors hand-translated. Previous: // 2026-09-01 BUILD 180 (founder: the Contacts picker claim that contacts "will be shared with zyppar.com" is false and contradicts our assurances): that dialog is the BROWSER's own - Chrome draws it, shows the site origin, and no app CSS or code can reword or restyle it (and its "Select contacts" list is the phone's dated type, not ours). What we control, we fixed: a new add.sysNote line rides the preface BEFORE the picker opens, quoting the phone's standard wording verbatim and defusing it - the people you pick go only into LoopKeeper on this phone, we never receive a copy (13 majors hand-translated). Every standalone zyppar.com in our copy is corrected to the full secure address: privacy.madeBy, welcome.outro.emphasis, storage.serverExplain, aboutTxt.privacy2, aboutTxt.version across all 39 locales; the install caption, the share-voice URL parameter, the help FAQ bookmark line; the investors peek endpoint gets its https scheme. The OG host field and the sysNote's verbatim quotation are the only bare forms left, both deliberate. Proofs: base href, build:180 in main bundle, sysNote in i18n. Previous: // 2026-09-01 BUILD 179 (founder: "Who's on your mind should present them the same agnostic interface shown by the add icon, all the way to the iPhone mitigations"): the walk's Not-this-one no longer opens a people-only chooser, and demo decks no longer cycle - ONE door for every deck. The walk emits whoRequest; home's channelWhoFromWalk opens the SAME add sheet as the add icon (two tabs, firm full-height anchor, device picker on Android, the honest iPhone ladder - one-at-a-time typing + the 3-step .vcf visual - where Apple walls the address book off). Every outcome arms the walk's Who card: row tap routes through nudgeArrived into armFromNudge (open loop lands on the words, bare contact arms at the thing) carrying the once-ever list marker; picker/.vcf/typed additions join the deck and the walk's own arrival-watcher fronts them as the Who (the build-159 mechanism). Retired with the split: the walk's people-only chooser, its demo cycling, hasRealPeople gatekeeper, chooseFromList, and the ModalController/SearchModal imports it alone needed; canSkip now means the door exists whenever a Who stands armed. No new i18n keys - the sheet speaks its own translated copy. Previous: // 2026-09-01 BUILD 178 (founder, three points on the tester page + the add sheet): (1) THE NUMBER, SHOWN - "just carries a private number" left room to guess; the privacy card on tester.html now shows the tester's own link number in the copy (codeShow span, filled from the URL): "This invite link just carries your tester link number - #NNNNNN. That's all I ever see." (2) IN-TEMPLATE ION-MODAL, THE ZYPPAR PATTERN - "I'll type one in" still only closed the sheet because a controller-presented form cannot reliably rise over a dying overlay; the create form is now DECLARED in home.page.html (ion-modal [isOpen]=manualAddOpen, cssClass contact-edit-fullscreen) and the sheet's manual role flips the flag - no ModalController timing to juggle; manualAddClosed normalizes + bumps the new person to the top of the deck, exactly like a device import, with the once-ever list marker and receipt. (3) IPHONE TAB LEADS WITH THE HONEST PATH - the phone tab on picker-less devices now recommends one person at a time, as needed, pending the iPhone app (add.iosOnestep), carries the typed door IN the pane body (footer keeps only the batch import), and shows a compact 3-step visual of the .vcf process (export from Google Contacts/iCloud - save to Files - tap the import door below). i18n 5 keys (iosOnestep, vcfHow, vcfStep1-3) across 39 locales, 13 majors hand-translated (615-key parity). Previous: // 2026-09-01 BUILD 177 (founder, five points on the add sheet + the tester page): (1) VCFILE HONESTY - the .vcf door is a FILE import, not a door into the Contacts app, on Android or iPhone alike; the phone pane now carries one quiet provenance line (add.vcfHint: it reads a contacts file you export first - Google Contacts, iCloud on the web, or your old phone) ABOVE the doors, never after them. (2) THE FIRM ANCHOR - the add sheet opened at breakpoint .95 with an autofocus search field, so the keyboard rose instantly and the ion-footer stood on the layout bottom behind it, out of view; the add sheet now opens FULL height (initialBreakpoint 1, the firmest anchor) and autofocus only arms on non-add sheets, so the doors stand on the true viewport bottom until the user taps the field. (3) TYPE ONE IN WAS A DEAD END - onWillDismiss resolved while the sheet was still animating away, and presenting the ContactCard create-form into a dying overlay swallowed it; the in-app doors (manual, row-tap) now wait for onDidDismiss (sheet fully gone) THEN present; the native OS doors (picker, .vcf file dialog) keep the warm onWillDismiss they need for user activation. (4+5) THE EXIT IS THE LAST ELEMENT - tester.html: the codeNote flourish after Accept the invitation is retired from markup AND script (the variable too - it would have thrown on the missing element); on the accepted state the fine print and share door stand BEFORE Open LoopKeeper, so every door out of a page ends the page. i18n add.vcfHint across 39 locales, 13 majors hand-translated (610-key parity). Previous: // 2026-09-01 BUILD 176 (founder: the testers pipeline broke a mid-sixties user at the share juncture): tester.html rebuilt around the hand-over. (1) THE SHARE DOOR - the ten-cohorts card ends hand the app to the friend you thought of first, and the page used to leave him there with no way to do it; now a Handing it to that friend card sits right under it with a Share LoopKeeper with that friend button (navigator.share, WhatsApp wa.me fallback), and the same quiet door rides the accepted state. The friend link is the PLAIN app URL on purpose - no tester code rides with a share, the friend opens it as a regular user, the seat never travels. (2) THE USHER MOVES ABOVE THE BUTTON - One tap to claim your seat. sits over Accept the invitation so the button is the deed, not a demand. (3) THE TECHNICALITY RETIRED - the old below-button line (Invite #480213 detected) read like a code to hand to someone else, the exact ambiguity that stalled the tester; below the button now only the reassurance: Nothing to type, nothing to pass on - the invitation rides in this link. Server 57: directive tells the assistant the plain-link answer. Previous: // 2026-09-01 BUILD 175 (founder x4): (1) SYNC/REACHABILITY QUESTION ANSWERED, no code change - the users/lookup API itself has NO restriction; a person only becomes findable in-app when THEY enabled backend sync (default OFF) AND saved their profile phone; copy recommendation to follow if founder wants. (2) THE DEAD PHONE DOOR FIXED - onDidDismiss resolved AFTER the closing animation and the Contact Picker API demands warm user activation; the sheet now answers onWillDismiss so the picker opens while the tap is still hot, and the phones own preface alert is skipped on this path (the phone tab already showed the copy; skipPreface). (3) BOTTOM BUTTONS INTO ION-FOOTER - every door of the add sheet rides a footer that is always in the viewport: people tab (type one in), phone tab Android (pick from phone + import .vcf), phone tab iPhone (import .vcf + type one in). (4) THE PARSER FROM WAY BACK - vCard import is here: src/util/vcard.ts (dependency-free; folded lines, Apple itemN.TEL groups, QUOTED-PRINTABLE names, N-order names, cell-first phones, ORG/CATEGORIES tags, size-capped base64 photos; esbuild-proven on 2.1/3.0 shapes), the sheet gains the Import a contacts file (.vcf) door (file read on-device, never uploaded; quiet failure line when a file holds nothing readable), home maps records to full cards (picker shape), trackListStartedOnce(vcf), importedToast with count. i18n add.vcfBtn/importedToast/vcfFail across 39 locales, 13 majors hand-translated (609-key parity). Previous: // 2026-09-01 BUILD 174 (founder: two tabs - device contacts AND LoopKeeper contacts): the roomy add sheet now opens with a quiet two-tab segment. Tab 1 (people): the searchable LoopKeeper deck with the type-one-in door at the foot. Tab 2 (phone): the DEVICE tab - on Android the honest preface (From your phone / they stay on this phone, we never message them) and the picker door; on iPhone the SAME tab shows but speaks Apple wall with an apology (add.iosTitle/iosBody: Apple does not let any web app read the phone contacts - we are sorry - nothing is wrong with your phone; two honest ways in: type people here seconds each, or wait for the App Store app) and the typed door leads. The dead picker button is gone everywhere; answers both the tester report and the founder ask to explain the iPhone way with apologies until the App Store build. i18n add.tabPeople/tabPhone/iosTitle/iosBody across 39 locales (13 majors hand-translated). Previous: // 2026-09-01 BUILD 173 (founder: illegible white text in the composers - URGENT): ROOT CAUSE FOUND AND KILLED - dark.system.css (Ionic system dark palette) painted Ionic ink WHITE whenever the phone OS was dark while the app theme stayed light, so the capture box placeholder (Tell the Assistant what to write...), the Assistant composer placeholder AND typed text, and the sheet search input rendered white-on-light. Fix: (1) dark.system.css import retired - the theme system is the app own: light by default, body.dark for dark, coherent either way; (2) belt-and-braces ink pins on the three inputs (loop-inbox capture textarea, chat-with-rolodex chat-input, search-modal search-input): --color, --placeholder-color, --placeholder-opacity:1, caret-color, local --ion-text-color fallback, all on the rolodex chain with solid hex fallbacks. Previous: // 2026-09-01 BUILD 172 (founder: apply the Loops contacts interface to the add icon): ONE ROOMY SHEET for reaching people everywhere. The cramped three-button add alert is retired - onCreateContact now opens SearchModalComponent (the same design the Loops Not-this-one chooser uses) with addDoors: the searchable list of people already here, tap a row opens their card, and the bring-them-in doors at the foot - Pick from my phone (rendered ONLY where the Contact Picker API exists: Android Chromium; iPhone Safari never offers it, so the dead door iPhone users kept tapping is gone) and Ill type one in (always). The deck add icon AND the walk MINE door route through this sheet; dismiss roles phone/manual/pick drive the existing picker, manual-entry and card-open paths unchanged. Answers the tester report she cannot sync contacts on iPhone: iOS offers no web contact access at all (Chromium-Android only), so the honest door there is typing one in - now prominent, translated, and no broken button. Rides with the founders uncommitted 169-171 work (picker preface, card #01 recapture bar, welcome/help/privacy/share-app polish, i18n). Previous: // 2026-09-01 BUILD 171 Card #01 recapture bar (matches every other Welcome slide) + full i18n RESET of every 169/170 key (EN on 39, majors hand-overlaid). Previous: // 2026-09-01 BUILD 170 Welcome last-four recapture lines (confidante/storage/outro/taste) so a prevaricator can skim; privacy emphasis is now the three facts. Previous: // 2026-09-01 BUILD 169 TRUST AT THE ASK (founder: startup skepticism vs giants): no loopkeeper.com yet - copy says LoopKeeper is made by Zyppar, bookmark zyppar.com/loopkeeper/. Welcome: privacy is a real slide after Zeigarnik (no account, book stays here, three facts), repeating footer slogan REMOVED; storage visual is Device-home with Cloud/Server dim and optional; Assistant copy says draft on phone, polish is the hop; pricing dropped from first-run. Contacts picker: in-app preface before the OS dialog, receipt "{{n}} on this phone. None sent anywhere." Walk: first-real-card whisper "on this phone"; polish "rewriting — the card stays here"; send "we don't send it." Privacy center leads with no login; analytics i18n'd honestly. About/Help/Billing/Invite/Share voice D match. FAQ no longer says "sign in again." Chat-directive privacy posture. Previous: // 2026-09-01 BUILD 168 (founder: "once user has real contacts, Demo should stop factoring into operational features"): THE DEMO PURGE, third and final strike. Full audit found NINE leak surfaces - the engines were being fed the MIXED deck (real + demo) whenever Demo was on, minting operational artifacts that outlived the toggle: (1) automateContactSetup(this.contacts) in loadContacts ran the follow-up engine + birthdays on the mixed deck EVERY boot; (2) runAutomation() fed followUpEngine.run / suggestReachOut / processUpcomingBirthdays the mixed deck at boot, cloud sync, import and toggle; (3) the empty-deck early-return skipped the sweep so Demo-off + zero-real devices kept their minted events forever; (4) birthday bday_* events + the handled-ledger were never swept by anything; (5) refreshRelationshipScores/getDormantCount scored the mixed deck; (6) rolodexAiNudge counted demo cards; (7) the reminders pane (rolodex opener AND the inbox reminders tab) showed demo rows from the mixed deck; (8) the walk picks surfaced loops minted from demo cards once real people existed. THE FIX - one rule stated once: demo factors operationally ONLY while it is the whole show (Demo ON + zero real contacts). operationContacts() is the deck engines see; purgeDemoArtifacts() deletes every calendar event owned by a demo contactId + prunes the birthday handled-ledger (idempotent; runs at boot when demo is not the show, on both toggle directions, and when the operational deck is empty); reminders-modal filters demo rows internally the moment one real contact arrives; walk rebuildWho drops demo-linked picks when real people exist. Display keeps the tour; operations never see demo again. Previous: // 2026-08-31 BUILD 167 (founder): THE INVITE COMPOSER - the recruiting explainer (D:/TODOs/tester recruiting system.txt, build 119 era) finally became a feature. tester-dashboard gains: (1) the composer - paste a tester code + optional name, pick Variation A (warm, personal) or B (short, group chat) verbatim from the explainer, prepend one sector hook (freelancer/recruiter/family/sales), live preview, one tap copies the finished message with the tester.html?t=CODE link riding inside (link-only copy kept); (2) the INVITE STATE TRACKER (founder directive) - copying marks the seat invited server-side (POST /api/loopkeeper/tester/invited, TESTER_ADMIN_KEY-gated, idempotent first-invitedAt wins), the roster ladder gains the amber INVITED rung between awaiting and accepted with invitedAt per slot, and the summary chips gain an invited count. Covers the founder build 166 invite-line tool (reverted then reinstated as this composer); counters were not bumped on 166, so this jump is 165 to 167. Previous: 2026-08-31 BUILD 165 (founder, blessed from dev): SLIDE 4 FINALLY READS - root cause was color, not data: the words card body hardcoded near-white #fff6ea, invisible on the founder light theme (the same bug hid the old call-beat CONTENT behind its amber labels, which read as context-free Say hi/The thing/Land it soft). Body now inherits the theme color like the slide-3 draft block. Slide 4 also: the accepted words show ONCE at the top (Here are words card, 3+ lines visible, internal scroll, call door no longer repeats the script - plain amber door now), the 162 lower-spacer skips slide 4 so it starts higher, and on a demo card the MINE door auto-scrolls into view (smooth, centered) as the slide opens. Previous: 2026-08-31 BUILD 164 (founder, blessed from dev): NOT THIS ONE OPENS THE CONTACTS MODAL - the cramped inline chooser (template+logic+styles) is retired; with real people aboard the walk now opens SearchModalComponent in a new pick mode (roomy 0.95 bottom sheet, translated title Who is it?/placeholder Find someone/empty line from existing walk keys, on-screen person excluded via excludeId, tap returns the contact). Open loop lands on the words, bare contact arms at the thing; pure-demo decks still cycle. Previous: 2026-08-31 BUILD 163 (founder, blessed from dev): the flip LEADS - the walk flip mark moved from the foot to above the slide (one mark, both views open with it at the top); the inbox shell is a notch longer, 57.5 to 62vh resting and 75 to 78vh expanded (kept under the 80vh ceiling that pushed the deck View toolbar out, BUILD 145). Previous: 2026-08-31 BUILD 162 (founder: default is the newer + slide lower): surface persistence REMOVED - every launch opens the Send Walk (the 157 last-used memory kept booting old-view devices onto the packed shelf); the flip lasts the session only. The slide no longer glues to the pane top: a shrinkable spacer (.sw-lower, clamp(20px,9vh,88px)) drops it toward the lower half and yields its height back on short screens. Previous: 2026-08-31 BUILD 161 (founder: fab restore + flip icon): the Search fab now comes back after every Loops process - the inbox-mirror resets at every inbox close/reopen (R-icon toggle, Settings swap, close, nudge travel), because a destroyed inbox can no longer emit its retract. The two surface doors (walk foot + shelf) became a small round flip icon (arrow-redo, the card-flip mark): the shelf one sits directly under the three sample pills, never below the fold. Previous: 2026-08-31 BUILD 160 (founder: kill demo prompts + no serial skip): loadContacts no longer seeds mockContacts unconditionally on an empty persisted deck - Demo OFF now means an EMPTY deck (no prompts, no engine-minted sample check-ins, ever). Walk: with real people aboard, Not this one opens the chooser (filter + tap, walk.find) instead of paging the deck one card at a time; pure-demo decks still cycle. Previous: THE MINE DOOR (founder): a first-timer walks a demo name all the way to the Send stage, where a big rounded MINE tile waits (styled like the Who card, + glyph, translated sub-line). Tap = elegant reset to slide 1, then their own device Contact Picker (add sheet where the picker does not exist); the pick lands as the Who card, past the fresh-pick sort trap. Add sheet + toasts translated (loopkeeper.add.*). device_list_started fires once ever per device at the first real card (walk confirm / picker / manual / invite) -> server activation row "Started their list" + Investors portal row. Empty deck (Demo OFF) gets its own teal door (whoAdd) to the same picker - no dead-end text. i18n walk.mine + add.* + walk.whoAdd across 39 locales.
};
