export const environment = {
  production: true,
  rolodexApiBase: 'https://zyppar.com/api/openloop', // the fresh rolodex sync backend (same droplet)
  version: '0.3.1', // LoopKeeper app version — compared against /api/updates/check
  build: 82, // 2026-08-24 Settings quicknav precision: scroll-margin on anchors so pills land on the item, not under the sticky bar
};
