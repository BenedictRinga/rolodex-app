export const environment = {
  production: true,
  // 2026-08-26 CANONICAL API PATH: /api/loopkeeper (rolodex-server keeps
  // /api/openloop + /api/rolodex as backward-compatible aliases).
  rolodexApiBase: 'https://zyppar.com/api/loopkeeper',
  version: '0.3.1', // LoopKeeper app version — compared against /api/updates/check
  build: 138, // 2026-08-28 SETTINGS FLOAT: Settings label + Home icon float with the quicknav as one sticky unit docked for the whole settings scroll - user deep in Settings is never lost and always one tap from home; scrollTo + scroll-margin offset the taller float
};
