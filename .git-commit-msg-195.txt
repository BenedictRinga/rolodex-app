build 195: THE LOCAL GREETING + THE GLOBAL UPDATE STRIP (founder's two asks). (1) THE WELCOME MEETS THE WORLD - the tour's first kicker now renders in one of OUR 39 translation languages, picked by the DEVICE CLOCK's timezone (no IP, no geolocation API - the IANA zone's city maps to a locale via a curated city table: Nairobi/Dar/Kampala -> sw, Lagos -> yo, Kano -> ha, Paris/Abidjan -> fr, Cairo/Riyadh -> ar, Mumbai -> hi, Beijing -> zh-cmn-Hans, Tokyo -> ja, Sao Paulo -> pt-br...); that locale's OWN i18n file supplies its translated welcome.intro.kicker (fetched once at welcome-open, ~8s slide tolerance; rendered raw via the new kickerRaw field - the template prefers it over the translate-piped key; fallbacks: device language -> current behaviour; the replay keeps its own kicker). 36 locale files swept with natural greetings (fr Bienvenue!, de Herzlich willkommen!, ar Ahlan wa sahlan!, yo Eku abo!, ha Sannu da zuwa!, ig Nnoo!, am Enkuan dehna megebu!, sn Mauya!, so Soo dhawoow!, ja Yokoso!, zh Huanying!, hi Swagat hai!...); en + sw KEEP "Karibu sana!" as the brand home. The rest of the tour stays in the user's chosen language; TTS narrates the local line too (stepTexts prefers kickerRaw). (2) THE HOME UPDATE BANNER - a slim global strip at the top of home (pulse dot + "Updates available - tap to apply" + X): shown whenever /updates/check reports a different server version; checked on home init + every 30 minutes; TAP = the full Zyppar apply (caches cleared + SW unregistered + hard cache-busting reload); X dismisses THAT version only (persisted lk_updates_dismissed) - a new deploy re-shows it, so a stale dismissal can never hide a fresh update. Server build 75 (e797887) keeps the directive fresh - the Assistant may open a new user's chat with the same local warmth. Proofs: yarn build typecheck clean (Done in 48.53s); yarn build:prod clean (Done in 67.40s); base href /loopkeeper/; build:195 in main bundle; "tap to apply" in chunk 4789; "Bienvenue"/"Herzlich willkommen" shipped in www i18n assets.
