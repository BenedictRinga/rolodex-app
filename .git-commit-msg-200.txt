build 200: THE BLANKET CONTROLS + THE BILLING POLICY LEDGER (founder: item-by-item dismissal of the dock "is a notional form of false imprisonment - more polite to allow blanket manual dismissal or snooze"; and "we are deliberately delaying billing enforcement until statistics tell us app is sticky, which is best gathered from first adopters"). (1) THE BLANKET CONTROLS - the dock header now carries two taps beside the drag grip: SNOOZE (time icon) opens an inline picker - Quiet until 30 min / 1 hour / 9 AM (the morning-digest hour) - and lifts the ENTIRE pile out of the way; DISMISS-ALL (close-circle) clears it in one tap (service.clear). The snooze is honest, not a black hole: sticky and timed items return INTACT when the window ends (their timers re-arm on return); alerts arriving DURING the window join the pile silently (nothing lost, nothing nags); the snooze survives a reload (persisted pile + until in lk_notif_snooze; a window that ended while closed hands the pile back at boot). The controls are taps, not drag handles (onDragStart excludes .notify-hbtn/.notify-snooze), with light-mode styling. (2) THE BILLING POLICY - recorded in AGENTS.md as standing law: NO billing enforcement of any kind until the investor-portal statistics (retention cohorts D1/D7, DAU/WAU trend, session depth from first adopters) prove stickiness; the trial stays re-fillable at the user's instance (build 199 stitch); prices ($1 Basic / $5 Assistant) advertised in the billing modal but never enforced; no paywalls, trial locks, or nudge-to-pay flows until the founder calls the moment. Proofs: yarn build typecheck clean (Done in 408.06s); yarn build:prod clean (Done in 178.86s); base href /loopkeeper/; build:200 in main bundle; lk_notif_snooze persisted key in main bundle. No server change.
