build 329: THE DEAD BINDING, NAMED (founder saw the first-timer doors inside the regular view Inbox and demanded the cause; the founder ruled: let us see it). THE ONE-LINE FIX, NOTHING ELSE: the walk's app-first-minute was bound [fmPhase]=fmPhase - an input that does not exist on the component (its input is phase, default ring) - so the component could NEVER receive panel and forever rendered its default ring face (The reply I owe || The decision I keep not making) wherever it stood. Invisible for days while the settled Inbox bindings were starved to firstMinute=false; it surfaced the moment build 328 restored the live wiring. Now [phase]=fmPhase: the regular view 01 = TASKS || PERSON (fmPhase panel -> the component panel face, exactly as the founder law states); the cocoon 01 (the home-level gates) untouched. MEASURED: typecheck clean; prod clean; base href /loopkeeper/; build:329 in main (1); the walk chunk (7708) carries the fix; www/build.json {"build":329}. DEPLOY: app www 232-329 (server unchanged). VERIFY: boot/reload the regular view -> the Inbox's 01 shows TASKS || PERSON (the welcome title, the two doors, Show me first, the courtesy) - NOT the two avoidance doors; the first-timer view unchanged (the cocoon's own gates at home level).