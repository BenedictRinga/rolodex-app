build 198: THE ALERTS STAND UP (founder: "Do the algo alerts show outside the Settings environment where they almost always are waiting for me, and if everywhere in app, they are very fragile, disappearing at first touch, and therefore of little or no utility?" - the audit confirmed all three defects). (1) THE CORNER: the notification dock's default corner sat UNDER the home page's search FAB (bottom-right 16px), so alerts were only fully visible in Settings - the one surface with no fab; the default now clears the fab zone (88px) and the position key is bumped to v2 so existing installs re-land on the cleared corner once (dragged corners still persist). The dock itself is at app root (renders everywhere, z 99999 above modals). (2) STICKY: reminder nudges, loop-wake pings and wake catch-ups were auto-dismissed at 5s/8s - gone before the user looked up, the "disappearing at first touch" read; algo alerts are now duration:0 - they wait until the user taps them (tap-through escalates into Loops) or explicitly dismisses with the X; transient confirmations (toasts) keep their short timers. (3) THE CATCH-UP: a reminder that came due while the app was CLOSED used to vanish silently - the boot scheduler only armed FUTURE timeouts; catchUpMissedAlerts now fires anything overdue within 24h once on boot, kept exactly-once by a persisted fired-ledger (lk_alert_fired, entries pruned past 24h) - the alert WAITS for the user, but never nags twice. No user-facing flow changed - the alerts simply stopped being fragile. No server change. Proofs: yarn build typecheck clean (Done in 104.72s); yarn build:prod clean (Done in 113.14s); base href /loopkeeper/; build:198 in main bundle; lk_alert_fired ledger in chunk 5389.
