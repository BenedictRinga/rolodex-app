build 196: THE LIVE YARDSTICK (founder: the Yardstick's static graph "should catch and reflect actual stats", dynamic + pinch + "expandable to LoopKeeper's CommandCenter", like Zyppar's CommandCenter chart). (1) NEW SHARED COMPONENT components/yardstick-chart/ - a dependency-free SVG line/area chart ported from the Zyppar CommandCenter retention-chart pattern: fixed 720x220 viewBox, x by day-index, y scaled to a rounded-up max grid, gridlines + day labels as SVG primitives, dots on every day, and the 2026-09-11 zoomscroll fix verbatim in spirit: PINCH with the point under the fingers anchored, +- buttons with the center anchored, a "latest ->" jump, the SVG scaling to zoom% width inside a touch-action pan-x pan-y wrapper. (2) THE PORTAL'S YARDSTICK now LEADS with the live 14-day series - an ion-segment toggle: Opens (app_launch/day), Arrivals (deviceGrowth first-syncs/day), Loops closed (loop_closed/day), each with its own color - straight from the summary's dailyEvents + deviceGrowth (same pipeline as every number on the page), plus a Command Center expand button; the hand-drawn concept curve (the gold asymptote) stays below, honestly labeled "the shape we drive toward". (3) THE COMMAND CENTER's Trends section hosts the same chart full-size. (4) Server build 76 (630587a): topTimezones widened top 8 -> top 12 + the naming truth documented - Africa/Nairobi 126 is the KENYAN fleet (single-timezone country = one zone named for its capital; America/New_York is US Eastern only). No user-facing app flow changed - the investor story gained eyes. Proofs: yarn build typecheck clean (Done in 109.45s); yarn build:prod clean (Done in 91.60s); base href /loopkeeper/; build:196 in main bundle; yardstick chart in chunk 5389.
