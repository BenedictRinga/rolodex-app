build 191: THE SESSIONAL APERTURE + THE PLACE TABLE (founder's three asks). (1) THE APERTURE OPENS THE COMMAND CENTER AS A VIEW - the founder's aperture-outline icon in RolodexPage (added by hand in the header toolbar) is now gated by InvestorGateService (new root service, in-memory only): it stays HIDDEN until the Investor portal password (NorthStar) succeeds THIS session - the about-rolodex unlock handler calls investorGate.unlock() - and a fresh app start re-locks it (no storage, no persistence). The tap opens RolodexView.CommandCenter (new enum member in PagemanagerService), exactly the Settings contract: settingsWillOpen/settingsClosed so the remembered Inbox steps aside, the SAME sticky float header with the external Home icon (home-outline -> showRegularView), plus the console's own internal Close (closed -> showRegularView). The CommandCenterComponent self-fetches /investor/summary when opened without [stats] (NetworkService.safeFetch, quiet; loading/error notes; ngOnChanges refresh on re-open), and cc-shell now lays out in BOTH hosts (portal full-screen modal + in-page view, min-height floor). Defensive: a locked tap falls through to the portal word. (2) LIVE ANSWER via /investor/summary: DAU 26 organic devices in the last 24h (sync-active 1; 246 devices ever; WAU 27, MAU 101). (3) THE PLACE TABLE - "Region | Devices | LAN seems so shallow": the deeper cut was ALREADY flowing - locales.topTimezones (full IANA zone, city-level, e.g. Africa/Nairobi: 61 devices) has been computed server-side since the locales work; the portal's Presence section now renders it as a Place (device timezone) | Devices | Launches table above Region, with an honest caption: the place is the device clock's own zone - never IP, never GPS, no country lookup - as close to a country as we get without breaking the no-geolocation rule. No server change needed for (3); server stays at build 70. AGENTS.md: aperture/view + locale-depth obligations recorded. Proofs: yarn build typecheck clean (Done in 50.30s); yarn build:prod clean (Done in 66.12s); base href /loopkeeper/; build:191 in main bundle; unlockedThisSession in the rolodex chunk (4789); "Place (device timezone)" + topTimezones in the portal chunk.
