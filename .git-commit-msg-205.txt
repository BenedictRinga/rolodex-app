build 205: THE PHANTOM MACHINE IS CLOSED + THE FIRST DEED LEADS (founder: "Plan and code, unless you disagree" on the Grok phantom-audit; every audit fact was verified against the code before building - the audit text lives in D:/TODOs/USE_NOW.txt). (1) THE PHANTOM-ID FIX, the audit's central claim VERIFIED AND CLOSED: RolodexSyncService.loadDeviceId() ran synchronously in the constructor, read ONLY the in-memory cache (storage.getSync never touches IndexedDB), and on a cold start that lost the hydration race it minted a FRESH rolodex-<random> id AND setSync() PERSISTED it (setSync writes through to IndexedDB) - overwriting the device's true identity. Every losing cold start minted a phantom device; DAU 172 with D1 0 was that machine, not 172 people. FIX: the provisional id is NEVER persisted by loadDeviceId; hydrateDeviceId() ADOPTS the stored id on race loss (or finalizes the provisional when none exists); new getDeviceIdFinal() exposes the settled identity; AnalyticsService.init() awaits it before queueing app_launch - and the flush already stamps deviceId live, so even queued events heal to the true id. (2) LANDING_SOURCE QUEUES FIRST: the old order awaited the flag-write before queueing - a 3-second bounce dropped the whole landing (171 launches vs 3 landings was that bug); now the event queues immediately after the dedupe read, flag write fire-and-forget. (3) FIRST_LOOP_STARTED (Grok plan #1): one once-ever event (lk_first_loop_started) fires in LoopsService.create the moment the first loop of ANY kind exists - a self-loop needs no contacts, no permissions, no iPhone apology; it LEADS the Activation table (server 77, 01166df, also excludes probe-* from DeviceState totals). (4) THE INSTALL DOOR OPENS ON THE DEED (Grok plan #3): after first_loop_started, AppInstallService.promptInstallNow() fires - the deferred beforeinstallprompt was captured and left idle; the home-screen icon is the only return path without a store. (5) THE DIGEST ARMS ON THE FIRST LOOP (Grok plan #4): resyncDigest counts OUTSTANDING loops (open AND waiting) - one 9AM reason to return from the first deed onward. (6) MINE IS THE FIRST DOOR (Grok plan #2): on an EMPTY deck the walk's first slide carries an add-someone pill (addRequest -> the add sheet with its two tabs) - a stranger's first tap can be bringing someone in, not a demo name. Deliberate divergence, noted: the Welcome tour stays the brand's opening with its existing "Don't show again" skip - full deferral is the founder's call. Distribution reality stands as Grok said: WhatsApp word-of-mouth is the only open channel - pin ?src=wa-<group> on every shared link; add the founder's + tester device ids to the droplet's LK_NOISE_DEVICES so the organic line cleans itself. Proofs: yarn build typecheck clean (Done in 201.59s); yarn build:prod clean (Done in 86.01s); base href /loopkeeper/; build:205 in main bundle; first_loop_started + lk_first_loop_started in chunk 5389.
