build 208: THE SHOW BOOLEAN + THE TRANSLATION PORTAL SWEPT (founder's three directives on the 207 thread: "1. Not just 'Welcome was never going to show (already dismissed)' but check the Welcome show boolean. 2. Yes, do the locales translation portal sweep. 3. ...You do not seem to have updated the Welcome modal slides with my last prompt. The cards are still solely person focused"). (1) THE WELCOME SHOW BOOLEAN: presentWelcome returns Promise<boolean> - TRUE only when the tour actually presented and dismissed, FALSE on the dismissed-key path and on a failed modal; ALL THREE callers (init, showWelcomeAgain, presentFullWelcome) begin the Inbox boot reveal when it is false, so a session that never shows Welcome can never hold the reveal (BUILD 207's conditional now rides a real answer, not an inference from the dismissal key). (2) THE TRANSLATION PORTAL SWEEP, the "inexcusable" root-caused and fixed: en-US.json (which SHADOWS en.json for US-English devices) still carried the old person-only copy - that is why the founder's slides never changed. The four expanded-card keys (welcome.card.copy / welcome.card.emphasis / welcome.loopmotto.copy / welcome.fourws.copy) now carry the founder's person-AND-task scope in ALL 38 locales: en-US mirrors en verbatim; the six already-translated locales (de, es, fr, he, ru, sw) were EXPANDED in their own language; the 31 English-fallback locales received natural translations (af, am, ar, bs, by, da, el, es-eu/Basque, fil, ha, hi, ig, it, ja, nb_NO, nl, om, pl, pt-br, pt-PT, sk, sl, sn, so, sv, th, tr, ua, yo, zh-cmn-Hans, zh-cmn-Hant) - "every task you keep postponing is a card", "One person. Any Task. One card.", the promise surfaces "the person or the task you owe", the 4 W's tell "the story behind every card, person or task". Key counts preserved, JSON round-trip verified, built assets verified (en-US emphasis + yo/de copy in www/assets/i18n). Server 78 follows with the Grok-review gaps. Proofs: yarn build typecheck clean (Done in 261.82s); yarn build:prod clean (Done in 113.64s); base href /loopkeeper/; build:208 in main bundle.
