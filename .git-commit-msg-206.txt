build 206: THE TOUR SLIMS + THE MANAGING CARDS MODAL + THE BOOT REVEAL + THE PULL-UP (founder's four directives on the USE_NOW audit thread). (1) WELCOME SLIDES 5-8 REMOVED: the tour keeps the brand arc - intro, card, loopmotto, fourws, outro, taste (6 slides); the four removed slides (follow-up, Loop-O-meter, Confidante, storage) become the NEW ManagingCardsModalComponent (components/managing-cards-modal/), shown ONCE inside the user's FIRST attempt to add a contact or build a card (home.onCreateContact gate, lk_cards_modal_seen) - the material answers the question actually asked at that moment, and it is where the coming card kinds (reminder, notes, task, others) will teach; the four tour key-sets are reused verbatim so every translation ships with it. (2) THE BOOT REVEAL: the Inbox shell starts at 50vh on first view/reload/entry ([class.boot] on mount, 350ms hold) - the Demo/Contacts deck shows itself below - then GENTLY extends to its full 70vh (.9s cubic-bezier(.25,.8,.4,1) grow; expanded state unaffected at 85vh). Balances viewport usage and tells the user to go look down below later. (3) THE SLIDE-2 PULL-UP: between the flip icon and the top pill there was room - backrow margin 2px 0 10px -> 0 0 6px, chips margin-top 4px -> 0: all FIVE intentions (owed reply, promise, check-in, decision, show-up) plus the "or a line of your own" anchor are obvious at first glance, no scroll. (4) THE WELCOME-DEFERRAL ANSWER: the tour STAYS the brand's opening (now 6 slides) with its Don't-show-again skip; the DEFERRED stage is the first add/build-card attempt - the Managing Cards modal is where the tour's second act now lives. Proofs: yarn build typecheck clean (Done in 195.88s); yarn build:prod clean (Done in 58.31s); base href /loopkeeper/; build:206 in main bundle; mc-body + booting in chunk 5390. No server change.
