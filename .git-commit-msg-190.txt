build 190: APP_ERROR IN THE PIPELINE + THE COMMAND CENTER (founder: "Global app error analytics would be more scalable than our current crash ledger, and it can be accessible, along with much of the current Investor portal analytics, in a dedicated command center component via a button inside Investor, so that this portal does not get too cluttered"). (1) APP_ERROR IN THE PIPELINE - the global error hook (window.onerror + unhandledrejection, living in CrashReporterService since 2026-08-27) now ALSO fires the app_error analytics event: { type, page } categorical ONLY (never message text), 5s-per-type+page flood valve so a render-loop error cannot crowd the analytics queue; every error rides the batched, idempotent, hourly-capped ingest - no file growth, no SSH tail, portal-visible by construction. The /crashes JSONL ledger STAYS as the detailed stream (messages + stacks server-side); server build 70 counts appErrors7d/30d + a top-8 type breakdown. (2) THE COMMAND CENTER - new component components/command-center/ (ts/html/scss, declared in rolodex.module): the portal's operations console carrying 01 Reliability (now WITH the app_error rows alongside chat/draft failures, the crash ledger, and ingestFailures), 02 Timeline, 03 Rooms, 04 Translations - opened by a button card (section 07 Operations) and an index chip through an in-template ion-modal cssClass command-center-modal (global.scss full-screen, the build-178 pattern; controller-presented overlays are fragile). The portal keeps the growth story - Delta, Live, Presence, Retention, Activation, Events, Redesign (renumbered 07) - and the index chips follow; the moved helpers (hourLabel/keysCount/formatTime/openTranslationReview/barWidth) now live in the component that renders them; dead code removed from about-rolodex. AGENTS.md: reliability events + the decluttering rule (new telemetry goes to the Command Center, not the portal). Proofs: yarn build typecheck clean (Done in 270.90s); yarn build:prod clean (Done in 87.75s); base href /loopkeeper/; build:190 in main bundle; Command Center + reliability rows in chunk 5154; inv-reliability GONE from www (portal decluttered); app_error hook in main. Server companion: rolodex-server build 70 (ebe181b).