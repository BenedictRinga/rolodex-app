build 197: THE TWO-REPOSITORY SEARCH (founder: the search fab "should equally have two columns - so it can also search the device contacts list, not just LoopKeeper's, much like the add Contact applies to both repositories. Be mindful of any qualifications in the add format, such as permissions, to make sure search is not lacking"). The search fab's sheet (SearchModalComponent) now carries the SAME two tabs as the add sheet, with the SAME qualifications: (1) the people/phone segment shows in deviceSearch mode; (2) the phone pane speaks SEARCH — "the phone's own contact picker opens; its search bar searches your device list (a web app cannot read the device contacts directly)" — because the web Contact Picker API is PICK-ONLY: the OS picker's own search is the only honest device search, and that qualification is stated in the pane; (3) the picker door renders ONLY where the Contact Picker API exists (Android Chromium), and on iPhone the honest Apple-wall ladder leads: .vcf import + type one in (the add format's exact doors, reused verbatim with the same translated labels); (4) home.openSearchModal answers doors on onWillDismiss for warm user activation (picker + file dialog — the build-175/177 lesson) and waits onDidDismiss for row taps (never present a card into a dying overlay); the typed door flips the declared create-form modal (build 178); (5) the picked device person is MATCHED against LoopKeeper — phone digits (last 9, country-code tolerant) then exact display name: found -> their card opens; not found -> they are brought in from the phone (the AI device path's shape: mapPickedContact, icon blob to base64, prepend, trackListStartedOnce('picker'), "Added from your phone") and their new card opens; (6) every device search is tracked as send_exit { channel: 'device-contacts', surface: 'search' } — the signal-detector doctrine: searching outside the deck is an exit, logged. The add sheet, the walk picker and the Loops chooser are untouched (their props do not set deviceSearch). No server change. Proofs: yarn build typecheck clean (Done in 64.49s); yarn build:prod clean (Done in 86.01s); base href /loopkeeper/; build:197 in main bundle; "Search LoopKeeper & your phone" in chunk 5389.
