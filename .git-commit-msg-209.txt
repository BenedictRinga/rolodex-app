build 209: THE GROK-REVIEW GAPS, CODED (founder: "Implement your sequence" - the prereq build before walk-first; the gaps are Grok's verified findings on the 205 review, saved in D:/TODOs/USE_NOW.txt). (1) THE MINE DOOR LIVES (gap 1): the "Bring in someone real" pill was DEAD for the people we care about - Demo is ON by default, so the first-timer's who IS John Doe and the old *ngIf="!who" never fired; the door now opens whenever the REAL deck is empty (send-walk realDeckEmpty getter), speaks the walk's own words (loopkeeper.walk.whoAdd), and hands to the AGNOSTIC add sheet (whoRequest -> channelWhoFromWalk, two tabs + device picker + .vcf + typed card), never the raw OS Contact Picker's scary share dialog (the old addRequest + add.pickPhone). (2) THE DIGEST ARMS ON THE DEED (gap 2): LoopsService.create() was the only loop mutation that never called resyncDigest - the first loop stayed silent until some later mutation; now the 9AM digest material (open + waiting) exists from the first loop onward. (3) THE INSTALL GESTURE SURVIVES (gap 3): the once-ever first-deed flag (lk_first_loop_started) is hydrated at CONSTRUCTION (firstLoopDone field), so when storage has already answered the whole first-deed block runs SYNCHRONOUSLY inside the tap and beforeinstallprompt.prompt() keeps its user activation; the old storage.get().then() hop burned the gesture and fell back to the manual-instructions alert on top of the walk. (4) THE MOCK LEAK CLOSED (gap 5, app half): rolodex.component saveProfile() was the ONLY push() call site passing the RAW deck - Demo's John Doe deck rode into DeviceState and posed inside the "3,159 contacts" investor sum; it now filters isMockData exactly like home's realContacts() callers. Server 78 completes gap 5 server-side (probe-* out of the remaining DeviceState surfaces) and gap 6 (chat-directive). Proofs: yarn build typecheck clean (Done in 38.86s, incremental); yarn build:prod clean (Done in 63.05s); base href /loopkeeper/; build:209 in main bundle; realDeckEmpty in chunk 5390.
