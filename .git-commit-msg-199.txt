build 199: THE POCKET FM PLAYBOOK, LOOPKEEPER ADAPTED (founder: "All three, but the first 7 days exhaustion should be re-fillable at user's instance" - the mapped list recorded in AGENTS.md; the playbook file D:/TODOs/Pocket_FM Playbook.txt was always Zyppar-mapped, never LoopKeeper). (1) THE MORNING DIGEST (#2 daily-drops habit lever): the loop-wake service replaces per-loop pings with ONE 9AM notification/day listing the waiting loops by their handles ("3 loops waiting: Ada, the venue, the insurance"); every loop mutation (snooze/bring-back/drop/markSent/closeFully/remove/load) calls resyncDigest which cancels the previous schedule (legacy per-loop ids + the digest) and re-arms the single alarm while anything waits; NATIVE = the OS holds the alarm through Doze with the app closed; PWA = in-page timer to the next 9AM + once-per-day catch-up when the slot already passed (persisted day key lk_wake_digest_day); the dock nudge is sticky. (2) THE ACHIEVEMENT SHARE (#3 cliffhanger shares, organic): closing loops IS the achievement - a sticky success nudge fires at week-milestones (1/3/5/10/20/30 closed loops in 7d, once per level via lk_achievement_shown): "You have closed N loops this week - tell someone"; tap opens the share sheet (home.openShareApp). (3) THE TRIAL STITCH (#1 exhaustion stitching) - THE FORGOTTEN POLICY FOUND: /api/loopkeeper/trial/reopen (reopenable on demand, counts trialReopens) and draft-engine.reopenTrial() existed all along, but the 2026-08-26 pre-release flag PRE_RELEASE_RENEWAL silently refilled the trial behind the user's back; the flag is now FALSE (revert only on request) and an expired trial MEETS the user at boot with the free path: "Re-fill my 7 days" (reopenTrial + thanks toast), "Invite a friend" (share sheet), "Later" - at most once per 48h per expiry (lk_trial_stitch_at). AGENTS.md carries the full mapped backlog (all six levers with statuses). Proofs: yarn build typecheck clean (Done in 48.82s); yarn build:prod clean (Done in 46.88s); base href /loopkeeper/; build:199 in main bundle; lk_wake_digest_day + resyncDigest in chunk 5389. No server change.
