build 291: THE HONEST 404 (founder: 'Says write did not land into .env'). PROBE-PROVEN ROOT: curling the live endpoint answered 'Cannot POST /api/rolodex/ownfleet/noise' — the running server has NO such route, for two stacked reasons: (a) server 103 registered the endpoint at the /api/loopkeeper path, but the server's own alias middleware rewrites incoming /api/loopkeeper/* to /api/rolodex/* BEFORE routes match — a route registered at the loopkeeper path can never fire, whatever the deploy state (server 104, commit 88f1987, re-registers at the internal canonical path /api/rolodex/ownfleet/noise); (b) the running server predates 103 anyway. THE APP FIX: the console's write feedback now tells the truth per HTTP status — 401 'Key rejected — try again' (the key prompt resets); 404 'The server does not know this command yet — deploy server build 104, then try again'; any other failure names the HTTP code. MEASURED: typecheck clean (yarn build); prod clean (yarn build:prod); base href /loopkeeper/; www/build.json {"build":291}. DEPLOY: server 104 FIRST (git pull && pm2 restart rolodex-server), then app www 232-291. VERIFY: curl -s -X POST https://zyppar.com/api/loopkeeper/ownfleet/noise -H 'Content-Type: application/json' -d '{"key":"WRONG"}' returns 401 forbidden (never 404); then in the console, tick + Write lands the confirmation.
