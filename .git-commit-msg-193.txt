build 193: THE DISCOVERY LENS (founder: "no clue how they are discovering the app - we see no indicators of sharing or sending by earliest users"). (1) landing_source - a once-per-device event from AnalyticsService.init(): ref = document.referrer reduced to host+path, query string STRIPPED (no tokens or campaign ids can leak - the crash reporter's rule; 'direct' when there is none, which typed URLs, WhatsApp webviews and forwarded plain links all produce), src = a short ?src= tag the founder may pin when posting (sanitized to [a-z0-9_-]{1,24}); the loopkeeper_landing_sent storage flag makes it exactly-once per device ever, and the existing consent gate already covers it. (2) Command Center section 06 Discovery (amber): the 14-day ARRIVAL record - bars of devices per first-sync day (trialStartedAt birth dates, server build 73's deviceGrowth) - above the landing-source table (top-8 referrers, 30d), with the honest note that WhatsApp forwards of the plain link land as 'direct' by design (build 176 made friend links unattributed on purpose) and that pinning ?src= tags on future posts turns communities into measurable channels. Bursts in the arrival record + posted tags = the discovery story; a flat tail after a burst = churn. No user-facing app flow changed. Server companion: rolodex-server build 73 (cae86bc) - deploy first or section 06 stays hidden. Proofs: yarn build typecheck clean (Done in 68.40s); yarn build:prod clean (Done in 80.32s); base href /loopkeeper/; build:193 in main bundle; landing_source in main + "How they arrive" in chunk 4789.
