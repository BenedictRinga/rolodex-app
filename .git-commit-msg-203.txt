build 203: THE FLIP GAP TRIMMED + THE SHELL SHORTENED (founder: "Between the Inbox card and the flip button above it, we have some space to pull up further, perhaps 10px, or maybe 7px. Then also shorten the Inbox from 75 to 70vh we see"). (1) THE FLIP GAP: the flip button (the card-flip mark on the Loops shelf, .li-flip) carried padding: 2px 0 10px - the 10px BOTTOM pad was the exact space between the flip button and the inbox main card below it; now padding: 2px 0 2px - the card below rises ~8px, inside the founder's 7-10px band. (2) THE SHELL: resting height 75vh -> 70vh (height/min-height/max-height in .li-shell); expanded holds at 85vh; the deck's sneak peek below the shell grows accordingly. Proofs: yarn build typecheck clean (Done in 429.76s); yarn build:prod clean (Done in 223.02s); base href /loopkeeper/; build:203 in main bundle; ".li-flip{...padding:2px 0}" verified in built chunk 5389. No server change.
