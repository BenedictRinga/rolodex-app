build 204: THE AIR RECLAIMED, NOT THE DECK (founder: "The idea is to get more material into the initial viewport without simultaneously over-suppressing the contacts card display space. What you have done has cost us the view of the first pill under 'Ma's doctor'"). The shell stays at 70vh - the contacts deck below keeps its display space - and the walk slide's own dead air yields instead: (1) the .sw-lower decorative spacer above the slide (build 162's "bring the slide down lower") was swallowing up to 88px of pure air - clamp(20px,9vh,88px) -> clamp(12px,6vh,44px); (2) the Who card's padding 22px 18px 20px -> 16px 18px 14px, its internal gap 10 -> 8 (the avatar keeps its 84px first impression); (3) rhythm margins tightened (sw-garden and sw-selfrow bottom 14 -> 10). Net: the first pill under the typed line ("A decision I keep not making") is back inside the viewport at 70vh, the Who card stays the first impression, the deck peek below the shell is untouched. No server change. Proofs: yarn build typecheck clean (Done in 109.25s); yarn build:prod clean (Done in 82.15s); base href /loopkeeper/; build:204 in main bundle.
