build 201: THE INBOX BREATHES + THE GARDEN REORDERED (founder: "Extend the home page inbox length to 85% of viewport so we do not have items hiding, and lose impact of first impressions. It also forces loopkeeper icon panel into footnote territory which is better. But perhaps even just 75% could do but only if we re-prioritize by taking the 'From your loops' section back down below 'Ma's doctor...' and its two accompanying pills that should come immediately after 'Not this one'. ... we should be allowing enough space for the panel so that first-timer also sees a sneak peek of the demo profiles"). (1) THE INBOX SHELL: 62vh -> 75vh resting, 78vh -> 85vh expanded (loop-inbox.component.scss .li-shell) - items stop hiding, the icon panel rides into footnote territory (which the founder calls better), and the first-timer's demo-profile deck peeks below the shell. (2) THE WALK SLIDE-1 GARDEN REORDER (send-walk.component.html): the pills (the user's own loop subjects - "Ma's doctor", the school, the insurance) and the typed line now come IMMEDIATELY after "Not this one"; the "From your loops" label steps DOWN beneath them as a quiet caption (scss margin-top replaces margin-bottom) - the deed leads, the explanation follows, a natural first-timer flow. Proofs: yarn build typecheck clean (Done in 84.15s); yarn build:prod clean (Done in 138.80s); base href /loopkeeper/; build:201 in main bundle; sw-garden markup in chunk 5389. No server change.
